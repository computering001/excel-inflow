# P0.2 micro-checkpoint (fresh-model handover paragraph)

On `agent/excel-inflow-v377-behavioural-closure` (this commit, parent `6a64f74`), the invariant "every known blocker reproduces deterministically or is explicitly classified as non-reproducible with missing evidence named" is proven: `test-corpus/blockers/corpus_manifest.json` registers the six mandatory Phase 0 cases with pre-normalisation SHA-256 hashes; `scripts/run_blocker_corpus_tests.mjs` (registered as `blocker-replay-corpus`) passed 80 checks — 7 fixtures hash-verified, a corrupted digest refused with the typed `BLOCKER_CORPUS_FIXTURE_REJECTED` (negative self-test), and three fast replays matched their documented signatures, including reproducing the KNOWN-FAILING stale-version-pin signature (`'3.7.6' !== '3.7.5'`, run_release_identity_governance_tests.mjs:29) that proves the registered CI gate is untruthful at exact head. Registry contract passed (695) and the oracle matrix passed (1.0) after the registry pin rebind. Evidence-gap declarations: AstraZeneca 2026-08-11 raw live bytes, v57-era ETR bytes and Rogo stall logs were never exported and are named as gaps with minimised reproducers. Frozen run output: `programme/P0.2_reproduction_report.json`. Next package is P0.3 (make exact-head CI truthful); its first failing command is `node scripts/run_release_identity_governance_tests.mjs` (currently exits non-zero on the stale pin), and its seal must flip the corpus case `package-identity-discrepancy` to PASS in the same commit.
