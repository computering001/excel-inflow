# Work Package Issue Card

- Work package ID: P0.1
- Title: Freeze the evidential baseline and create the closure branch
- Active invariant: Every result can be tied to one exact source commit, package identity and raw-input hash set.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: github.com/computering001/excel-inflow · `agent/excel-inflow-v377-behavioural-closure` cut from `433bd237b73e15bc844d5cd64175f0758ebe1d9f` (`agent/excel-inflow-v376-package-repair` head)
- Working tree status: clean at freeze (`git status --porcelain` empty)
- Observed failure or discovery question: No frozen, immutable baseline identity existed; the closure branch did not exist; no receipt bound source identity to an archive hash.
- Earliest responsible layer: programme control (repository identity), no product code
- Reproduction command: `git rev-parse 433bd237b73e15bc844d5cd64175f0758ebe1d9f^{tree}` (expects `d9c9fa56d3b146251e5dc89625bffee0c9675795`)
- Focused failing test ID: n/a (discovery package) — proofs are the three mandatory verifications below
- Allowed files/modules to change: `programme/*`, git tag `excel-inflow-v376-evidence-baseline`, branch `agent/excel-inflow-v377-behavioural-closure`; external archive under private custody
- Explicitly out of scope: all product source (`scripts/`, `assets/`), goldens, CI workflow semantics
- Required independent/physical proof: fresh-checkout tree equality against recorded tree hash
- Required integration gate: dirty-working-tree negative test against the existing release-compilation guard
- Raw input/evidence hashes: baseline archive sha256 `422fea6b6222514a94f71b2fd639608340d56fc4bb80583b75c5e15edc61cf38`; per-file manifest `programme/baseline_source_sha256.txt`
- Last known green commit: `433bd237b73e15bc844d5cd64175f0758ebe1d9f`
- Rollback plan: `git checkout excel-inflow-v376-evidence-baseline` (immutable tag) reproduces tree `d9c9fa56...` byte-for-byte
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification

- Intended correction: none (this package changes no economics; only programme artifacts added)
- Presentation-only: none
- Accepted normalization: two tracked symlinks (`fixtures/external/Codex`, `fixtures/external/codex-runtimes`) are recorded in the manifest by their link-target string, since their content lives in external custody by design
- Regression: none
- Unresolved: none

## Completion receipt

- Commit: (this commit — P0.1 seal on `agent/excel-inflow-v377-behavioural-closure`)
- Commands and exit codes:
  - `git tag -a excel-inflow-v376-evidence-baseline 433bd23...` → 0
  - `git branch agent/excel-inflow-v377-behavioural-closure 433bd23...` → 0
  - fresh clone + `git checkout 433bd23...` → tree `d9c9fa56...` == recorded → PASS
  - `git archive | gzip -n | shasum -a 256` (fresh clone) → `422fea6b...` == archive → PASS
  - dirty file + `node scripts/run_current_package_source_identity_check.mjs --out ...` → exit 1, typed refusal "Package/source identity check requires a clean worktree." → PASS (guard pre-exists at `scripts/run_current_package_source_identity_check.mjs:24`; no new guard added)
  - branch-origin: `git rev-parse agent/excel-inflow-v377-behavioural-closure` == baseline commit → PASS
- Evidence paths/hashes: `programme/baseline_receipt.json`; `programme/baseline_source_sha256.txt` (526 files + 2 symlinks); archive `/Users/archiepreston/Documents/Codex/private-test-custody/excel-inflow-baselines/excel-inflow-v376-evidence-baseline-433bd23.tar.gz` (chmod 444)
- Residual risks: the baseline tag is only protectable locally and on origin by convention — GitHub tag-protection rules are an org setting the model cannot set; the receipt records the hash so any tag movement is detectable.
- Next package first action (P0.2): enumerate every historical live failure named in the phase cases (AstraZeneca blocker, Apple US-GAAP canary, prior working-capital blocker, ETR recursion/normalisation, package identity, deadline overrun) and, for each, locate raw inputs + expected terminal classification; discovery command: search the repo and custody stores for existing fixtures per case before authoring anything new.
