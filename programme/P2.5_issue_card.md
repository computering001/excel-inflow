# Work Package Issue Card

- Work package ID: P2.5
- Title: Statement-family residual treatment + protected-identity role externalization (FOOTING SLICE)
- Active invariant: A material statement-family total that does not foot against its members receives an explicit typed treatment (refusal finding with provenance) — it never silently passes as source_input; an empty member-set is recorded as a typed finding, never skipped; the protected-identity role set is a declared asset with pinned membership (roles may be added, never removed).
- Release/phase: v3.7.7 / Phase 2
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (on top of `3d4e2ef`)
- Working tree status: this work package's files only
- Observed failure or discovery question: The P2 gap agent proved residual treatment existed only for debt pools (coverage residual pool) and the broker bridge (residual plug rows) — statement families had none: a material reported_parent total whose filed values do not sum to its working children compiled model IR v3 to proof.status PASS with 0 blockers and 0 warnings (red proof: total [100,100,100] vs member sum 90 → PASS/0/0); an empty member-set also compiled PASS with 0 findings; the protected-identity role set was a hardcoded Set literal at scripts/lib/model_ir_v3.mjs:847 with no membership pin.
- Earliest responsible layer: compileModelIrV3 (the case compiler's reported_parent branch `continue`s past its own face_additivity footing check, so the sealed IR was the earliest layer that could still refuse); workbookSemanticProofContract for the role literal
- Reproduction command: `node scripts/run_statement_family_residual_tests.mjs`
- Focused failing test ID: statement-family-residual (39 checks incl. mutations: (a) unfooted material total must BLOCK, (b) empty member-set surfaces STATEMENT_FAMILY_EMPTY_MEMBER_SET, (c) removing any role from the protected set fails the pin)
- Allowed files/modules: scripts/lib/model_ir_v3.mjs (footing pass + typed findings + role externalization; exported signatures unchanged), assets/protected-identity-roles-v1.json (new declared asset), scripts/run_statement_family_residual_tests.mjs (new suite), this card
- Explicitly out of scope (REMAINING, on the work order): minting a visible residual line in the case compiler (a validator records/refuses, it never repairs); footing balance-sheet-style families outside income_statement/cash_flow; wiring the new finding codes into the terminal-reason registry
- Required integration gate: user-flow 14/14, filings 43, case-compiler equivalence byte-identical to pre-change baseline (zero new diffs; the cohort's pre-existing 55 economic diffs / 90 compile blocks / 1 failed solve already exit 1 at `3d4e2ef` with this package stashed AND with HEAD solver.mjs — documented in P4.2's card — and contain zero STATEMENT_FAMILY findings), layered-graph constitution 8 mutations / 0 violations (standard-maximal-v2), statement-family-residual 39 — met
- Last known green commit: `3d4e2ef`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: compileModelIrV3 now foots every statement-family total that asserts an independently filed series (source_input face totals and the retained reported series behind reported_total_reconciled): a material mismatch beyond the filed rounding tolerance is a STATEMENT_FAMILY_UNFOOTED_TOTAL blocker carrying display_ids/period/filed/members_sum; an immaterial mismatch is the same finding recorded as a warning; a missing member value is never coerced to zero — the period is recorded as STATEMENT_FAMILY_UNFOOTABLE_PERIOD (material totals); an empty member-set records STATEMENT_FAMILY_EMPTY_MEMBER_SET; a rendered protected-role row with zero member edges records PROTECTED_IDENTITY_EMPTY_MEMBER_SET. The protected-identity role set moved from the line-847 literal to assets/protected-identity-roles-v1.json, exported as frozen PROTECTED_IDENTITY_ROLES — same six roles, membership pinned.
- Regression: none (both certified fixtures still compile PASS with zero warnings; all four integration gates green; formula-owned derived_formula totals assert no filed series and are deliberately not footed)
- Unresolved: residual-line minting with provenance belongs to the compiler layer (work order); new finding codes not yet in the terminal-reason registry

## Regression follow-up
- CI at 5154936 exposed four suite failures (phase9-broker-end-to-end-scenarios, degraded-broker-delivery, prebroker-ownership-controller, stage4-content-addressed-resume): compileModelIrV3 BLOCKed the canonical synthetic donor with STATEMENT_FAMILY_UNFOOTED_TOTAL (pre_tax_income 150 vs "3 members sum to 145"; cash_flow_profit_before_tax 150 vs 145).
- Root cause classification: (a) — the footing pass mis-derived member values. The donor's FILED evidence foots (IS face: 150 = operating 150 + finance income 0 + finance costs 0; CF face: 119 + 31 = 150), and the case compiler's face_additivity passes. In zero-broker "Forecast Waterfall" mode only, applyFiledFinanceHistoricalAuthority (scripts/lib/forecast_candidate_compiler.mjs:271) deliberately restates interest_expense history to -|historical_interest_reconciliation.reported_interest| = -5 (the schedule basis, per the declared basis "filed_finance_expense_including_lease_interest"), and synchronizeDerivedHistoricalForecastCaches caches derived evaluations (cash_flow_net_income link → 114). My pass summed those SCHEDULE-/FORMULA-owned model caches as if they were the filed member series and refused a false contradiction. The face-vs-schedule interest divergence is already governed by the typed historical_interest_reconciliation plug contract — not a face-footing defect.
- Repair (earliest responsible layer = the P2.5 footing pass itself, scripts/lib/model_ir_v3.mjs): member filed-series lookup is now authority-aware — source_input members contribute via values, reconciled members via reported_historical_values; schedule_link/derived_formula (any non-face-owned) member history makes the period unfootable-from-face and is RECORDED as STATEMENT_FAMILY_UNFOOTABLE_PERIOD with reason "non_face_member_history" (delegation to the owning schedule/derivation contract), never refused and never summed. Filed-domain refusals are unweakened (suite check: source_input members that genuinely mis-foot still BLOCK; tolerance untouched).
- Red proof: suite checks 8–10 (donor-shaped schedule_link member -5 under reported 150; derived link member cache 114 under reported 150) failed against the pre-repair pass (BLOCK/no reason field); green post-repair. Donor no-pack path verified end to end: IR PASS, 0 family blockers, typed non_face delegations on pre_tax_income / cash_flow_profit_before_tax / CGFO / cash_from_investing / cash_from_financing.
- Note: the donor evidence remains internally tense (IS face prints finance costs 0 while its historical_interest_reconciliation lane declares reported interest 5, sourced from cf.finance_costs_paid); the emitted workbook's pre-tax historical formula prints the schedule basis (145) against a filed 150, governed by the reconciliation plug contract. If a future package wants the donor face itself consistent, the owner is evidence_run.mjs's face projection (resolve() prints empty schedule shells as 0) — changing it cascades into every pinned donor number and the raw-canary pin, so it is reported here, not done.

## Completion receipt
- statement-family-residual 39 → 0 (red proof pre-repair: unfooted material family PASS/0 findings, empty member-set PASS/0 findings; post-repair both typed); regression follow-up extends the suite to 44 (donor-shaped schedule/derived member checks 8–10, red against the pre-repair member derivation)
- Protected roles pinned: cash_from_operations, cash_from_investing, cash_before_financing, cash_from_financing, net_change_in_cash, ending_cash — removing any = FAIL; contract identities on both fixtures unchanged
