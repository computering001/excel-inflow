# Work Package Issue Card

- Work package ID: P3.6a
- Title: Execute the two proven latent row/node-identity defects (PRE-P3.6 SLICE): J9 group-key label join and S6 rows_by_id silent overwrite
- Active invariant: Instrument→group-subtotal edges join on the minted GROUP KEY (a declared group whose subtotal node is absent is a typed violation, never a silently dropped edge); every rows_by_id identity has exactly one writer, with the deliberately-masked cash-interest pair enumerated in an exported allowlist that P3.6 proper retires.
- Release/phase: v3.7.7 / Phase 3 (pre-P3.6)
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on `5154936`, uncommitted per work order)
- Working tree status: two modified files + one new suite + this card; commit withheld by instruction
- Observed failure or discovery question: (1) J9 — semantic_graph.mjs joined instruments to debt/interest group subtotals via normaliseGroupKey(display_group LABEL); DEBT_PRESENTATION_GROUPS key `unclassified_review` carries label "Unclassified — review required" which normalises to `unclassified_review_required` ≠ the key, and link() silently dropped edges to absent nodes — every instrument in the review group lost both subtotal edges with no violation (proven red: subtotal node present, edge absent). (2) S6 — rows_by_id had 24 mint sites with only the statement allocator guarded; a synthetic statement row `minimum_cash` compiled silently with the waterfall mint overwriting it (proven red: statement row 67 silently shadowed by row 138), the same shape that masks the schedule's deliberate reuse of `cash_interest_paid` behind the statement-first fallback at build_dynamic_model.mjs:1251-1258.
- Earliest responsible layer: row_plan.mjs mints (label carried without its key; unguarded rows_by_id writers) and semantic_graph.mjs group join (label re-derivation + silent link drop)
- Reproduction command: `node scripts/run_row_identity_defect_tests.mjs`
- Focused failing test ID: row-identity-defects (21 checks; written RED first — pre-repair: KNOWN_ROW_ID_COLLISIONS export absent, unclassified-review subtotal/interest edges absent, absent-group-node case silent, synthetic new collision compiled without a throw)
- Allowed files/modules: scripts/lib/row_plan.mjs (additive: display_group_key on the instrument plan mint; KNOWN_ROW_ID_COLLISIONS + mintPlanRowId guard on all 23 non-statement mint sites), scripts/lib/semantic_graph.mjs (display_group_key on the instrument node; key-based group join with typed ECONOMIC_GRAPH_GROUP_NODE_ABSENT violation; label-normalising helper removed), NEW scripts/run_row_identity_defect_tests.mjs, this card
- Explicitly out of scope (REMAINING P3.6, on the work order): the canonical two-part identity mint (Part A/Part B), the bijectivity suite, retiring the build_dynamic_model.mjs:1251-1258 statement-first mask and with it KNOWN_ROW_ID_COLLISIONS, the raw-join census gate
- Required integration gate: user-flow, filings pipeline, layered-graph constitution (standard-maximal-v2), forecast topology, forecast behavior, new suite, node --check on touched files — all green; full custody-cohort equivalence remains the pinned KNOWN RED with totals UNCHANGED (55 path diffs / 90 compile blocks / 1 failed compiled solve / 0 hard plan diffs / 0 failed plan compiles)
- Last known green commit: `5154936` (gate green before edits; equivalence cohort already pinned red per EQUIVALENCE_COHORT_BASELINE.md)
- Rollback plan: discard the working-tree edits to the two library files and delete the new suite + this card
- Attempt count (1-3): 1
- Status: DONE (uncommitted working tree per work order; seal belongs to the parent session)

## Difference classification
- Intended correction: instrument plans and nodes now carry display_group_key alongside the unchanged label; the group join binds on the key and throws typed ECONOMIC_GRAPH_GROUP_NODE_ABSENT when a declared group's subtotal node is absent; all 23 non-statement rows_by_id mints route through a duplicate guard that throws typed ROW_PLAN_DUPLICATE_ROW_ID for any collision outside the exported KNOWN_ROW_ID_COLLISIONS (cash_interest_paid, cash_interest_received). No label text changed; runtime lookup behavior for the known collision family unchanged (schedule row still wins rows_by_id; statement row still survives in statement_rows for the consumer fallback).
- Regression: none — full gate green; collision census across the gate suites AND the full custody cohort minted zero duplicates, so the guard fires on nothing shipped today.
- Unresolved: the mask itself (build_dynamic_model.mjs:1251-1258) is deliberately retained for P3.6 proper to retire together with KNOWN_ROW_ID_COLLISIONS; no shipped fixture exercises the unclassified_review group end-to-end because contract-v2 instrument period state rejects `unclassified` instruments (the suite compiles the state pre-mutation to reach the group — P3.6 should decide whether that reachability gap is itself a defect).

## Completion receipt
- row-identity-defects: red → {"status":"PASS","checks":21}
- Gate: run_user_flow_tests 0, run_filings_pipeline_tests 0, run_layered_graph_constitution_tests (standard-maximal-v2) 0, run_forecast_topology_tests 0, run_forecast_behavior_tests 0, node --check clean on both touched files
- Equivalence custody cohort re-run under the guard: exit 1 with baseline-identical totals 55/90/1/0/0 (KNOWN RED unchanged)
