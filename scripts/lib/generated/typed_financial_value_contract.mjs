// GENERATED by compile_canonical_contracts.mjs — do not edit.
// Contract: typed_financial_value v1.0.0
// Validation walks the DEFINITION semantics directly (objects, maps, arrays,
// enums, consts, discriminated unions, refs, patterns) — deliberately NOT the
// repository JSON-Schema validator, which does not enforce oneOf/$ref.
export const CONTRACT_NAME = "typed_financial_value";
export const CONTRACT_VERSION = "1.0.0";
export const SUPPORTED_VERSIONS = ["1.0.0"];
const ENUMS = {"ValueState":["reported_number","reported_zero","reported_blank","nil","missing","parse_failure","period_support_required","prior_filing_support","derived_number","captured","not_applicable","unresolved"]};
const TYPES = {"SourceLocation":{"kind":"object","fields":{"document_sha256":{"type":"string","pattern":"^[a-f0-9]{64}$","required":true},"page_or_note":{"type":"string","required":false},"source_line_id":{"type":"string","required":false}}},"Derivation":{"kind":"object","fields":{"operator":{"type":"string","required":true},"refs":{"type":"array","items":{"type":"string"},"required":true}}},"PriorFilingProvenance":{"kind":"object","fields":{"prior_document_sha256":{"type":"string","pattern":"^[a-f0-9]{64}$","required":true},"prior_source_line_id":{"type":"string","required":true},"prior_period_index":{"type":"number","required":true}}}};
const ROOT = {"kind":"discriminated_union","discriminator":"state","variants":{"reported_number":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"reported_number","required":true},"value":{"type":"number","required":true},"raw_text":{"type":"string","required":true},"currency":{"type":"string","required":false},"unit_scale":{"type":"number","required":false},"precision":{"type":"number","required":false},"source_location":{"type":"ref","ref":"SourceLocation","required":false}}},"reported_zero":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"reported_zero","required":true},"value":{"type":"const","const":0,"required":true},"raw_text":{"type":"string","required":true},"source_location":{"type":"ref","ref":"SourceLocation","required":false}}},"reported_blank":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"reported_blank","required":true},"source_location":{"type":"ref","ref":"SourceLocation","required":false}}},"nil":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"nil","required":true},"raw_text":{"type":"string","required":true},"source_location":{"type":"ref","ref":"SourceLocation","required":false}}},"missing":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"missing","required":true}}},"parse_failure":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"parse_failure","required":true},"raw_text":{"type":"string","required":true},"failure_reason":{"type":"string","required":true},"source_location":{"type":"ref","ref":"SourceLocation","required":false}}},"period_support_required":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"period_support_required","required":true},"period":{"type":"string","required":true}}},"prior_filing_support":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"prior_filing_support","required":true},"value":{"type":"number","required":true},"provenance":{"type":"ref","ref":"PriorFilingProvenance","required":true}}},"derived_number":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"derived_number","required":true},"value":{"type":"number","required":true},"derivation":{"type":"ref","ref":"Derivation","required":true}}},"captured":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"captured","required":true},"capture_parent_id":{"type":"string","required":true}}},"not_applicable":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"not_applicable","required":true}}},"unresolved":{"fields":{"contract_version":{"type":"string","pattern":"^\\d+\\.\\d+\\.\\d+$","required":true},"state":{"type":"const","const":"unresolved","required":true}}}}};
export const ValueState = Object.freeze(["reported_number","reported_zero","reported_blank","nil","missing","parse_failure","period_support_required","prior_filing_support","derived_number","captured","not_applicable","unresolved"]);

function checkField(field, value, at, errors) {
  switch (field.type) {
    case "string":
      if (typeof value !== "string") errors.push(`${at}: expected string`);
      else if (field.pattern && !new RegExp(field.pattern).test(value)) errors.push(`${at}: pattern mismatch`);
      break;
    case "boolean":
      if (typeof value !== "boolean") errors.push(`${at}: expected boolean`);
      break;
    case "number":
      if (typeof value !== "number" || Number.isNaN(value)) errors.push(`${at}: expected number`);
      break;
    case "null":
      if (value !== null) errors.push(`${at}: expected null`);
      break;
    case "const":
      if (value !== field.const) errors.push(`${at}: expected const ${JSON.stringify(field.const)}`);
      break;
    case "enum":
      if (!ENUMS[field.enum].includes(value)) errors.push(`${at}: ${JSON.stringify(value)} not in enum ${field.enum}`);
      break;
    case "array":
      if (!Array.isArray(value)) errors.push(`${at}: expected array`);
      else value.forEach((item, index) => checkField(field.items, item, `${at}[${index}]`, errors));
      break;
    case "map":
      if (!value || typeof value !== "object" || Array.isArray(value)) errors.push(`${at}: expected object map`);
      else for (const [key, item] of Object.entries(value)) checkField(field.values, item, `${at}.${key}`, errors);
      break;
    case "ref":
      checkDefinition(TYPES[field.ref], value, at, errors);
      break;
    default:
      errors.push(`${at}: unknown field type ${field.type}`);
  }
}

function checkObject(fields, value, at, errors) {
  if (!value || typeof value !== "object" || Array.isArray(value)) {
    errors.push(`${at}: expected object`);
    return;
  }
  for (const [name, field] of Object.entries(fields)) {
    if (!(name in value)) {
      if (field.required) errors.push(`${at}.${name}: required field absent`);
      continue;
    }
    checkField(field, value[name], `${at}.${name}`, errors);
  }
  for (const name of Object.keys(value)) {
    if (!(name in fields)) errors.push(`${at}.${name}: unexpected field`);
  }
}

function checkDefinition(definition, value, at, errors) {
  if (definition.kind === "object") {
    checkObject(definition.fields, value, at, errors);
  } else if (definition.kind === "discriminated_union") {
    for (const variant of Object.values(definition.variants)) {
      const attempt = [];
      checkObject(variant.fields, value, at, attempt);
      if (attempt.length === 0) return;
    }
    errors.push(`${at}: no union variant matched`);
  } else {
    errors.push(`${at}: unknown definition kind`);
  }
}

/**
 * Validate a typed_financial_value object at its producer boundary.
 * Unknown contract_version values are REFUSED, never reinterpreted.
 */
export function validate(candidate) {
  if (!candidate || typeof candidate !== "object") {
    return ["the candidate must be one object"];
  }
  if (!SUPPORTED_VERSIONS.includes(candidate.contract_version)) {
    return [
      `unsupported contract_version ${JSON.stringify(candidate.contract_version)}; supported: ${SUPPORTED_VERSIONS.join(", ")} — an old object must migrate explicitly, never silently reinterpret`,
    ];
  }
  const errors = [];
  checkDefinition(ROOT, candidate, "$", errors);
  return errors;
}
