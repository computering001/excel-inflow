# Work Package Issue Card

- Work package ID: P1.7
- Title: Ban unsafe nullable-to-number coercion across languages
- Active invariant: Blank, nil, missing, parse failure and unresolved never become zero — no NEW coercion can enter, and every existing one is inventoried for migration.
- Release/phase: v3.7.7 / Phase 1
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: clean at seal
- Observed failure or discovery question: 399 distinct coercion lines (436 occurrences) live in JS+Python sources; 252 classified suspect_value_coercion by reading each line.
- Earliest responsible layer: repository tooling (fingerprint-bound linter) + inventory
- Reproduction command: `node scripts/run_coercion_ban_tests.mjs` (verify) / `--write` (regenerate, classification-preserving, byte-idempotent)
- Focused failing test ID: coercion-ban (registered; 2004 checks; three built-in negative self-tests; content-fingerprints not line numbers; occurrence counting so copying an inventoried line also fails; retired entries reported non-fatally)
- Allowed files/modules: scripts/run_coercion_ban_tests.mjs + ci/coercion_inventory.json (subagent-authored); registry + pins + audit census (me)
- Explicitly out of scope: REPAIRING the inventoried coercions — that is the migration the inventory exists for; owners assigned below
- Required integration gate: registry 770, ci-census 315, governance — green
- Last known green commit: `4857acb`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED

## Top migration targets (owner phases assigned)
1. verify/tolerances.py:78 — the ORACLE's numeric reader coerces blank→0.0 (owner: P7.6 cross-language oracles; the verifier must embody typed-value semantics)
2. verify/validate_dynamic_model.py:2726 — additivity check treats missing parent as 0 (owner: P7.6)
3. lib/solver.mjs:721 — solver commits unresolved values as 0 (owner: P4.7 solver graph)
4. lib/fixed_point_constitution.mjs:25 — fixed-point identities read missing roles as 0 (owner: P4.7)
5. lib/validation_invariants.mjs:356-362 + verify/invariants.py:929 — instrument roll-forward invariant computed from ?? 0 defaults (owner: P4.3 instrument-period states)

## Difference classification
- Intended correction: ban-new mechanism live; no behaviour changed
- Regression: none
- Unresolved: the 252 suspect migrations (tracked by fingerprint; owners named)

## Completion receipt

- Commands and exit codes: coercion-ban 2004 → 0 (verify mode; --write byte-idempotent); registry 770 → 0; census green
- Evidence: ci/coercion_inventory.json (399 lines/436 occurrences, classified 140/252/7); top-5 migration targets with owner phases in this card
- Residual risks: 252 suspect coercions are tracked migration debt, fingerprint-bound
- Next package: P1.8 (sealed same session)
