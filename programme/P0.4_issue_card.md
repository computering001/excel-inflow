# Work Package Issue Card

- Work package ID: P0.4
- Title: Version the global support-envelope contract
- Active invariant: Every case is classified CERTIFIED, SUPPORTED_DEGRADED, EXPERIMENTAL or UNSUPPORTED before expensive source processing, from intake facts alone.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit; parent `7743229`)
- Working tree status: clean at seal
- Observed failure or discovery question: No machine-readable support envelope existed anywhere in the repository; support claims lived in narrative only.
- Earliest responsible layer: product contract (versioned envelope) + pure classification module
- Reproduction command: `node scripts/run_support_envelope_tests.mjs`
- Focused failing test ID: support-envelope (registered; 87 checks — table-driven per dimension value, worst-of aggregation, five early-stop predicates, boundary cases, terminal-state mapping, example completeness)
- Allowed files/modules to change: `assets/support-envelope-v377.json` (new), `scripts/lib/support_envelope.mjs` (new, pure), `scripts/run_support_envelope_tests.mjs` (new), `assets/excel-inflow-vnext-run.schema.json` (+optional support_envelope identity), `scripts/run_excel_inflow_vnext.mjs` (receipt stamp only), `assets/deployment-profile.json` (allowlist), registry + oracle pin
- Explicitly out of scope: intake-side classification wiring (the classifier is not yet CALLED at preflight — that lands with Phase 2 statement-surface/entity work, where entity_type facts become available); workbook quality-panel stamp (Phase 5, to avoid unaudited golden churn — declared in the contract's wiring_status, not silent)
- Required independent/physical proof: n/a (contract package); packaging proof arrives via the CI package-source-identity job on the next closure-branch run (the new asset is allowlisted and closure-loaded)
- Required integration gate: registry contract 720 PASS; census 295 PASS; decisions-stage user-flow smoke exit 0 with the receipt stamp in place
- Raw input/evidence hashes: envelope sha256 `e2681666c6a693f07814e18e29f4eb991630900af9b5c29eb342830984f5ac0a` (stamped into every vnext run state from this commit)
- Last known green commit: `7743229`
- Rollback plan: revert this commit; the run-state field is optional in-schema so prior states remain valid
- Attempt count (1-3): 1
- Status: SEALED (packaging proof rides the next CI run)

## Difference classification

- Intended correction: none to economics — the envelope is new contract surface
- Presentation-only: run receipts gain the governing envelope identity
- Accepted normalization: playbook's suggested path `contracts/support_envelope_v377.json` adapted to the repository's sealed packaging convention (`assets/` is the shipped contract directory; a `contracts/` dir would not install) — decision order: versioned repo conventions above playbook illustrative paths
- Regression: none
- Unresolved: early-stop wiring INTO intake preflight is future work (Phase 2) — the classifier is pure and proven; the guarantee that classification precedes document work is structural (no I/O in the module) and will be enforced at the call site when wired

## Completion receipt

- Commands and exit codes: envelope suite 87 → 0; registry contract 720 → 0; census 295 → 0; user-flow decisions smoke → 0
- Evidence paths/hashes: assets/support-envelope-v377.json (`e2681666…`), ci/test_registry_census.json (regenerated)
- Residual risks: dimension values for entity_type/topology are not yet auto-detected — until Phase 2 wiring, classification requires caller-supplied facts; unknown values classify to declared unknown classes (silence is classified, never a pass)
- Next package first action (P0.5): read the P0.5 spec (terminal-reason registry and outcome compiler contract); discovery = inventory every terminal/blocker/reason string the runtime currently emits (grep NEEDS_, BLOCKED, ACTION_REQUIRED, fatal_reason, blocker_class across scripts/lib) and compare against the pack's seven declared terminal states.
