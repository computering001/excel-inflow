# Work Package Issue Card

- Work package ID: P3.8
- Title: Authority seal over the Economic IR — bind the whole compiled economic state, and RE-VERIFY the transformation receipt
- Active invariant: The economic authority seal binds all five compiled artifacts — the Economic IR, the semantic manifest, the instrument period state, the equation graph and the solution — so any one of them changing invalidates the seal AND is localised by name; and the transformation receipt is RE-VERIFIED rather than merely written.
- Release/phase: v3.7.7 / Phase 3
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `596d7cc` (worktree `excel-inflow-v377`)
- Working tree status: UNCOMMITTED at hand-off (commit withheld by instruction); shared with concurrent P3.3 and P4.6 work
- Observed failure or discovery question: The Phase-5 stage-parity receipt bound four sha256s — `model_case_sha256`, `completion_census_sha256`, `forecast_ledger_sha256`, `ownership_receipt_sha256` — every one of them an INPUT or a census OF inputs. Proven red on the answered case `standard_net_cash`: none of the five economic artifact hashes appeared in the receipt and no key even named one, so a mutation to EACH of the five (a typed IR slot rewritten to 999999, a manifest node_id renamed, an instrument period state field added, the solver's max_iterations dropped to 1, a solved forecast period tampered) left `verifyEconomicStageParity` at 0 errors and `verifyEconomicStageParityAfterBuild` at PASS — five for five. Separately, `transformationReceipt()` recorded `economic_ir_sha256` and was never read again: no verifier existed under any name in the tree, and the two production references to `.transformation-receipt.json` (orchestrate_release.mjs:85, live_delivery_attestation.mjs:36) are filename inventories.
- Earliest responsible layer: the economic stage-parity seal (`forecast_completion_constitution.mjs`) — it sealed the inputs to the economics and never the economics
- Reproduction command: `node scripts/run_economic_authority_seal_tests.mjs`
- Focused failing test ID: economic-authority-seal (registered; 169 checks across eight sections — five-artifact binding, content bindings, the five invalidation mutations, earned localisation, transformation-receipt re-verification both ways, determinism incl. cross-process, containment, seal integrity, and the 1.1 -> 1.2 refusal)
- Allowed files/modules: NEW economic_authority_seal.mjs (the seal, the derivation, the localisation and both receipt verifiers), forecast_completion_constitution.mjs (seal + verification region only — P3.3's tax-operator membership test left intact), the new suite, this card
- Explicitly out of scope: wiring `verifyTransformationReceiptAgainstSeal` over the EMITTED `<workbook>.transformation-receipt.json` (controller held by P6.1 — exact line reported for the holder to apply); economic_ir.mjs needed no accessor, the existing `shadowEconomicIrOf` / `economicIrContentSha256` sufficed
- Required integration gate: economic-authority-seal 169, forecast-completion 15, completion-census-scope 66, economic-ir 79, etr-dag-safety 112 (P3.3's, which depends on the edited file), internal-outcome 16, user-flow 14/14, etr-receipt-boundary 39, `node --check` on all touched files — green
- Last known green commit: `596d7cc`
- Rollback plan: delete economic_authority_seal.mjs and the suite; revert the seal/verification region of forecast_completion_constitution.mjs to `economic-stage-parity/1.1` (no other module imports the seal)
- Attempt count (1-3): 2 (attempt 1 was lost mid-implementation with the localisation assertion red; its work was preserved and completed rather than restarted)
- Status: COMPLETE — uncommitted, awaiting seal

## Difference classification
- Intended correction: the parity receipt carries an `economic_authority` seal over five derived artifacts plus the transformation receipt; the post-Build verifier RE-DERIVES all of them; the schema bumps 1.1 -> 1.2 so a receipt that binds no economic state is refused by version rather than read as agreement
- Regression: none — no producer module changed; 0 differing emitted paths on both fixtures (see completion receipt)
- Unresolved: the emitted receipt file is bound and re-derivable but the on-disk artifact is not yet re-read by a controller (out of scope above); a full Build could not be driven in this environment (Python with openpyxl unavailable), so that wiring is reported unverified end to end

## Completion receipt
- economic-authority-seal 169 → 0. The five mutations each invalidate the seal and each is named.
- LOCALISATION was the hard half. The five artifacts are not independent: `semantic_manifest.instrument_period_state` IS the instrument-period-state artifact, embedded verbatim, so one edit moved TWO bound hashes and the verifier condemned both. Fixed by evidence, not by relaxing the assertion: every binding now records `own_content_sha256` — the artifact hashed with each embedded copy of another bound artifact replaced by a marker naming it — plus `embedded_artifact_ids`. The embedding is DISCOVERED by walking each artifact against its peers' hashes (~15 ms on the 489 KB manifest), not read off a table that could rot. A divergence whose own content is unchanged is reported as `inherited:<id>:` against the peer that moved; one whose own content also moved is named `artifact:<id>:`. Every unattributable case falls through to a DIRECT finding, so the verifier over-reports rather than under-reports.
- Localisation is proved un-abusable, not merely asserted: two genuinely independent changes yield TWO direct findings and no inherited one; tampering only the manifest's embedded copy of the instrument state (leaving the artifact itself untouched) is named as the MANIFEST's change; a bound artifact missing its own-content hash is refused by the integrity check; the own-content hashes sit inside the sealed body, so editing one breaks `authority_sha256`.
- The verdict now reports the seal AS PRESENTED alongside its own declaration and the re-derived hash. A seal edited and re-hashed keeps a stale-looking agreement otherwise; with the presented hash reported, and a residual `seal:unexplained_divergence` check for a body difference nothing else named, a re-hashed lie cannot read as agreement.
- 1.1 -> 1.2 is a refusal, not a relabelling: an AUTHENTIC pre-P3.8 receipt (the exact 1.1 body, self-consistently hashed, binding the right case) is refused BY VERSION with `receipt_sha256` and `model_case_sha256` both clean, refused again as `authority:absent:`, refused again post-Build, and the fail-closed call throws a typed internal outcome on it.
- Output unchanged, path by path (P3.3's technique): on both certified fixtures, the instrument period state, both solves, the row plan, the semantic manifest, the model IR, the transformation receipt and the model-IR sidecar BYTES were compiled before the P3.8 lane ran and again after; TOTAL DIFFERING EMITTED PATHS: 0, sidecar byte-identical (252905 / 262494 bytes), and the case is unmutated by sealing. No producer module was touched.
- Verified in production, not only in the suite: a real `run_user_flow.mjs` run on the clean evidence sealed `economic-stage-parity/1.2` for case `standard_net_cash` with all five artifacts bound, the manifest recording `embeds=[instrument_period_state]`, and the transformation receipt bound with `economic_ir_sha256` equal to the IR binding. Re-verifying that persisted receipt post-Build returns PASS / authority PASS in 306 ms with presented == re-derived, and a tampered instrument-state hash localises to `authority:artifact:instrument_period_state:sha256` alone.
- P3.2's call site already carries the authority lane, so the "no production caller" half needed no new wiring: `assertEconomicStageParityAfterBuild` (run_user_flow.mjs:2094) now re-derives all five artifacts and the transformation receipt.
