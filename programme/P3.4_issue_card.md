# Work Package Issue Card

- Work package ID: P3.4
- Title: Period-specific authority scoring (rank vectors, family aggregate, declared recovery)
- Active invariant: Forecast-ownership scoring is decided and persisted PER FORECAST PERIOD — a rank vector per candidate per period, a family aggregate over all children rather than the strongest child alone, capture marks that never collapse a per-period authority, and a recovery pass that is a DECLARED code path rather than an environment-variable gate.
- Release/phase: v3.7.7 / Phase 3
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: clean at seal
- Observed failure or discovery question: The Phase-3 gap agent proved Preflight A collapsed periods (one winner for the whole forecast strip), scored only strongest-child-vs-parent with no family aggregate, persisted no rank vectors, sealed certificates outside the period loop, and reached its recovery pass only when `EXCEL_INFLOW_OWNERSHIP_DEGRADE=historical_average` was set in the environment — an undeclared, unreceipted code path.
- Earliest responsible layer: forecast_ownership_resolver.mjs (Preflight A scoring), with the rank-vector comparison primitives absent from forecast_authority.mjs
- Reproduction command: `node scripts/run_period_scoring_tests.mjs`
- Focused failing test ID: period-scoring (63 checks; red against the pre-repair resolver)
- Allowed files/modules: forecast_authority.mjs (+3 exported primitives: `compareForecastRankVectors`, `forecastRankVectorDecidingDimension`, `aggregateForecastFamilyRankVector`), forecast_ownership_resolver.mjs (per-period scoring loop, single `writePeriodAuthority` period-authority writer, `pinnedPeriodAuthority` so a row-level capture mark cannot collapse a per-period authority, `declaredOwnershipRecovery` normalising the legacy env gate into a typed declaration with `OWNERSHIP_RECOVERY_MODES`), the new suite
- Explicitly out of scope: the P3.6 identity crosswalk (period-scoped node identity); certificate sealing beyond the period loop is addressed only insofar as the period authorities now carry it
- Required integration gate: period-scoring 63, user-flow 14/14, forecast-behaviour 33, forecast-topology, forecast-completion 15, forecast-observation 13/13, completion-census-scope 66, ownership-census 10 (regenerated) — all green
- Last known green commit: `86ca5ba`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification
- Intended correction: scoring becomes period-indexed and family-aggregated; the legacy environment gate is normalised into a declared, receipted recovery mode (the env var is still honoured, but it is translated into the same typed declaration rather than being a hidden branch); all period-authority writes route through one writer.
- Regression: none observed across the forecast battery; the certified fixtures have period-invariant winners, so per-period scoring is a no-op on them by construction.
- Unresolved: none for this invariant. The recovery pass remains bounded to downstream-only scope.

## Completion receipt
- period-scoring 63 → 0; the pre-repair resolver fails the per-period assertions.
- Ownership census regenerated (the resolver's mutation surface moved); single-writer discipline preserved — capture custody still routes through capture_transition primitives per P3.5's gate.
- PROVENANCE NOTE: the implementing subagent was lost after completing the implementation and its suite but before writing this card. The card was authored by the coordinator from the verified diff (`git diff` of the two modules) plus a full re-run of the integration gate above; no claim here is taken on trust from the lost agent's report, because no report was received. The suite is the evidence.
