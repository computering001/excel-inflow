# P3.6 execution brief — canonical row/node identity binding
(Discovery agent, 2026-08-19, repo @ 30a6d50-era head. Condensed; anchors verified.)

## Scheme inventory (11 canonical + 6 satellite)
S1 source_line_id (extract_filing_statements.py:1560-1567; global across sections, label-derived)
S2 row_id (case_compiler.mjs:304; sanitizeRowId :119-125 STRIPS is./cf. prefix — lossy S1→S2)
S3 semantic_role (46-role taxonomy asset; schema leaves it an OPEN string; solver.mjs:866-872 first-wins)
S4 semantic_id (semantic_graph.mjs:277 + :530-666 re-mints; uniqueness checked for statement_row only)
S5 manifest node_id (semantic_graph.mjs mint sites; edge-only pseudo-nodes source.* / unresolved.* have
   NO node record; endpoint totality checked ONLY for depends_on — validation_invariants.mjs:674-682)
S6 plan rows_by_id keys (row_plan.mjs:4743; 26 mint sites; ONLY :3698 has a duplicate guard —
   23 unguarded mints; cash_interest_paid statement row SILENTLY OVERWRITTEN by :4614)
S7 A1 cell addresses (formula_dsl.mjs:186-191 mint; plan_builder.mjs:552-559 store; no semantic id on cells)
S8 equation-graph node ids (assets/equation-graph.v1.json, 35 nodes; ZERO cross-layer edges to rows)
S9 completion-census tuple {section,row_id,forecast_index} (forecast_completion_constitution.mjs:102-127;
   index not date; no injectivity check; cardinality pinned by literal ===18)
S10 ownership-census cell_key — TWO different constructions (ownership_census.mjs:164 vs :207) + a third
   node namespace model_node_id; only census that is date-scoped; injectivity checked :292-299
S11 capture parent ids (capture_transition.mjs:104,172-180; row-level SCALAR, period-less, while
   certificates are per-period arrays — structural disagreement)
Satellites: S12 instrument_id (no dots) / S12b state_id date-keyed :502 vs S12c index-keyed :758;
S13 model-IR display_id + concept `${basis}:${scope}:${section}:${role}` (period-less);
S14 layered-graph stableLayerNodeId (5 layers; forecast layer uses INDEX not date :480);
S15 broker metric ids (bolted onto rowPlan post-hoc build_dynamic_model.mjs:12386);
S16 oracle-matrix keys; S17 normalised label text — the physical oracle's ONLY join key
(workbook_semantic_oracle.py:58,405-417,801-802).

## Join register highlights (25 ad-hoc joins J1-J25 catalogued in the full agent report)
Worst offenders: J4 build_dynamic_model.mjs:1258 two-tier rowForId fallback masking the S6 overwrite;
J6 semantic_graph.mjs:692 fabricates `unresolved.*` node instead of failing; J9 group-key defect (below);
J11 forecast_candidate_compiler.mjs:414 four schemes in one Set, no precedence; J13-J15
forecast_authority_ledger fallback collapses + J14 :107 fabricated pseudo-address never reconciled to S7;
J16 model_ir_v3.mjs:826-831 .slice("statement.".length) string surgery; J19/J21 `role==="ending_cash" ||
row_id==="ending_cash"` duplicated in two modules; J23 forecast_completeness.mjs inline triple-join;
J24 label-text join in the independent oracle.
Good precedents: layered_graph_constitution projection layer (ROW_PLAN_MULTIPLE_WRITERS/ORPHAN :566-624);
validation_invariants.mjs:445-480 state_id↔node ordered binding; compileSourceCrosswalk
(semantic_graph.mjs:1144-1211) — the only artifact named crosswalk, covers 2 of 11 schemes.

## Mint point verdict
No single point sees all schemes. Two-part mint:
- Part A (logical): case_compiler.mjs after applyRoleRecipes (:3610), before compileForecastPlan (:3799).
  Binds S1↔S2↔S3↔S11↔S12↔S15↔period-date, sealed with binding_sha256.
- Part B (physical): build_dynamic_model.mjs between :12386 and :12387 (after broker_metric_rows,
  before compileSemanticManifest). Adds S6/S7/S5/S13/S14 + the S8↔row edges. Strongest host:
  extend compileLayeredGraphConstitution (already invoked at semantic_graph.mjs:1069, has stable ids,
  closure hash, mutation suite).

## Bijectivity suite plan
Template: run_layered_graph_constitution_tests.mjs (mutation harness asserting BLOCK + code + hash move).
Checks: totality per leg (with documented_exclusion vs unmapped), injectivity (must catch the S6
overwrite), surjectivity (no orphan nodes/rows), round-trip inverse for 1:1 pairs (kills J16/J9),
period totality (index↔date, both censuses produce the same cell set), cross-census set equality
(replaces the ===18 literal), seal binding + drift, one adversarial mutation per violation code.
Artifact shape twin: assets/broker-crosswalk.schema.json (+ coverage_ledger idiom).
Add a raw-join gate to the census suite: `?? row.(row_id|semantic_role)` or startsWith("statement.")
outside the crosswalk module = defect (mirror of the P3.5 capture gate).

## Sequencing constraints (BINDING)
- P2.3 collides DIRECTLY (compileSourceCoverage :727-780 is J1's home) — land P2.3 first, then P3.6
  consumes the resolved function; or one commit.
- P2.5 collides in model_ir_v3 (:844-847 three lines from J16's join). P4.2 same-file
  instrument_period_state (disjoint functions). P4.5 rewrites formula_dsl wholesale — P3.6 first.
- Frozen modules: additive edits only (Part-B mint is ONE insertion; row_plan gets duplicate guards
  + display_group key carried alongside label; NO extraction).
- workbook_semantic_oracle must keep label matching as corroboration — the self_confirming_only
  governance mutation fires if it starts trusting the compiler's own artifact.
- Other reachability: flow_impact.mjs:139-140 second compile path; release_nodes.mjs:271-280 N9 gate
  (determinism check compares two compileRowPlan runs — crosswalk must be deterministic);
  run_user_flow.mjs:1880 sole parity-seal caller.

## TWO LIVE DEFECTS FOUND (independent of P3.6, need cards)
1. J9 group-key mismatch: semantic_graph.mjs:851-861 joins instruments to debt/interest group subtotals
   via normaliseGroupKey(display_group LABEL); DEBT_PRESENTATION_GROUPS (row_plan.mjs:3716-3721) key
   `unclassified_review` vs label "Unclassified — review required" → normalises to
   `unclassified_review_required` ≠ key; link() (:831-840) silently drops edges for absent nodes, so
   every instrument in the unclassified-review group loses its subtotal edges with NO violation.
   Latent: no fixture exercises the group. Verified by direct execution.
2. S6 silent overwrite: row_plan.mjs rows_by_id — statement `cash_interest_paid` (:3698, guarded)
   overwritten by interest-schedule mint (:4614, unguarded; 23 unguarded sibling sites). Masked at the
   single consumer (build_dynamic_model.mjs:1251-1258 statement-first fallback with an admitting
   comment); any second consumer reads the wrong row silently.
