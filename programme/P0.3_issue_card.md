# Work Package Issue Card

- Work package ID: P0.3
- Title: Make exact-head CI truthful and authoritative
- Active invariant: A failed critical test makes the authoritative CI gate fail.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `9b8b646` + `7743229` (canary proof on temporary branch `tmp/p03-failing-canary`)
- Working tree status: clean at each seal
- Observed failure or discovery question: (1) the registered identity-governance suite failed at exact head on a stale `3.7.5` version literal — the required gate ran red for a full release cycle (the baseline PR's own gate run concluded FAILURE); (2) three playbook-named critical suites (tax/ETR, deadline, forecast-completion) were unregistered; (3) three gate lanes presented green on failure via `continue-on-error`; (4) 25 `run_*` scripts had no registration or classification; (5) the corpus suite was mis-profiled portable and would fail on custody-less hosts.
- Earliest responsible layer: test/CI governance (test literals, registry, workflow presentation) — no product source
- Reproduction command: `node scripts/run_release_identity_governance_tests.mjs` (failed at baseline, exit 1)
- Focused failing test ID: release-identity-governance (red at baseline → green after repair); ci-census (new, 293 checks, two built-in negative self-tests)
- Allowed files/modules to change: `scripts/run_release_identity_governance_tests.mjs` (version literal + one message), `.github/workflows/read-only-development-gate.yml` (verdict steps + census step), `assets/development-test-registry.json` (+5 entries), oracle-matrix registry pin, `scripts/run_ci_census_tests.mjs` (new), `scripts/run_blocker_corpus_tests.mjs` (typed absence + expectation flip), corpus manifest, `ci/*`
- Explicitly out of scope: all economic product source; goldens; gate machinery semantics (run_development_gate, matrix compiler — verified authoritative as-is)
- Required independent/physical proof: live Actions runs — deliberate failing canary must conclude FAILURE; clean closure head must conclude SUCCESS
- Required integration gate: registry contract (715), census (293), corpus (79), oracle matrix (1.0), identity governance — all exit 0 locally at head (ci/exact_head_gate_report.json)
- Raw input/evidence hashes: ci/test_registry_census.json + ci/exact_head_gate_report.json bind head commit/tree and toolchain (node v22.23.2, venv Python 3.9.6, LibreOffice 26.2.5.2)
- Last known green commit: `431ef3e` (P0.2 seal)
- Rollback plan: revert `9b8b646`+`7743229`; delete `tmp/p03-failing-canary`
- Attempt count (1-3): 1
- Status: VERIFYING (awaiting live canary/closure run receipts)

## Difference classification

- Intended correction: identity-governance tripwire literal 3.7.5 → 3.7.6 (with bump discipline documented in-line); corpus expectation flipped in the same commit
- Presentation-only: lane verdict steps (fail visibility; no test semantics changed)
- Accepted normalization: registry +5 typed entries (tax-rate-policy, run-deadline, forecast-completion, ci-census, blocker-replay-corpus requires tokens); oracle pin rebinds
- Regression: none; the verdict chain (compile_ci_gate_matrix exit 1 on any non-PASS) was verified pre-existing and untouched
- Unresolved: 7 ORPHANED_* scripts classified in the census — register-or-delete decision deferred to Phase 9 (v3.8 code deletion) by design

## Completion receipt

- Commit: `9b8b646`, `7743229` (+ canary receipt below)
- Commands and exit codes: see ci/exact_head_gate_report.json (five gates, all 0)
- Evidence paths/hashes: ci/test_registry_census.json, ci/exact_head_gate_report.json, programme/P0.3_ci_run_receipts.json (written when live runs conclude)
- Residual risks: branch protection/required-check enforcement is a GitHub org setting the model cannot set; the workflow is truthful but requiring it on merges is an owner action.
- Next package first action (P0.4): discovery — read the P0.4 spec (support-envelope contract) and search assets/ for any existing support classification vocabulary before authoring the versioned contract.
