# Work Package Issue Card

- Work package ID: P7.1a
- Title: Support-envelope coverage grid and evidence-gap test (REPORT-MODE ONLY)
- Active invariant: Every value the support envelope CLAIMS is joined to the evidence proving it, tier-labelled; a claimed value with no proving evidence is reported as a typed coverage gap.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `f203749` (worktree `excel-inflow-v377`)
- Working tree status: five new files, no existing file modified; `assets/support-envelope-v377.json` and `scripts/lib/support_envelope.mjs` untouched (proven by `git diff --stat` on both paths returning empty)
- Observed failure or discovery question: `assets/support-envelope-v377.json` declares 11 dimensions and 50 enumerated claim values (40 dimension values plus the 10 members of `debt_instruments.declared_matrix`), consumed only by the classifier. NOTHING joined a declared dimension value to evidence that the product actually handles it, so the support-claim-vs-evidence gap test had no producer and the envelope's claims were unfalsifiable. The question this package answers: for each declared value, what evidence proves it, at which tier, and where is there none?
- Earliest responsible layer: the evidence-join layer — absent entirely. The envelope declared claims, the corpus held evidence, and no artifact related the two.
- Reproduction command: `node scripts/run_support_coverage_grid_tests.mjs` (add `--write` to regenerate the committed grid; add `--custody-root <dir>` or set `EXCEL_INFLOW_CUSTODY_ROOT` to exercise the custody-root probe)
- Focused failing test ID: support-coverage-grid (671 checks). Red before the grid existed with `SUPPORT_COVERAGE_GRID_FAIL: the committed grid assets/support-coverage-grid-v1.json is missing`, exit 1.
- Allowed files/modules: NEW `assets/support-coverage-grid-v1.json` (the joinable artifact), NEW `assets/support-coverage-grid-v1.schema.json` (its contract), NEW `scripts/lib/support_coverage_grid.mjs` (the compiler: tier table, structural probes, custody joins, curated mappings), NEW `scripts/run_support_coverage_grid_tests.mjs` (generator + verifier), NEW `programme/P7.1a_issue_card.md`
- Explicitly out of scope (REMAINING P7.1, on the work order): the 150-200 case global corpus (the reachable corpus is ~17 real issuers in custody plus 2 synthetic fixtures, so this package delivers the JOIN and the GAP MEASUREMENT, not volume); corpus-fill of the eleven absent archetypes; any narrowing of the envelope (the narrowing decision is the user's and follows from the numbers below); registration of the new suite in `assets/development-test-registry.json` and disposition in the CI census (both forbidden to this package); the absolute host path hardcoded at `test-corpus/blockers/corpus_manifest.json:7`
- Required integration gate: support-coverage-grid 671 green with AND without a custody root; support-envelope 87; support-preflight-wire 7; programme-control 97; `node --check` on both new scripts; both new JSON assets parse
- Last known green commit: `f203749`
- Rollback plan: delete the five new files. Nothing existing was modified, no run consumes the grid, and no classifier behaviour changed, so removal restores the previous state exactly.
- Attempt count (1-3): 1
- Status: SEALED (measurement complete; the narrowing decision is the user's and is deliberately NOT taken here)

## Headline coverage numbers (THE DECISION INPUT)

| Measure | Count |
| --- | --- |
| Values declared by the envelope | **50** (40 dimension values + 10 declared-matrix instrument classes, across 11 dimensions) |
| Proven (every required tier met) | **22** |
| Partially proven (one required tier met) | **1** |
| **Unproven (no required tier met)** | **27** |
| Rows carrying real-filing (extraction) evidence | 10 |
| Rows carrying synthetic-case (model-shape) evidence | 17 |
| Rows requiring extraction evidence only | 6 |
| Rows requiring model-shape evidence only | 28 |
| Rows requiring BOTH tiers | 16 |

Gap composition: 27 unproven rows split `no_synthetic_case_authored` 15, `archetype_absent_from_corpus` 11, `no_real_filing_available` 1. By tier, 12 rows lack extraction evidence (10 because the custody register has NO classification axis for the dimension, 2 because it has the axis but no candidate covers the value) and 27 rows lack model-shape evidence.

**Structural finding:** 7 of the 11 declared dimensions cannot be joined to a real filing AT ALL, because the in-repo custody register classifies only `accounting_framework`, `issuer_region`, `reporting_period`, `document_format` and `statement_structure`. The unjoinable dimensions are `acquisition_overlay`, `broker_availability`, `cash_flow_method`, `debt_instruments`, `entity_type`, `fiscal_calendar`, `restructuring_complexity`. For values in those dimensions, closing the extraction gap needs register classification work, not merely a new filing — a distinction the grid types separately (`custody_register_lacks_dimension_axis` vs `no_real_filing_available`).

**Two CERTIFIED values are wholly unproven:** `entity_type/utility` and `fiscal_calendar/week_52_53`. The envelope makes its strongest promise about both and no evidence of any kind stands behind either. These are the sharpest narrowing candidates.

### The 27 unproven declared values, with typed gap reasons

Two CERTIFIED, five SUPPORTED_DEGRADED, eight EXPERIMENTAL, nine UNSUPPORTED, three declared-matrix members.

| Dimension / value | Declared class | Primary gap reason | Missing tiers |
| --- | --- | --- | --- |
| `entity_type/utility` | CERTIFIED | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `fiscal_calendar/week_52_53` | CERTIFIED | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `entity_type/holding_company` | SUPPORTED_DEGRADED | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `entity_type/mixed_group_predominantly_non_financial` | SUPPORTED_DEGRADED | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `historical_periods/two_with_prior_filing_support` | SUPPORTED_DEGRADED | archetype_absent_from_corpus | extraction (no candidate) + model_shape |
| `restructuring_complexity/discontinued_operations_disclosed` | SUPPORTED_DEGRADED | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `debt_instruments/none_disclosed` | SUPPORTED_DEGRADED | no_synthetic_case_authored | model_shape |
| `broker_availability/broker_pack_absent` | SUPPORTED_DEGRADED | no_synthetic_case_authored | model_shape |
| `entity_type/reit` | EXPERIMENTAL | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `cash_flow_method/direct` | EXPERIMENTAL | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `debt_instruments/convertible_or_hybrid` | EXPERIMENTAL | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `debt_instruments/exotic_or_structured` | EXPERIMENTAL | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `restructuring_complexity/major_perimeter_change` | EXPERIMENTAL | archetype_absent_from_corpus | extraction (register lacks axis) + model_shape |
| `accounting_framework/local_gaap_english` | EXPERIMENTAL | no_real_filing_available | extraction (axis exists, no candidate) |
| `acquisition_overlay/multi_step_or_contingent` | EXPERIMENTAL | no_synthetic_case_authored | model_shape |
| `accounting_framework/other_or_unknown` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `entity_type/bank` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `entity_type/insurer` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `entity_type/fund` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `entity_type/financial_spv` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `entity_type/investment_company` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `filing_language_format/non_english` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `historical_periods/fewer_than_two` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `statement_topology/cash_flow_absent` | UNSUPPORTED | no_synthetic_case_authored | model_shape |
| `debt_instruments.declared_matrix/bond_floating` | DECLARED_MATRIX_MEMBER | no_synthetic_case_authored | model_shape |
| `debt_instruments.declared_matrix/term_loan_fixed` | DECLARED_MATRIX_MEMBER | no_synthetic_case_authored | model_shape |
| `debt_instruments.declared_matrix/overdraft` | DECLARED_MATRIX_MEMBER | no_synthetic_case_authored | model_shape |

Plus one PARTIALLY PROVEN row: `statement_topology/condensed_or_interim` (EXPERIMENTAL) — a real interim filing is attested in custody, but no synthetic interim case exists, so the interim period arithmetic is unproven (`no_synthetic_case_authored`).

The nine UNSUPPORTED rows are the cheapest to close and the least alarming: each is a declared refusal decided from intake facts alone, its classifier verdict already pinned by `scripts/run_support_envelope_tests.mjs`. That adjacency is recorded on each row as `adjacent_non_case_evidence` and deliberately does NOT count as proof — a unit assertion is not a corpus case, and the UNSUPPORTED release promise ("the run stops before document extraction, model compilation or any broker retrieval") is an end-to-end claim no unit test exercises. Closing all nine needs nine synthetic intake cases and no filings.

## Tier classification summary

The classification is TOTAL over the envelope: all 50 declared values carry a required tier and a one-line justification, and a hole in the tier table makes the compiler refuse rather than emit an untiered row.

- `extraction_evidence` (a real filing is required — the claim concerns reading messy source documents; evidence lives in custody, referenced by hash/id only): 6 values alone, 16 more in combination. Assigned to framework identity, document format/language, entity-archetype recognition, comparative-period reading, statement-topology recognition, cash-flow-method recognition, 52/53-week labels, exotic instrument disclosures, discontinued-ops re-presentation, restated comparatives.
- `model_shape_evidence` (a synthetic in-repo case suffices — the claim concerns economics, period arithmetic, or a pre-extraction refusal from intake facts alone): 28 values alone, 16 more in combination. Assigned to instrument mechanics, the broker overlay, acquisition overlays, fixed-date period arithmetic and every declared refusal.
- BOTH tiers: 16 values whose risk is genuinely dual (each names the two risks separately in its `tier_justification`); each reports which tier is missing rather than collapsing into a single verdict.

## Difference classification
- Intended correction: a new, purely additive measurement artifact plus its compiler, contract and verifier. Evidence is scanned, never asserted: 33 structural probes over the two in-repo synthetic cases, 3 typed-field probes over the 6 blocker-corpus cases, 6 attested-category joins, 1 attested-qualification probe and 3 structural media-metric probes over the custody register, and exactly 2 curated mappings — each of which cites a manifest field and is refused unless the cited token is actually present in it.
- Regression: none. No existing file was modified; no run, controller or classifier reads the grid.
- Unresolved: corpus fill for the eleven absent archetypes; suite registration and CI disposition; whether the envelope should be narrowed (the user's decision, and the reason these numbers exist).

## Completion receipt
- support-coverage-grid 671 → 0. Red first: the suite failed with the committed grid missing (exit 1) before `--write` compiled it.
- 50 declared values, 50 rows, zero silence. The anti-silence gate proved on disk: dropping `fiscal_calendar/week_52_53` from the committed grid fails by name; gutting a whole dimension fails value by value.
- Five on-disk mutation drills each fail on their own gate: dropped declared value; a `proving_case` citing `ghost-case`; an unproven row relabelled proven ("misreports its satisfied tiers"); a custody join used without being declared with a justification; an absolute host path leaked into the artifact.
- Portability proven both ways: identical 671 checks with no custody root, with `--custody-root`, with `EXCEL_INFLOW_CUSTODY_ROOT`, and with an unresolvable root. The grid is byte-identical in every case, because custody families are enumerated BY MANIFEST ONLY. Custody-root absence is typed `CUSTODY_ROOT_ABSENT_NOT_A_FAILURE` and reported on stderr; `grid_effect` is `none`. Absence is never failure and never proof.
- Custody hygiene enforced by test: the artifact carries no `/Users/`, no `http://`, no `https://`, no `.pdf` and no custody root name, and every custody reference is a 16-hex opaque candidate id.
- Envelope binding: the grid records `envelope_sha256 e2681666…5ac0a`, matching the digest the support-envelope suite reports. An envelope edit turns this suite red until the grid is regenerated — the grid can never lag the claims it joins.
- This card lints: `scripts/run_programme_control_tests.mjs` matches letter-suffixed cards (`^P\d+\.\d+[a-z]?_issue_card\.md$`) and moves 97 → 98 checks with this card present, 97 with it removed.
