# Work Package Issue Card

- Work package ID: P1.3
- Title: Centralise decimal, unit, currency and period arithmetic
- Active invariant: Currency, unit, scale, period and context are explicit in source arithmetic; missingness propagates by declared policy, never as silent zero.
- Release/phase: v3.7.7 / Phase 1
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (staged with P1.1/P1.2 shadow work)
- Working tree status: clean at commit
- Observed failure or discovery question: Arithmetic is scattered ad hoc across the compiler with a single implicit epsilon culture; no operation records its operands' states or dimensions.
- Earliest responsible layer: arithmetic service (target sole authority for source arithmetic)
- Reproduction command: `node scripts/run_typed_arithmetic_tests.mjs`
- Focused failing test ID: typed-arithmetic (registered; 20 checks)
- Allowed files/modules to change: `scripts/lib/typed_arithmetic.mjs` (new), `scripts/run_typed_arithmetic_tests.mjs` (new), registry + pins + audit census
- Explicitly out of scope (REMAINING P1.3 WORK, declared): replacing the high-risk ad hoc call sites (tax/ETR, historical totals, cash-flow identities, debt bridge, broker consensus) — authority switches per site with dual-read proof after P1.4; the independent Python arithmetic oracle rides that migration; European/US number-format parsing lives with the typed-value PARSER migration (P1.2 producer work)
- Required independent/physical proof: deferred to the call-site migration (dual-read + Python oracle)
- Required integration gate: registry contract 750 PASS; census 307 PASS
- Raw input/evidence hashes: bound by the sealing commit tree
- Last known green commit: `b989037`
- Rollback plan: revert; no runtime consumer yet
- Attempt count (1-3): 1 (one dimensional-rule repair: same-currency division is a lawful ratio — caught by the suite's own divide-by-zero test before commit)
- Status: IMPLEMENTING (service core sealed in shadow; call-site migration outstanding)

## Difference classification

- Intended correction: none to runtime yet
- Presentation-only: none
- Accepted normalization: none
- Regression: none
- Unresolved: call-site migration; conversion-evidence operations (FX/scale) are refused pending their typed evidence contract

## Completion receipt (core)

- Commands and exit codes: typed-arithmetic 20 → 0; registry 750 → 0; census 307 → 0
- Evidence: every operation returns an auditable receipt (operand states + dimensions + policy + tolerance); mixed currency/scale/period-kind refuse; missing propagates to unresolved unless partial aggregation is EXPLICITLY permitted and then the omission is recorded; tolerance derives from source precision + thresholds, never one global epsilon; divide-by-zero is unresolved, never Infinity
- Residual risks: none new
- Next action: P1.4 (producer-boundary validation) discovery after P0.8 — starting from the P1.1 finding that oneOf/$ref go unenforced in the JS validator
