# Work Package Issue Card

- Work package ID: P7.3
- Title: Property/generated case cohort (SEEDED GENERATOR, SHRINKER, SEED REGISTRY)
- Active invariant: Cases are GENERATED from a declared dimension space by a seeded, reproducible generator — a seed fully determines a case, the distribution over the space is reported, a failing seed is PERSISTED and replayable, and a failure is SHRUNK to a minimal reproducing case.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: five new files only; no existing file modified
- Observed failure or discovery question: The P7 gap agent proved the cohort was a FIXED LIST, not a generator — `compile_synthetic_cohort.mjs:17-49` enumerates exactly 32 named recipes, `EXPECTED_IDS` is a literal 32-element set in both the compiler and `run_frozen_cohort.mjs:18`, `mulberry32` (:68) only decorates fixed cases from per-descriptor constant seeds, and there was no property framework, shrinker, seed registry, failing-seed persistence or distribution report. Volumes were 32 against a required 2-3k PR / 10k nightly / 25k weekly. `run_frozen_cohort.mjs` is dispositioned PIPELINE_ENTRYPOINT and never runs in CI, so even those 32 were dark.
- Earliest responsible layer: the cohort construction layer — a cohort enumerated by hand can only assert what its author already thought of
- Reproduction command: `node scripts/run_generated_cohort_tests.mjs` (cheap `ci_default` slice; `--tier pr|nightly|weekly` or `--cases N` for volume)
- Focused failing test ID: generated-case-cohort (NOT YET REGISTERED — registry entry reported to the programme owner; the registry is forbidden to this package)
- Allowed files/modules: NEW `scripts/lib/case_generator.mjs` (seeded generator, dimension sampler, property evaluator, shrinker), NEW `assets/generated-case-dimension-space-v1.json` (declared space + tier volumes + stratification + dispositioned defect set, re-derived from the support envelope), NEW `scripts/generate_case_cohort.mjs` (CLI: `--count/--seed/--out/--report/--tier/--emit-space/--update-seed-registry/--seed-shapes`), NEW `scripts/run_generated_cohort_tests.mjs` (the registrable suite), NEW `ci/generated_case_seeds.json` (generated seed registry), NEW this card. `compile_synthetic_cohort.mjs` and `run_frozen_cohort.mjs` were READ for case-construction knowledge and left untouched.
- Explicitly out of scope (REMAINING P7.3, on the work order): a full solvable/emittable generated case (the generated case is a preflight-and-register case; the archetype seed-shape path is how full cases enter, and `seed_shape_mode` is `off` by default so the pinned corpus does not churn with the sibling catalogues); registering the suite; repairing the fifteen defects found below
- Required integration gate: this suite green at all four tiers; user-flow 14/14; donor hash unchanged against `RAW_CANARY_EVIDENCE_SHA256`; `node --check` on all touched files — green
- Last known green commit: `85ac6f2`
- Rollback plan: delete the five new files; nothing else was touched, so removal is a no-op for every other suite
- Attempt count (1-3): 1
- Status: SEALED (generator + shrinker + registry landed; fifteen found defects dispositioned OPEN, none repaired here)

## Difference classification
- Intended correction: the cohort becomes a SAMPLE of a declared 2.9e11-point space instead of a 32-element list; volume is a parameter with declared PR/nightly/weekly tiers; a failing seed is persisted with its shrunk minimal reproduction
- Regression: none — every file is new and the isolated-checkout A/B shows user-flow 14/14 with and without them
- Unresolved: the fifteen dispositioned defects (four never-zero coercions, four register selections of non-numbers, four uncovered UNSUPPORTED early-stop paths, three EXPERIMENTAL terminal-legality holes) are OPEN with owners and repair pointers; the suite is not yet registered

## Completion receipt
- generated-case-cohort: red (module absent) → PASS 577 checks / 300 cases / 5,986 property assertions at the CI slice; 25,000 cases in 1.7s at the weekly tier with zero undispositioned signatures
- 16 declared properties, all reached: envelope classification, typed early stop, registered reason codes, terminal legality per support class, never-zero on declared values, register selection, not-selected reason typing, translation authority, internal-never-ACTION_REQUIRED, the CERTIFIED release promise, degraded disclosure
- Replay proof: five spot seeds plus every persisted seed reproduce byte-identical canonical JSON and digests, across processes, against the digests in `ci/generated_case_seeds.json`
- Shrink proof: a synthetic oracle subject of 9 instruments / 8 periods shrinks to 1 instrument / 2 periods with the magnitude bisected to the exact boundary, deterministically, with every accepted step signature-preserving; the same shrinker reduces a real defect from 3 instruments / 6 periods to a 1-instrument minimal case
- Distribution report: 86 declared value slots, zero unreached and zero under-sampled at the CI slice; four declared strata exist because uniform sampling reached CERTIFIED zero times in 2,500 draws and SUPPORTED_DEGRADED only 1 time in 300
- DEFECTS FOUND: 15, each with a persisted seed, a failure signature, a shrunk minimal case, an owner and a repair pointer. None repaired, no property loosened.
