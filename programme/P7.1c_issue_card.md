# Work Package Issue Card

- Work package ID: P7.1c
- Title: Archetype case catalogue — PRESENTATION/STRUCTURE group (statement structure, classification, currency, units, provenance shapes)
- Active invariant: Each archetype case exercises ONE named statement-structure / presentation / currency shape that a naive implementation gets wrong, and the catalogue records TYPED, CHECKABLE expectations the runner asserts against shipped product code; "compiles clean" is never an acceptable sole expectation, and a typed, declared refusal is a PASS.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `7812b07` (worktree `/Users/archiepreston/Documents/Codex/excel-inflow-v377`)
- Working tree status: additive only — 29 new fixtures + README under `test-fixtures/archetypes/presentation/`, one new catalogue asset, one new suite, this card. No commit made (explicitly out of scope for this package).
- Observed failure or discovery question: Phase 7 had no model-shape evidence tier for presentation. The support envelope (11 dimensions, ~40 values) makes release claims about statement topology, cash-flow method, history depth and filing format, and the statement taxonomy declares 46 roles, but NOTHING held a case per accounting shape with typed expectations. The discovery question: for each real presentation shape a filing can take, does the product take a DECLARED path (a correct reading, or a typed refusal with a registered reason), or does it take a silent one?
- Earliest responsible layer: model-shape evidence tier (test assets); the defects surfaced sit in `statement_classifier` + `assets/statement-semantic-taxonomy.v1.json`, in the two duplicated footing-tolerance policies (`case_compiler.sourceTolerance` vs `model_ir_v3.footingTolerance`), and in the support-envelope/case-contract claim mismatch on history depth, currency and unit scale. NOTHING in production was edited under this package.
- Reproduction command: `node scripts/run_archetype_presentation_tests.mjs`
- Focused failing test ID: archetype-presentation (new; 557 checks over 29 archetypes / 94 expectations, incl. schema validation of the catalogue against the shared contract, a two-directional coverage gate, and three self-mutations)
- Allowed files/modules: NEW `test-fixtures/archetypes/presentation/*.json` (29 probe cases) + `test-fixtures/archetypes/presentation/README.md` (synthetic-provenance note), NEW `assets/archetype-catalogue-presentation-v1.json`, NEW `scripts/run_archetype_presentation_tests.mjs`, NEW `programme/P7.1c_issue_card.md`
- Explicitly out of scope: `scripts/lib/**` and all production scripts (read-only imports only — no validator was weakened and no product code was edited to make a case pass); `test-fixtures/cases/**` (globbed by other suites; its derived donor is hash-pinned in `scripts/lib/raw_canary_fixture.mjs`); `assets/support-envelope-v377.json`, `assets/archetype-case-catalogue-v1.schema.json`, `assets/terminal-reason-registry-v1.json`, `assets/development-test-registry.json` (all read-only); `audit/**`, `ci/**`, `architecture/**`, `programme/index.json`; the economics archetype group (`test-fixtures/archetypes/economics/`, `assets/archetype-catalogue-economics-v1.json`) owned by a parallel agent; REPAIR of every defect recorded below; registering this suite in the development-test registry; committing or pushing.
- Required integration gate: archetype-presentation 557/0, user-flow 14/14, statement-classifier on `standard-maximal-v2` green, evidence-run on `test-fixtures/cases` green with the emitted donor sha256 still `22710b0d…71f8` (equal to the `RAW_CANARY_EVIDENCE_SHA256` pin), `node --check` on the new suite — all green
- Last known green commit: `7812b07`
- Rollback plan: delete the four new paths (`test-fixtures/archetypes/presentation/`, `assets/archetype-catalogue-presentation-v1.json`, `scripts/run_archetype_presentation_tests.mjs`, `programme/P7.1c_issue_card.md`). The package is purely additive: no shipped module, shared asset or pinned digest is touched, so rollback cannot regress anything.
- Attempt count (1-3): 1
- Status: SEALED (catalogue group complete; 6 recorded defects and 21 extraction-evidence obligations tracked, not repaired)

## Difference classification
- Intended correction: a new evidence tier only. 29 synthetic presentation archetypes, each bound to typed expectations asserted against shipped product code (`classifySupport`, `classifyStatementLine`, `compileStatementTopology`, `typedValue`/`numericValueOf`, `typed_arithmetic`, `sourceHistoricalSumMatches`, `compileModelIrV3`, `validateCaseShape`, `validateFiscalPeriods`). Behaviour additive; nothing in the product changed.
- Regression: none. Donor digest unchanged; no case added to the globbed `test-fixtures/cases/` directory; no shared asset edited.
- Unresolved: (1) `Revenue from contracts with customers`, `Group revenue` and `Net revenues` do not classify — the first two tie against `cost_of_sales` at ZERO separation margin, the third leads with `net_income`; (2) `Net cash generated from operating activities` produces a three-way tie at 0.72 across operations/investing/financing while the aliased `Net cash from operating activities` accepts at 0.95; (3) the taxonomy declares no direct-method operating role, so every direct-method receipts/payments line is unmapped although the envelope classifies `cash_flow_method=direct` as a supported (EXPERIMENTAL) value; (4) two footing-tolerance policies disagree — `model_ir_v3.footingTolerance` claims to mirror `case_compiler.sourceTolerance` but adds a 1e-9 relative float allowance the latter does not have, so identical printed figures with no declared precision foot in one oracle and fail in the other; (5) a dash left ENCODED AS ABSENT silently removes a period from the footing pass, so a genuine reported nil escapes verification unless extraction classifies the dash; (6) the envelope certifies `historical_periods=three_or_more` while the case contract admits exactly three historical periods, and offers no representation at all for a functional currency distinct from the presentation currency or for a per-statement unit scale — three claim/representation mismatches. 21 of the 29 entries additionally carry `extraction_evidence_still_required: true`.

## Completion receipt
- archetype-presentation: 557 checks → 0 failures; 29 archetypes; 94 expectations (3 registered typed refusals, 91 positive/typed-state expectations); kinds: authority_selection 27, value_state 21, identity_or_footing 20, presentation_topology 15, period_arithmetic 4, compiles_clean 4, unsupported_profile_early_stop 3. Every entry carries at least one non-`compiles_clean` expectation (asserted by the suite, not by review).
- Coverage gate proven in BOTH directions plus a matcher mutation: a catalogue expectation no probe binds is reported declared-but-unasserted; a probe assertion the catalogue never declares is reported asserted-but-undeclared; and a blank cell asserted to read as zero fails the matcher.
- Non-vacuity proven exhaustively out-of-band: all 234 expected leaves across the 29 fixtures were perturbed one at a time and every single perturbation turned the suite red (0 escapes), so no expectation in this catalogue is decoration.
- Envelope claim hygiene is machine-checked: every `proves_envelope_values` pair must exist in `assets/support-envelope-v377.json`, and `envelope_extension_candidate` must be true exactly when the claim list is empty — 8 entries claim envelope values, 21 are recorded claim-set gaps.
- Donor custody: `node scripts/run_evidence_run_tests.mjs test-fixtures/cases --emit-clean <scratch>/donor.json` → exit 0, sha256 `22710b0de9212d34d3ce597579d95e3836f8e49ffb4ad3b2aca097127e9371f8`, byte-identical to `RAW_CANARY_EVIDENCE_SHA256`.
- Naming inconsistency recorded (not a defect — the terminal-registry suite already mirrors it deliberately): the support envelope emits `UNSUPPORTED_PROFILE.<suffix>` while the terminal-reason registry registers `PROFILE.<suffix>`. The catalogue names the REGISTERED code and the runner resolves the mirror explicitly.
