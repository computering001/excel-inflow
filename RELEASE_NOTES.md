# Excel Inflow current development candidate

<!-- release-identity:generated/1.0 BEGIN -->
<!-- release-version=3.7.11 release-channel=candidate commit=64263456458ce17ab00173d2b4603bd81e08d572 generated_at=2026-08-22T18:29:14.000Z -->
<!-- Written by scripts/compile_skill_release.mjs --write-release-identity from assets/release-identity.json. Hand edits are a drift violation and are reverted by the writer. -->
<!-- release-identity:generated/1.0 END -->

## Purpose

This candidate hardens the installed-filings and runtime-promotion boundary identified by the post-audit D52 review. It does not claim to close the separate economic-architecture programme.

## Principal repairs

- A standard-library public bootstrap now verifies package identity before loading the internal controller and converts pre-import failures into typed refusals without displaying a false Company screen.
- Runtime preflight now proves physical work/temp filesystem operations, measured disk-space policy, one-interpreter dependency closure, a real frozen-PDF extraction, bounded Inline XBRL execution and functional LibreOffice workbook capability.
- Installed capability reports, receipts and pointers are content-addressed, durably published and independently verified by a standard-library-only oracle.
- Development source, inactive installed candidate and active production identities are distinct. The Company screen is dynamically nonce-bound to a durable, single-use screen-session receipt.
- Exact-head CI is read-only and separates source identity, registry census, targeted runtime tests, the full portable gate, two independent package builds, reproducibility, archive-only capability, mutation measurement, synthetic merge and final aggregation.
- The prior audited candidate and package bytes remain immutable. A new deterministic development package is produced only from the final exact source head.

## Source and package status

- Public-bootstrap, runtime-doctor, installed-capability-receipt and screen-session mutation suites pass at the current source head.
- Two independent package builds from the pre-documentation implementation head produced byte-identical inventories and archives. Because release documentation is part of the final source history, the deterministic package must be regenerated and externally attested at the final source head before installation.
- Local host-sensitive mutation aggregation remains red because the current macOS sandbox cannot complete Unix-socket and LibreOffice process probes and no single local Python supplies the entire declared lane closure. The provisioned GitHub Ubuntu job is the required exact-head authority.

## Installation status

`NOT INSTALLED`

The candidate has not been installed into an inactive Rogo slot. No production pointer has been changed. D52 remains open until exact-head CI passes and the installed-host matrix proves the two fresh sessions, IFRS and US-GAAP filing routes, broker states, package immutability, promotion control and rollback.

## Deliberately held claims

The following are not claimed by this candidate:

- production promotion or global production certification;
- native Microsoft Excel recalculation/save/reopen certification;
- closure of unresolved forecast-state ownership, missing-to-zero coercion, shadow Economic IR, Forecast Waterfall aliasing, family scoring, support-envelope gaps, complete mutation measurement or runtime-ceiling semantics;
- a successful AstraZeneca three-broker plus DCS delivery from the exact installed bytes.

Exact source, package, CI, installation and promotion identities must be supplied by their external attestations rather than written into this source document.

<!-- filings-ladder-claims:generated/1.0 begin -->
## Filing extraction route status

This section is generated by `python3 scripts/run_filings_ladder_corpus_rehearsal.py --record`; hand edits fail the drift gate.

- Packaged and pipeline-wired routes: **Inline XBRL, PDF geometry, plain HTML tables**. Their archive-suite assertions are declared in source; a separate all-route PASS artifact is still required before any stronger claim.
- Maintained source routes not yet proved from the unpacked package: **OCR raster**.
- Sanitized rehearsal: **8/8 route obligations satisfied** (6 extraction passes; 2 typed OCR-unavailable refusals).
- Scope: repository-owned synthetic fixtures only. This is neither real-filing evidence nor installed-host certification.

Evidence: `assets/filings-ladder-corpus-rehearsal-v1.json` (`ea63e7fd84fd07b6…`).
<!-- filings-ladder-claims:generated/1.0 end -->
