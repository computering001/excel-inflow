# Work Package Issue Card

- Work package ID: P5.8
- Title: Pair the style/geometry mutation half to the visual half — a declared `rendered_geometry` scope, a real-render pairing oracle, and an honest NO-DETECTOR register
- Active invariant: A style or geometry mutation that a reader would SEE is provably detected, and the two halves are paired — a mutation declared PAIRED must fire its named visual signal, and a mutation declared unpaired must be proven blind (zero changed pixels against an approved baseline), never merely unaccused.
- Release/phase: v3.7.7 / Phase 5
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ b92eeee (working tree, uncommitted)
- Working tree status: dirty — this slice's two files plus four unrelated in-flight clusters (`scripts/run_user_flow.mjs`, `scripts/lib/flow_runtime.mjs`, `scripts/lib/evidence_work_graph.mjs`, `scripts/lib/optional_broker_circuit_breaker.mjs`, `scripts/run_attachment_evidence_pipeline.py` and six untracked files, none touched here). Every gate below was re-run in an isolated `HEAD + this slice` worktree so no other package's edits are being credited or blamed.
- Observed failure or discovery question: the mutation half and the visual half were disjoint. `assets/critical-invariant-oracle-matrix-v1.json` carried fifteen domains and no rendered-geometry scope of any kind. `scripts/render/check_render.py` (the visual half) had never been fed a mutant, and `scripts/render/selftest.py` — the thing that proves each render CHECK fires — cannot run on this host at all (no `reportlab`). `scripts/run_standardised_design_contract_mutations.mjs` (the style/geometry mutation half: 15 mutations including column drift, red font, print fit-width, style/dxf tables) was dispositioned `ORPHANED_BARE_RUNNABLE` by P7.6a and had NO caller — nor did the validator it imports, `scripts/validate_standardised_design_contract.mjs`. Nothing anywhere said which visual check catches which style defect, so both halves could have been blind simultaneously and every gate would still have gone green. `visual_manual` was listed in the registry's `allowed_audit_classes` and referred to by nothing.
- Earliest responsible layer: the oracle matrix asset (no geometry scope to hang the claim on) and the absence of any caller joining `check_render` to a style/geometry mutation.
- Reproduction command: `venv python scripts/run_visual_mutation_pairing_tests.py --soffice /Applications/LibreOffice.app/Contents/MacOS/soffice`
- Focused failing test ID: visual-mutation-pairing (NEW, 70 checks). RED PROOF, run against the unedited asset before any asset change: `AssertionError: VISUAL_PAIRING_FAIL: assets/critical-invariant-oracle-matrix-v1.json declares no rendered_geometry_scope: geometry is absent from the oracle matrix as a domain`.
- Allowed files/modules: `assets/critical-invariant-oracle-matrix-v1.json` (surgical additive only, NOT `registry_sha256`); NEW `scripts/run_visual_mutation_pairing_tests.py`; the design-contract harness file itself had it been deleted (it was not — see below); NEW `programme/P5.8_issue_card.md`
- Explicitly out of scope (REMAINING P5.8, HANDOVER — all in files forbidden to this package): (1) `scripts/run_visual_mutation_pairing_tests.py` must be registered in `assets/development-test-registry.json` or dispositioned in `scripts/run_ci_census_tests.mjs` — until then `node scripts/run_ci_census_tests.mjs` is RED with exactly `unclassified run_* scripts ...: run_visual_mutation_pairing_tests.py` (census is GREEN at HEAD: 421 checks / 231 scripts, and green again the moment this one file is removed, so the redness is this file and nothing else); (2) `scripts/run_standardised_design_contract_mutations.mjs`'s census disposition should move from `ORPHANED_BARE_RUNNABLE` to `SUBORDINATE_ORACLE` now that it has a caller; (3) `metadata_contract.audit_class_by_test_class` in the registry maps NO `test_class` to `visual_manual`, so no registered test can ever carry the class the registry allows — the mapping needs an owner; (4) `scripts/render/selftest.py` needs a host carrying `reportlab`; (5) P6.7's runtime doctor default interpreter still reports the render lane unsatisfiable — correct for CommandLineTools python3.9, stale for the project venv (see Regression note).
- Required integration gate: visual-mutation-pairing PASS (70 checks, 4 PAIRED, 2 proven-blind, 8 declaration-governance mutations caught, design-contract harness 15/15 with 0 vacuous); critical-invariant oracle matrix PASS (22/22, 5 governance mutations, the five `NOT_INDEPENDENTLY_PROVEN` flags intact); critical-invariant oracle matrix ALSO re-run in the shared tree alongside P5.5's concurrent edits: PASS 31/31; plan-reconstruction 153; user-flow 14/14; `py_compile` + `node --check` on every touched file — all green in the isolated HEAD+slice worktree
- Last known green commit: b92eeee
- Rollback plan: `git checkout -- assets/critical-invariant-oracle-matrix-v1.json` and delete `scripts/run_visual_mutation_pairing_tests.py` and this card
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: (a) a top-level `rendered_geometry_scope` block added to the matrix asset — declaring the scope, its `visual_manual` audit class, its four readers, its host requirements, and a six-row style/geometry mutation → visual-detection pairing table; (b) a new oracle that builds a real workbook, authors a per-page pixel baseline from the UNMUTATED build, applies six adversarial mutations to the emitted `.xlsx` BYTES (never to a receipt), and judges the declared table against what the two halves actually observed; (c) the orphaned design-contract mutation harness and its validator adopted as subordinate oracles of that suite.
- Regression: none. The scope is declared OUTSIDE the `domains` array on purpose — `required_domains` in `scripts/run_critical_invariant_oracle_matrix_tests.py` pins that array element-for-element and admits only two `evidence_scope` values, so adding a sixteenth domain from the asset side would have broken the matrix gate and silently widened what fifteen other domains may claim. The suite asserts `rendered_geometry` is absent from `domains`, that no `synthetic_unit_only` domain claims independence, and that each of P7.6a's five `NOT_INDEPENDENTLY_PROVEN` domains either still carries the flag or has left it WITH a `promoted_from` record naming the detector that replaced the synthetic unit fact — so a documented promotion is allowed and a silent removal is refused. That form was chosen deliberately after P5.5, editing this same asset concurrently, legitimately promoted `workbook_error_scan` to `evidence_scope: emitted_workbook_cell` with such a record; a frozen five-name literal would have fought a package doing the right thing. `registry_sha256` untouched; this package's asset diff is 162 insertions and one `]`→`],` (the shared file also carries P5.5's concurrent `physical_mutation_oracle` block and `workbook_error_scan` promotion, which are not this package's edits). Separately: `node scripts/run_mutation_adequacy_tests.mjs` is red at HEAD ("the artifact claims 71 mutation-class suites; the registry declares 76") — pre-existing registry drift from concurrent packages, and it fails before it ever reads this asset.
- Unresolved: the five handover items above. Also honest: the two proven-blind mutations have NO automated detector anywhere, only a file-level one; closing them needs the installed/native tier this repository permanently excludes, or a human.

## Completion receipt
- RED → GREEN: suite refused at check 1 on the unedited asset; 70 checks green after the additive declaration.
- Negative control (runs first, so a detector that fires on everything cannot look perfect): unmutated workbook — file reader PASS with zero findings, provenance authority PASS, render PASS, 3 pages compared, 0 changed pixels, geometry checks confirmed present in `checks_run` (clipping, presence, overlap, structure).
- Pairing table, all six executed against a real LibreOffice conversion of a real emitted workbook:
  - `column_width_collapse` (label column 46→6 chars) — FILE `RECON_COLUMN_MISMATCH`; VISUAL `check_clipping` 37 defects + 3 overlaps + page count 3→4 — **PAIRED**
  - `model_row_hidden` (first labelled body row `hidden="1"`) — FILE `RECON_ROW_MISMATCH`; VISUAL `check_presence` defect — **PAIRED**
  - `section_band_fill_recoloured` (darkest solid fill repainted) — FILE `RECON_FILL_MISMATCH` ×137; VISUAL pixel regression, 86,172 changed pixels unattributed on 3/3 pages — **PAIRED**. Note: `check_structure` alone does NOT catch this — it samples the render against the FILE's own declared fill, so a `styles.xml` edit moves both sides together; only an external baseline sees it.
  - `source_font_colour_black` (blue provenance font FF0000FF→FF000000) — FILE `RECON_FONT_MISMATCH` ×301 AND P5.3's `provenance_authority_oracle.py` PASS→BLOCK; VISUAL pixel regression, 5,778 changed pixels — **PAIRED**. No render check samples glyph colour; the baseline is the sole visual detector.
  - `freeze_pane_removed` — FILE `RECON_FREEZE_PANE_MISMATCH`; VISUAL **NONE**: render PASS with **0 changed pixels on every page**. A frozen pane has no printed representation, so no addition to `check_render` could ever see it. Audit class `visual_manual`.
  - `gridlines_shown` (`showGridLines` 0→1) — FILE `RECON_SHOW_GRIDLINES_MISMATCH`; VISUAL **NONE**: render PASS with **0 changed pixels**. `showGridLines` governs the screen; printing gridlines is a separate `printOptions/gridLines` flag. Audit class `visual_manual`.
- Blindness is proven, not inferred: an unpaired row must show `changed_pixels == 0`, so "no check complained" is never accepted as evidence.
- 8 declaration-governance mutations, all caught, run against the SAME observations so the table cannot lie in either direction: claim a detector for a blind mutation; disclaim a detector that fires; wrong file finding code; drop a mutation; unpaired without a reason; unpaired without the `visual_manual` class; wrong style-authority status; all-unpaired (vacuity).
- Design-contract harness VERDICT: **ADOPTED, not deleted.** Unlike P6.2's `RunStore`, it is not dead — it runs green, visits 556–557 checks per candidate, kills 15/15 mutations against a live validator over `assets/standardised-design-contract.v4.json`, an asset three other scripts read (`run_frozen_cohort.mjs:261`, `run_authority_projection_mutations.mjs:30`, `validate_authority_projection.mjs:34`), and its mutation set (M03/M04/M07/M08/M11/M12/M13) IS the style/geometry mutation half this package needed. It was orphaned for want of a caller, not for want of value. The pairing oracle is now that caller and asserts its verdict, kill count, non-vacuity (`vacuous == 0`) and the seven named style/geometry kills, plus the validator's own exit and visit count. Census disposition change is the handover.
- `visual_manual` VERDICT: **given meaning, not removed.** It is now the declared audit class of the `rendered_geometry` scope and of the two proven-blind mutations, defined as: reader-visible workbook state whose only detector is a human or a native spreadsheet host, because the print-render lane is provably blind to it. The suite enforces the binding — the class must be in the registry's `allowed_audit_classes`, the scope must carry it, and every `NO_VISUAL_DETECTOR` row must declare it. (Removal was the alternative, and is not available: `assets/development-test-registry.json` is forbidden to this package.)
- Host reality, corrected: the render lane IS satisfiable here. The project venv imports openpyxl 3.1.5 and PyMuPDF 1.26.5 together, LibreOffice is present, and a real conversion produced 9 pages across 3 sheets with 0 defects. P6.7's runtime doctor reports the lane unsatisfiable only because it defaults to CommandLineTools python3.9; with `EXCEL_INFLOW_PYTHON` set to the venv, `python_single_interpreter_lane_closure` passes. What genuinely needs another host: `reportlab`, for `scripts/render/selftest.py`'s synthetic per-check self-test — so the render checks are exercised here through real conversions of real mutants, but their synthetic fault-injection self-test remains unrun.
