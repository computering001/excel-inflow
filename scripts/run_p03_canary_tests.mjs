#!/usr/bin/env node
// P0.3 deliberate failing canary — TEMPORARY BRANCH ONLY. Proves that a
// failing registered test turns the authoritative exact-head gate red.
console.error("P0.3 canary: deliberate failure to prove the gate is authoritative.");
process.exit(1);
