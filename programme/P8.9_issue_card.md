# Work Package Issue Card

- Work package ID: P8.9
- Title: Close the last two freeze criteria — ONE skill-version declaration with every other site derived from it, and a certified runtime CODE closure recorded in a manifest that is structurally outside the set the digest is taken over (freeze criteria 9 and 11)
- Active invariant: A version flip is a ONE-FIELD edit to `assets/runtime-manifest.json#/skill_version` and the tree stays green, because no other site holds a copy of the version — every one derives it, and a new hard-coded literal anywhere in the shipped or checked surface is refused by a suite whose search space is enumerated from the repository's own manifests. And the certified runtime code closure is a genuine closure over the modules the shipped entry points transitively reach, recorded on an artifact that can never be a member of it, verified by recomputing the import graph and never by reading anything the recorded artifact says about its own membership.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`; started at `aa715ca`, and HEAD advanced TWICE mid-package as concurrent packages landed — `4577dc6` (P4.9 integration) and `5fd1b2a` (P5.9). All three values are recorded rather than reconciled; the final gate sweep was taken at `5fd1b2a`.
- Working tree status: DIRTY and SHARED. At least one other package was writing while this one ran: `scripts/lib/canonical_sum.mjs`, `scripts/run_never_zero_and_order_invariance_tests.mjs` and `programme/SCOPE_DECISION_2026-08-20.md` appeared untracked, and `scripts/lib/solver.mjs`, `scripts/lib/opening_debt_bridge.mjs`, `scripts/lib/model_ir_v3.mjs`, `scripts/lib/equation_graph.mjs` and `scripts/build_dynamic_model.mjs` were modified by their owners. None of those is this package's file. The consequence is recorded, not hidden: the certified code-closure digest MOVED under this package between one recording and the next (`6337e269…` -> `e2337e74…`, ~20 minutes apart, with no edit by this package to any closure member). That is the criterion-11 invariant working exactly as designed, and it is also why the recording must be renewed by P8.8 on the frozen tree immediately before the tag — see Unresolved (a).
- Observed failure or discovery question: two freeze criteria, both with a RED PROOF taken before any repair.
  - **Criterion 9.** The declared 3.7.6 -> 3.7.7 flip fails at head. Editing only `assets/runtime-manifest.json#/skill_version` turned FIVE registered suites red: `run_runtime_version_contract_tests.mjs:18`, `run_source_identity_tests.mjs:22`, `run_governance_evidence_tests.mjs:46` and `run_release_identity_governance_tests.mjs:35` each threw `ERR_ASSERTION "'3.7.7' !== '3.7.6'"`, and `run_blocker_corpus_tests.mjs` failed transitively because its replay corpus shells out to the identity-governance suite (`Error: Command failed: node run_release_identity_governance_tests.mjs`). A sixth site, `run_source_identity_tests.mjs:23` (`"Excel Inflow v3.7.6"`), was masked behind line 22 and would have fired next. A seventh, `KNOWN_LIMITATIONS.md:1`, goes stale silently because no gate reads it. This is the SAME failure the blocker corpus already immortalises from the 3.7.5 -> 3.7.6 flip (`corpus_manifest.json`, `known_reason_code`), reproduced one release later on a tree whose whole purpose was to become tag-able.
  - **Criterion 11.** `assets/runtime-manifest.json` recorded no `certified_runtime_code_closure_sha256`, so post-tag mutation had nothing to invalidate. `node scripts/run_certified_code_closure_tests.mjs` returned FAIL with `record.absent` and `record.definition_absent`. The deeper discovery is WHY it had never been recorded: **the obvious reading of the criterion is unsatisfiable**. P8.1's compiler computes a PACKAGE closure via `runtimeCodeClosureMembers` (scripts + Python modules + declared ASSETS + resources + vendored bytes) and states the digest for a sealer to transcribe into the manifest. But `assets/runtime-manifest.json` is in the deployment profile's `asset_allowlist`, so the manifest is a MEMBER of that closure: writing the digest into it changes the bytes the digest is taken over, and no fixed point exists. Proven, not argued — `packageClosureRecordingIsSelfReferential` hashes the manifest, writes that digest into it, and hashes again: `ded35827…` -> `035884b0…`. That is the fifth appearance of this programme's self-reference trap, and it is the reason `compile_skill_release.mjs:1240`'s recorded-vs-computed comparison could only ever throw.
- Earliest responsible layer:
  - (Criterion 9) The tripwire convention itself, at four sites. Each carried a comment saying it was "a DELIBERATE TRIPWIRE, not derivation", whose stated purpose was to force a bump owner to re-read the expectations. It never did that. `run_release_identity_governance_tests.mjs:32` records in its own words that the previous bump missed the line and "the registered gate ran red at exact head for a full release cycle (P0.3)". A tripwire that is discovered by a red build is not a review mechanism; it is a deferred red build. The layer beneath it is that the version had five declarations and no single one.
  - (Criterion 11) The MEMBERSHIP RULE in `scripts/lib/source_identity.mjs#runtimeCodeClosureMembers`, which admits assets. The publisher is only trapped inside the closure because the closure was defined to include the file that publishes it.
- Reproduction command: `node scripts/run_skill_version_declaration_tests.mjs`; `node scripts/run_certified_code_closure_tests.mjs`
- Focused failing test ID: skill-version-declaration (21 checks) and certified-runtime-code-closure (17 checks); both were red before this package and are green after
- Allowed files/modules: NEW `scripts/lib/skill_version_declaration.mjs`, NEW `scripts/lib/certified_runtime_code_closure.mjs`, NEW `scripts/run_skill_version_declaration_tests.mjs`, NEW `scripts/run_certified_code_closure_tests.mjs`, this card; the four tripwire suites (`run_runtime_version_contract_tests.mjs`, `run_source_identity_tests.mjs`, `run_governance_evidence_tests.mjs`, `run_release_identity_governance_tests.mjs`); the two fixture-literal suites (`run_release_dossier_tests.mjs`, `run_release_rollback_policy_tests.mjs`); `scripts/compile_skill_release.mjs` (one added refusal, in a currently unreachable branch); `assets/runtime-manifest.json` (the recording); `KNOWN_LIMITATIONS.md`; and the integration surfaces — `assets/development-test-registry.json` (two surgical rows), `assets/critical-invariant-oracle-matrix-v1.json` (`registry_sha256` rebind), `audit/v375-governance/test-registry-classification-census.json`, `architecture/ownership_census.json` + its two `FILE_OWNERSHIP` rows in `run_ownership_census_tests.mjs`, `programme/index.json`, `programme/checkpoints/**`.
- Explicitly out of scope, reported for their owners instead: `scripts/lib/solver.mjs`, `scripts/lib/canonical_model_modules.mjs`, `scripts/lib/model_ir_v3.mjs`, `scripts/lib/equation_graph.mjs`, `scripts/lib/metamorphic_relations.mjs`, `scripts/run_metamorphic_tests.mjs` (concurrent packages own them). `scripts/lib/source_identity.mjs` and `scripts/lib/identity_vocabulary.mjs` (imported and reused, never edited — the digest formula `runtimeCodeClosureIdentity` and the specifier grammar `release_js_import_scanner.mjs` are BORROWED, so this package adds no second digest formula and no second import grammar). The `assets/deployment-profile.json` `script_allowlist` disagreement found below. `programme/P8.7_issue_card.md` (a different, already-sealed package; read only). `test-corpus/**`, `evidence/**`, `programme/baseline_receipt.json` (historical records that must keep naming the versions they name — see the search-space note). `ci/test_registry_census.json` (blocked by another package's unregistered suite; see Unresolved (b)).
- Required integration gate: skill-version-declaration 21, certified-runtime-code-closure 17, runtime-version-contract 10, source-identity 22, governance-evidence 21, release-identity-governance, blocker-replay-corpus, release-dossier 281, release-rollback-policy 159, identity-vocabulary, release-package-attestation, package-ab 107, carrier-product-identity 38, runtime-closure 22, ci-governance, test-registry-contract 1070, ownership-census 10, behavioural-golden 197, programme-control, user-flow 14/14, certified fixtures byte-identical, `node --check` on every touched script
- Last known green commit: `5fd1b2a`
- Rollback plan: delete `scripts/lib/skill_version_declaration.mjs`, `scripts/lib/certified_runtime_code_closure.mjs`, `scripts/run_skill_version_declaration_tests.mjs`, `scripts/run_certified_code_closure_tests.mjs` and this card; `git checkout 5fd1b2a --` the six edited suites, `scripts/compile_skill_release.mjs`, `assets/runtime-manifest.json`, `KNOWN_LIMITATIONS.md`, `assets/development-test-registry.json`, `assets/critical-invariant-oracle-matrix-v1.json` and `scripts/run_ownership_census_tests.mjs`; regenerate the two censuses. Every behavioural change is additive: the two new libraries are imported by nothing that ships (neither is reachable from any `script_entry_points` root, which is checked), the new manifest fields are read by nothing that existed before, and the compiler's added refusal sits inside a branch that cannot be reached at `status: v2_development`.
- Attempt count (1-3): 1
- Status: SEALED for both criteria. Criterion 9 is closed end to end: the flip was performed in a scratch state and 15 suites stayed green. Criterion 11 is closed structurally and the digest is recorded; the RECORDED VALUE, however, is bound to the tree state at recording time and MUST be renewed on the frozen tree by P8.8 — this package proved it goes stale on a shared moving worktree, which is the invariant working and not a defect in it.

## Difference classification

- Intended correction:
  - **Criterion 9 — real derivation, not a second copy with a guard.** `assets/runtime-manifest.json#/skill_version` is the sole declaration. `scripts/lib/skill_version_declaration.mjs` is the only reader, and it offers no way to supply an expected version — there is no second value in the system to compare against. The four tripwires now derive:
    - `run_source_identity_tests.mjs` is the load-bearing one and is now STRONGER than the literal it replaced. `identity.skill_version` and `identity.release_name` come out of the whole `resolveSourceIdentity` pipeline (package manifest, external attestation, environment overrides, runtime manifest); the expectation comes from the declaration and from the deployment profile's product stem. They agree only if that pipeline really derives them, so a regression that let a stale `release-manifest.json` or an `EXCEL_INFLOW_*` override supply the version still fails here — which is exactly what the hard-coded literal used to catch, and it now catches it at every version rather than only at 3.7.6.
    - `run_governance_evidence_tests.mjs` and `run_release_identity_governance_tests.mjs` wanted a different property all along: that the ACTIVE candidate is not the historical release whose audit evidence they validate. That is now asserted directly (`assert.notEqual(runtime.skill_version, expectedVersion)`) instead of being smuggled through a literal that happened not to be `3.7.3`.
    - `run_runtime_version_contract_tests.mjs` keeps a shape assertion and an accessor-binding assertion: repoint the shared accessor at a different declaration site and this equality breaks.
    The per-site "review" the tripwires were reaching for is now STRUCTURAL: it is enforced once, by the new suite, over the whole surface, rather than four times by hand.
  - **The enumeration is derived from the repository's own manifests.** `versionLiteralSearchSpace()` is the union of `assets/deployment-profile.json` (`script_entry_points`, `script_allowlist`, `python_entry_points`, `python_module_allowlist`, `asset_allowlist`, `declared_only_assets`, `reference_allowlist`, `resource_directory_allowlist`), every `script` in `assets/development-test-registry.json`, a recursive walk of `scripts/`, `SKILL.md` and `KNOWN_LIMITATIONS.md` — **618 files**. No path is typed out. The `scripts/` walk was added because this package found that `run_governance_evidence_tests.mjs` — one of the four tripwires — is in NEITHER manifest, so neither manifest could ever have seen its literal.
  - **Two rules, no allowlist.** Rule A: a line (or JSON leaf) carrying the declared version next to a `skill_version`/`skillVersion` binding. Rule B: the derived release name `"<stem> v<version>"` anywhere. Prose that merely names a release ("the v3.7.x pack", "carried into the v3.7.7 freeze") is deliberately NOT flagged: prose cannot make a gate go red, and forbidding it would require historical sentences to be falsified on every bump. Nothing is exempted by path, so a new file gets no grace; the suite proves this by reporting all four offenders in a synthetic root rather than the first.
  - **Criterion 11 — the closure that can be recorded.** `certified-runtime-code-closure/1.0` is the transitive ES-module import graph under `scripts/`, walked from `assets/deployment-profile.json#/script_entry_points` (39 roots, **150 modules**). Assets are NOT members, so the publisher — an asset — is structurally outside the set the digest is taken over. There is no redaction, no exemption and no fixed point to solve, and `assertPublisherOutsideClosure` turns the structural fact into a checked one so that readmitting assets would be refused rather than silently restoring the paradox.
  - **The recording carries its own definition.** `certified_runtime_code_closure_sha256` is accompanied by `certified_runtime_code_closure.{definition, roots, membership_rule, module_count, publisher, publisher_is_a_member, self_reference_note, recorded_by, verified_by, certification_claim}`. `certification_claim: false` — the tree is `v2_development` and this is a recording, not a certification. And `compile_skill_release.mjs` now REFUSES, by name, to compare a manifest that names a closure definition against the package closure it computes, so the two definitions can never be silently compared.
  - **`--record` is the only writer.** A gate run recomputes and compares and writes nothing (P0.9).
- Regression: none intended and none found. The only behavioural changes to committed code are (1) four suites that assert derived equalities instead of literal ones, (2) five synthetic fixture version literals moved from `"3.7.7"` to `"0.0.1"` in `run_release_dossier_tests.mjs` and `run_release_rollback_policy_tests.mjs` — a fixture whose literal equals the declared version silently starts agreeing with production and stops testing anything, and at 3.7.7 these five would have done exactly that — and (3) one added refusal in `compile_skill_release.mjs` inside a branch unreachable at `status: v2_development`. No validator was weakened and the four repaired suites were proven not to have been: each still fails on the property it exists to protect (`run_source_identity_tests` still catches the instruction-rollback mutation and the four `assertCertifiedProductionIdentity` refusals; both governance suites still catch every convergence mutation and the historical-masquerade case), and the version half is strictly wider than before because it now holds at EVERY version rather than at one.
- Unresolved:
  - (a) **The recorded closure digest is bound to the tree state at recording time, and this tree moves.** It changed once during this package with no edit by this package (`6337e269…` -> `e2337e74…`). P8.8 must run `node scripts/run_certified_code_closure_tests.mjs --record` on the frozen tree immediately before the tag, and the suite will legitimately be red between any runtime-code change and the next recording. That is the invariant, not a defect in it — but it means this suite is a FREEZE gate, not a development gate, and whoever composes the tiered gate (P7.7) should place it accordingly.
  - (b) **`node scripts/run_ci_census_tests.mjs` cannot be regenerated or verified at this head, and the cause is not this package.** It refuses with `unclassified run_* scripts: run_never_zero_and_order_invariance_tests.mjs` — an untracked suite belonging to a concurrent package, neither registered nor given a disposition. Both of THIS package's suites are classified REGISTERED. The census must be regenerated by whoever lands that suite.
  - (c) **`assets/deployment-profile.json` `script_allowlist` disagrees with the real import closure at this head, in BOTH directions**, and the release compiler refuses on either: `lib/canonical_sum.mjs` is reachable from the entry points but absent from the profile (it is a concurrent package's untracked file — integration checklist item (f), missed for the fourth time), and `lib/lane_resource_policy.mjs` is declared in the profile but unreachable from any entry point (it is imported only by `run_bounded_parallelism_tests.mjs` and a Python module, so it is not in the JS closure at all — a P6.5-era declaration). Neither is this package's file; both must be fixed before `compile_skill_release.mjs` can build anything.
  - (d) **The certified CODE closure covers JavaScript modules only.** The 63 shipped Python modules are outside it, so editing `verify/xlsx.py` does not move this digest. That is criterion 11's own scope ("every module the shipped entry points transitively reach, per the deployment profile's `script_allowlist`"), and it is the narrowest closure that is recordable at all — but it is narrower than the package closure and the manifest says so in `membership_rule` rather than leaving a reader to assume otherwise. Widening it to the Python graph is possible (the compiler already computes that closure with an `ast` walk) and is named here as the follow-on rather than done silently.
  - (e) **Two closure definitions still coexist under adjacent names**, which is P8.2's unclosed divergence: the package closure (`runtime_code_closure_sha256`, assets included) and the certified code closure (`certified_runtime_code_closure_sha256`, modules only). They are now DISTINGUISHABLE — the recording names its definition and the compiler refuses to compare across definitions — but they are not unified, and unifying them requires deciding what `assertCertifiedProductionIdentity`'s `runtime_code_closure_match` should compare. That decision belongs with P8.2/P8.8.
  - (f) `run_governance_evidence_tests.mjs` carries a registered-gate-grade tripwire and is in neither `assets/development-test-registry.json` nor the deployment profile, so CI does not run it. This package repaired its literal and covered it with the `scripts/` walk, but registering it is somebody's decision, not a silent addition here.
  - (g) `KNOWN_LIMITATIONS.md` now carries NO version literal at all. Its heading reads "Excel Inflow development candidate". If a reader needs the version, it is one field away in the manifest; putting it back into the document reopens the criterion.

## Completion receipt

- **RED PROOF 1 (criterion 9), taken before any repair.** Flipping only `assets/runtime-manifest.json#/skill_version` to `3.7.7` at head: `run_runtime_version_contract_tests` exit 1 `'3.7.7' !== '3.7.6'` at `:18`; `run_source_identity_tests` exit 1 at `:22`; `run_governance_evidence_tests` exit 1 at `:46`; `run_release_identity_governance_tests` exit 1 at `:35`; `run_blocker_corpus_tests` exit 1 (`Command failed: node run_release_identity_governance_tests.mjs`). `run_carrier_product_identity_tests` and `run_runtime_closure_tests` stayed green, which localises the failure to the literals rather than to identity resolution. The manifest was restored byte-for-byte afterwards.
- **RED PROOF 2 (criterion 11), taken before any repair.** `node scripts/run_certified_code_closure_tests.mjs` exit 1 with exactly two findings — `record.absent` ("records no `certified_runtime_code_closure_sha256`, so a post-tag mutation of the runtime code has nothing to invalidate") and `record.definition_absent`.
- **THE SELF-REFERENCE PROOF.** `packageClosureRecordingIsSelfReferential` over the live manifest bytes: `ded358270537d584…` before recording, `035884b0ed0356e4…` after. The suite first asserts that `asset_allowlist` really does contain `runtime-manifest.json`, so the proof is about the real closure and not a straw one. The publisher-readmission refusal fires by name on both spellings (`is a MEMBER of the closure whose digest it publishes`, `admits only modules under scripts/`).
- **THE FLIP, PERFORMED END TO END.** In a scratch copy of the working tree the entire flip is one line — `diff` of the manifests reports exactly `4c4  "skill_version": "3.7.6," -> "3.7.7,"` and nothing else. Fifteen suites were green before the flip and the SAME fifteen were green after: skill-version-declaration, certified-runtime-code-closure, runtime-version-contract, source-identity, governance-evidence, release-identity-governance, blocker-replay-corpus, carrier-product-identity, runtime-closure, release-dossier, release-rollback-policy, identity-vocabulary, release-package-attestation, package-ab, ci-governance. `run_user_flow_tests` 14/14 in the flipped state. Derived values in the flipped state: `declaredSkillVersion` `3.7.7`, `declaredReleaseName` `Excel Inflow v3.7.7`, and `resolveSourceIdentity` returned `skill_version: 3.7.7` / `release_name: "Excel Inflow v3.7.7"` — the identity pipeline followed the one field with no other edit.
- **THE DECLARED VERSION AT THE END OF THIS WORK IS `3.7.6`, DELIBERATELY.** The flip was proven in scratch and the product tree was returned to its declared version. Performing the bump is not this package's act: `programme/SCOPE_DECISION_2026-08-20.md` puts it at integration step 3 ("skill_version flip to 3.7.7 (P8.9's centralised declaration)"), after the in-flight wave lands and before P7.8/P8.8. Bumping here would have declared a version for a tree that is not frozen, whose P5.9 blocker is open, and which is being written by other agents. What this package owes the freeze is that the flip is a one-field edit that stays green — which is proven — not the edit itself.
- **skill-version-declaration: 21 checks, 0 failures.** Product tree: exactly one declaration, well-shaped, release name derived from the profile stem. Search space: every `script_allowlist` entry, every `asset_allowlist` entry and every registered suite is inside it; all five sites this package repaired are inside it; the unregistered `run_governance_evidence_tests.mjs` is inside it. Red proofs, each on a synthetic root: a literal bound to `skill_version` in a shipped script (`literal.skill_version_binding`), in a registered suite, and in a shipped ASSET (reported by JSON pointer `#/pinned/skillVersion`); the derived release name in a document (`literal.release_name`); a second whole-value declaration inside the declaration file itself (`declaration.not_unique`, pointer `/pinned_version`). Non-vacuity: four offenders in one root are all four reported, not the first. Precision: prose naming a release is not flagged, and a digit run CONTAINING the version (built from the fixture version, never typed) is not flagged while the exact version in the same file is.
- **The suite caught itself.** Its own boundary-precision check originally typed the literal `"3.7.6"`, and the scan flagged `run_skill_version_declaration_tests.mjs:256` as a hard-coded version site. The check now builds its near-miss strings from the fixture version. That is the clearest evidence the scan is load-bearing: the first thing it refused was this package's own code.
- **certified-runtime-code-closure: 17 checks, 0 failures.** Product tree: 39 entry points, 150 modules, digest recorded and matching. The closure is proven to be a real graph walk — re-walking reproduces the set exactly, every member is an entry point or has an importer, and the closure is strictly narrower than a `scripts/lib/` glob. Red proofs on a synthetic root, all four directions: EDITING a module inside the closure changes the digest and yields `record.moved`; ADDING one changes it and yields `record.moved` + `record.module_count`; REMOVING one changes it (member count falls and the orphaned module leaves the set); and editing FOUR things outside the closure — an unreachable module, a suite that imports only unreachable modules, a reference document and a shipped asset that is not the publisher — leaves the digest bit-identical and the verdict PASS.
- **Anti-self-reference, proven four ways.** (1) Mutating the PUBLISHER — changing `skill_version` and adding a 500-byte field — does not move the digest and the verdict stays PASS. (2) Falsifying the profile's `script_allowlist` completely (removing a real member, adding a non-existent file and an unreachable one) does not move the digest, because membership comes from the graph and only the ROOTS come from the profile. (3) The manifest records no collection of any kind under `certified_runtime_code_closure`, and the suite asserts that structurally, so there is no member list for a verifier to read back — a deliberate departure from `compile_skill_release.mjs`, which records `certified_runtime_code_closure_files` for difference localisation. This package accepts worse diagnostics ("recorded X, computed Y" with no changed-file list) in exchange for a verification path that cannot consult the artifact it is checking. (4) The verifier module and its suite are themselves outside the closure, so neither the publisher nor the verifier can perturb the digest.
- **Refusals rather than approximations.** A non-literal dynamic import, a missing module and an import escaping `scripts/` are each refused by name; a digest recorded without its closure definition is refused; an absent recording is two findings, never a silent pass.
- Integration gate met in the product tree: skill-version-declaration 21, certified-runtime-code-closure 17, runtime-version-contract 10, source-identity 22, governance-evidence 21, release-identity-governance, blocker-replay-corpus, release-dossier 281 (plus `assemble_release_dossier.mjs --verify` PASS, zero violations), release-rollback-policy 159, package-ab 107, carrier-product-identity 38, runtime-closure 22, test-registry-contract 1070, ownership-census 10, behavioural-golden 197, programme-control, user-flow 14/14. Certified fixtures byte-identical: `test-fixtures/`, `fixtures/` and `goldens/` are untouched by this package's diff.
