# Work Package Issue Card

- Work package ID: P0.5
- Title: Create the terminal-reason registry and outcome compiler contract
- Active invariant: Every possible terminal reason has an owner and a legal terminal-state mapping.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit; parent `ecccef5`)
- Working tree status: clean at seal
- Observed failure or discovery question: The public boundary emits NEEDS_INTERNAL_WORK / generic BLOCKED / quality modes / blocker classes with no unified registry; inventory found 12 distinct status tokens, 4 sealed fatal reasons, 12 blocked_outcomes and 3 user-blocking classes across the boundary.
- Earliest responsible layer: terminal outcome contract (registry asset + enforcement suite)
- Reproduction command: `node scripts/run_terminal_reason_registry_tests.mjs`
- Focused failing test ID: terminal-reason-registry (registered; 228 checks)
- Allowed files/modules to change: `assets/terminal-reason-registry-v1.json` (new), `scripts/run_terminal_reason_registry_tests.mjs` (new), registry + oracle pin
- Explicitly out of scope: rewiring run_excel_inflow_vnext statuses onto the new registry (Phase 6 runtime fault isolation — declared in migration_wiring_status, not silent); changing any current emitted status (stored run states must keep validating)
- Required independent/physical proof: n/a (contract package)
- Required integration gate: registry contract 725 PASS; census 297 PASS
- Raw input/evidence hashes: bound by the sealing commit tree
- Last known green commit: `ecccef5`
- Rollback plan: revert this commit; nothing consumes the registry at runtime yet
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification

- Intended correction: none to runtime behaviour — the registry is new contract surface with test-time enforcement
- Presentation-only: none
- Accepted normalization: 15 reason codes unify the four sealed delivery fatal reasons (two SOURCE.*, two INTERNAL.*), the five envelope PROFILE.* stops, degradations and cancellation
- Regression: none
- Unresolved: the outcome-compiler rewiring (statuses actually emitted at the boundary) is Phase 6; until then the migration map is the sealed bridge and the suite enforces coverage of every schema-enumerated status

## Completion receipt

- Commands and exit codes: terminal suite 228 → 0; registry contract 725 → 0; census 297 → 0
- Evidence: assets/terminal-reason-registry-v1.json; the misclassification firewall (category_to_user_owned_terminals) is enforced with a mutation negative test — an internal category mapped to ACTION_REQUIRED is detected
- Residual risks: legacy stored run states keep the old vocabulary until Phase 6; the migration map classifies every token (preserve/rename/split/remove) so nothing is orphaned
- Next package first action (P0.6): the programme-control templates (issue/checkpoint/handover/diagnosis/release) must live IN the repository — copy the pack's templates into programme/templates/ and add a small conformance check that each exists and carries its required headings.
