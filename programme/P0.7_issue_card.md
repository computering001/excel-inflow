# Work Package Issue Card

- Work package ID: P0.7
- Title: Create the architecture ownership and mutation census
- Active invariant: Every current writer, reader and mutator of evidence, authority, forecast, ownership, graph, formula and workbook state is identified and classified; no refactoring during the census.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit; parent `dfb17e7`)
- Working tree status: clean at seal
- Observed failure or discovery question: No ownership map existed; the scan found 469 mutation sites across 49 files in eight state families, including three concrete duplicate-writer seams.
- Earliest responsible layer: architecture census (static; generator + gate)
- Reproduction command: `node scripts/run_ownership_census_tests.mjs` (verify) / `--write` (regenerate)
- Focused failing test ID: ownership-census (registered; committed census must match the source scan byte-for-byte; unknown-writer gate at zero)
- Allowed files/modules to change: `architecture/*` (new), `scripts/run_ownership_census_tests.mjs` (new), registry + oracle pin
- Explicitly out of scope: ANY refactor of the scanned files (prohibited by the package); runtime mutation receipts (declared for the Phase 3/5 shadow migration that consumes them)
- Required independent/physical proof: n/a (static census)
- Required integration gate: registry contract 735 PASS; census 301 PASS
- Raw input/evidence hashes: bound by the sealing commit tree
- Last known green commit: `dfb17e7`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification

- Intended correction: none — record-only package by design
- Presentation-only: none
- Accepted normalization: none
- Regression: none
- Unresolved: three named duplicate-writer seams (forecast state written by candidate compiler AND row_plan AND capture_transition; capture ownership with three writers; workbook formulas written at plan time AND rewritten post-emission) — these are the declared Phase 3/5/9 targets, recorded not repaired

## Completion receipt

- Commands and exit codes: ownership-census --write → 0; verify → 0 (9 checks, 469 sites, 49 files); registry contract 735 → 0; census 301 → 0
- Evidence paths/hashes: architecture/ownership_census.json, architecture/current_mutation_map.md
- Residual risks: the scan is line-regex based; dynamic property writes can evade it (named in the pack as a known limit) — the runtime mutation receipts in the shadow-migration phases close that gap
- Next package first action (P0.8): assemble the Phase 0 checkpoint binding all seven package seals, the CI run receipts (canary red + head green), test results, corpus hashes, known defects, rollback point and Phase 1 prerequisites; blocked only on the two live Actions runs concluding.
