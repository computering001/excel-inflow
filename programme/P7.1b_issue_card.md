# Work Package Issue Card

- Work package ID: P7.1b
- Title: Archetype case catalogue — ECONOMICS group (period, tax, debt-schedule, lease and perimeter shapes)
- Active invariant: Each archetype case exercises ONE named accounting shape a naive implementation gets wrong, and the catalogue records TYPED, CHECKABLE expectations the runner asserts; an entry whose only claim is that the case compiles is inadmissible, and a declared expectation with no implemented check (or a check that asserts nothing) fails the suite.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` at `85ac6f2` (worktree /Users/archiepreston/Documents/Codex/excel-inflow-v377; NOT committed by this package)
- Working tree status: dirty by design — this package adds new files only (catalogue, 33 cases, runner, this card); the pre-existing in-flight modifications of other packages were untouched
- Observed failure or discovery question: the product's model-shape evidence was two certified case shapes (standard-maximal, standard-net-cash). Nothing proved what happens to the accounting shapes real issuers file — a 53-week retail year, a stub period, a changed year end, a loss year's tax, a zero-rate jurisdiction, PIK and accreting debt, a fully drawn or fee-only revolver, a benchmark floor, mixed FX balance bases, sale-and-leaseback, lease-only leverage, impairment and discontinued perimeters, NCI, associates, deferred revenue, capitalised interest, depletion, a regulated asset base, hyperinflation. Which of those does the compiler get right, and where does it get them wrong?
- Earliest responsible layer: evidence tier (no production layer is modified by this package); the defects it found sit in scripts/lib/validation_invariants.mjs (solution roll-forward identity), scripts/lib/tax_rate_policy.mjs (zero-versus-credit classification), scripts/lib/instrument_period_state.mjs (untyped class refusal), scripts/lib/support_envelope.mjs + assets/support-envelope-v377.json (UNSUPPORTED verdict with no early-stop predicate) and assets/model-case-v2.schema.json (no NOL, IAS 23 or period-length vocabulary)
- Reproduction command: `node scripts/run_archetype_economics_tests.mjs`
- Focused failing test ID: archetype-economics (NOT yet registered — see out of scope; 598 checks over 33 archetypes and 65 typed expectations, including 8 self-mutations)
- Allowed files/modules: NEW assets/archetype-catalogue-economics-v1.json; NEW test-fixtures/archetypes/economics/*.json (33 synthetic v2 cases) + README.md; NEW scripts/run_archetype_economics_tests.mjs; NEW programme/P7.1b_issue_card.md. Read-only: assets/archetype-case-catalogue-v1.schema.json (shared with the presentation group), assets/support-envelope-v377.json, assets/terminal-reason-registry-v1.json, assets/model-case-v2.schema.json, test-fixtures/cases/*. No production script, no scripts/lib/**, no validator weakened.
- Explicitly out of scope: the registry row for this suite (assets/development-test-registry.json is owned elsewhere, and `node scripts/run_ci_census_tests.mjs` will list run_archetype_economics_tests.mjs as unclassified until `{"id":"archetype-economics","phase":"evidence","runtime":"node","script":"run_archetype_economics_tests.mjs","timeout_seconds":600,"mutates_product_tree":false,"test_class":"mutation"}` is added beside archetype-presentation); repairing any of the eight defects found; the extraction tier — every entry carries extraction_evidence_still_required: true, so a real filing is still needed to prove the product can READ these shapes; test-fixtures/archetypes/presentation/** and assets/archetype-catalogue-presentation-v1.json (parallel group)
- Required integration gate: run_archetype_economics_tests.mjs green over every entry (598/33); user-flow 14/14; `node scripts/run_evidence_run_tests.mjs test-fixtures/cases --emit-clean <tmp>/donor.json` yielding donor sha256 22710b0de9212d34d3ce597579d95e3836f8e49ffb4ad3b2aca097127e9371f8 — unchanged from the pin in scripts/lib/raw_canary_fixture.mjs; economic-IR 79; archetype-presentation 557/29 (parallel group undisturbed); `node --check` on the runner and JSON.parse on every new asset
- Last known green commit: `85ac6f2`
- Rollback plan: revert the seal commit. The package is additive-only (a catalogue asset, 33 fixtures under test-fixtures/archetypes/economics/, a runner and this card), so reverting removes evidence and changes no product behaviour; the registry row and the ownership-census classification are removed with it. The hash-pinned synthetic donor is unaffected either way.
- Attempt count (1-3): 1
- Status: COMPLETE (evidence tier authored; registry row and defect repairs tracked, not silent)

## Difference classification
- Intended correction: new evidence only — a typed archetype catalogue plus its runner. No production behaviour changes; no validator was relaxed to make a case pass.
- Regression: none. The suites that glob test-fixtures/cases/ are untouched and the hash-pinned synthetic donor is byte-identical; the new cases live only in test-fixtures/archetypes/economics/.
- Unresolved: eight defects/gaps recorded as archetype expectations rather than repaired — (1) validateSolutionInvariants' debt.instrument_roll_forward identity omits pik_interest_native and fair_value_movement_native, so every PIK or accreting instrument fails a release-grade invariant the certified fixtures never exercise; (2) the tax-rate ledger classifies a genuine reported zero rate as tax_credit_on_profit, so a tax holiday is indistinguishable from a credit and the row escalates; (3) instrument class lease_liability is in the model-case schema enum and in the envelope's declared debt matrix, yet compiling it throws an untyped Error (no code, no typed_internal_outcome, no registered reason); (4) classifySupport returns support_class UNSUPPORTED for an UNSUPPORTED dimension verdict with early_stop.stopped false and reason_code null, so the only legal terminal (UNSUPPORTED_PROFILE) is unreachable; (5) no NOL/tax-loss stock exists in the case contract, so a recovery year after forecast losses is taxed in full; (6) no IAS 23 capitalised-borrowing-cost field exists, so the whole coupon is expensed and capex understated; (7) no period-length, stub or changed-year-end vocabulary exists, so a stub period and a changed year end are only ever reported as per-period fiscal-year mismatches, mapped to no terminal reason code; (8) the case contract spells the 52/53-week calendar `52_53_week` while the support envelope's fiscal_calendar dimension spells it `week_52_53`, so no string join relates a case's declared calendar to the envelope value that certifies it (the same prefix split exists between the envelope's UNSUPPORTED_PROFILE.* early-stop codes and the registry's PROFILE.* keys, which this runner normalises rather than repairs).

## Completion receipt
- archetype-economics: 598 checks over 33 archetypes and 65 typed expectations — 6 period_arithmetic, 26 value_state, 15 identity_or_footing, 9 authority_selection, 8 schedule_behaviour, 1 typed_refusal (SOURCE.opening_debt_unresolved, registered). Zero entries rest on compiles_clean.
- Runner self-mutations (8): an unimplemented expectation, an archetype with no check group, a bound check that asserts nothing, a compiles-only entry, an unregistered refusal reason code, a case_path inside the hash-pinned donor directory, a case_path inside the presentation group's directory, and an envelope claim naming an undeclared dimension value — each must fail the run, and each does.
- Red proofs of bite: deleting benchmark_floor from the floored-term-loan case fails floating_benchmark_floor_binding; converting the PIK note to a cash coupon fails pik_only_debt; appending an unchecked expectation to any entry fails with "has no implemented check".
- Donor integrity: donor sha256 22710b0de9212d34d3ce597579d95e3836f8e49ffb4ad3b2aca097127e9371f8 before and after the package, equal to the pin in scripts/lib/raw_canary_fixture.mjs.
