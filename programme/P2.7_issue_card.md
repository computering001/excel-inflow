# Work Package Issue Card

- Work package ID: P2.7
- Title: Versioned policy registry — model-owned assumption families bind to {policy_id, version, owned_assumption, evidence_hierarchy} (REGISTRY + THREE-FAMILY BINDING SLICE)
- Active invariant: Every model-owned assumption family binds to a versioned registry entry; the case-level policy objects (rcf/cash/lease) carry the stamped binding; a registry version change is receipt-visible on the sealed case.
- Release/phase: v3.7.7 / Phase 2
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on `dec2126`; uncommitted per programme rule)
- Working tree status: shared with in-flight P4.3 (solver.mjs, filings-extraction schema, schedule_typed_states) — P2.7 files are disjoint from that set except none
- Observed failure or discovery question: ~17 assumption families, only four with any version identity; the three case policy objects were structurally UNVERSIONABLE — proof A showed `model-case-v2.schema.json` refuses a `policy_binding`/`version` field on rcf/cash/lease (additionalProperties:false at the :1230/:1321/:1405 regions), so no policy decision could ever be version-receipted.
- Earliest responsible layer: model-case contract (schema) + compilePolicies attach seam (case_compiler.mjs)
- Reproduction command: `node scripts/run_policy_registry_tests.mjs`
- Focused failing test ID: policy-registry (NOT yet registered — assets/development-test-registry.json is forbidden this package; registration is follow-on). 286 checks incl. proof-A resolution on both certified fixtures, 10 validator mutation negatives, the version-bump-changes-receipt mutation, and an end-to-end keel-derived compile carrying all three bindings.
- Allowed files/modules: NEW assets/policy-registry-v1.json (16 families) + assets/policy-registry-v1.schema.json + scripts/lib/policy_registry.mjs (load-validate-freeze mirroring economic_solve_policy.mjs) + scripts/run_policy_registry_tests.mjs; MODIFY assets/model-case-v2.schema.json (ONLY the three policy objects: optional `policy_binding {policy_id const, version}` — +54 additive lines, everything else byte-identical), scripts/lib/case_compiler.mjs (compilePolicies return now `stampPolicyBindings(...)` + one import line), scripts/lib/rcf_policy.mjs + scripts/lib/lease_policy.mjs (POLICY_ID/VERSION consts). OUT-OF-LIST BUT GATE-REQUIRED: scripts/run_case_compiler_equivalence.mjs — one additive JUSTIFIED pattern `/^(?:rcf_policy|cash_policy|lease_policy)\.policy_binding\b/` (certifiedAbsent-only, value disagreements still count); without it the stamped binding exits the keel 1 and breaks the registered classification-resolution gate (18). This mirrors how forecast_authority_ledger/ownership_census/rcf_policy.mode landed.
- Explicitly out of scope (REMAINING P2.7, follow-on): binding the 13 declared_only families (registry declares them with binding_status:"declared_only"): tax_rate_policy receipt rebind; acquisition (four in-place writers in forbidden files); forecast_assumptions driver vocabulary (solver.mjs forbidden); coverage_policy (coverage.mjs); historical_interest_basis LEGACY_BASIS default; debt-recon residual 0.05 tri-plication; fx_translation minting at case_compiler.mjs:3258-3269 (outside allowed region); residual_interest_authority; materiality/economic-solve/runtime-budget/debt-class external identities; proofs B (equivalence policy-hash receipt in model_ir — model_ir_v3.mjs forbidden), C (declared-vs-defaulted lease basis parity), D (role_policy_owned census visibility — forecast_completion_constitution.mjs forbidden); suite registration in assets/development-test-registry.json.
- Required integration gate: user-flow 14/14, filings 43, classification-resolution 18, statement-classifier PASS (fixture standard-maximal-v2), policy-registry 286, full custody equivalence KNOWN RED totals UNCHANGED (55 diffs / 90 blocks / 1 failed solve / 0 / 0, zero policy_binding diffs) — all verified green/pinned
- Last known green commit: `dec2126` (with this working tree)
- Rollback plan: revert the five modified files and delete the four new files + this card
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked above)

## Difference classification
- Intended correction: the three policy objects become versionable and are stamped at compile; registry declares full 16-family coverage with explicit binding_status; all changes additive (legacy sealed cases without a binding stay legal — the field is optional; newly compiled cases always carry it)
- Regression: none (all gates green; custody baseline totals byte-identical; model-case schema strictness elsewhere proven unchanged by suite negatives)
- Unresolved: declared_only bindings and proofs B/C/D (listed above with named owners/files)

## Completion receipt
- Proof A red→green: `policy_binding` on rcf/cash/lease in a fixture copy failed with 3 "not an allowed property" errors; after the surgical schema addition both certified fixtures validate stamped, while unknown properties, cross-family policy_ids, versionless bindings and extra binding fields all still refuse
- policy-registry suite: {"status":"PASS","checks":286,"families":16,"bound":3,"declared_only":13,"custody_e2e":"run"}
- Version-bump mutation: bumping rcf_policy to 1.1 in a registry clone changes the stamped binding and the sealed policy hash (hashValue) — receipt-visible by construction
