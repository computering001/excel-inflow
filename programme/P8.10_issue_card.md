# Work Package Issue Card

- Work package ID: P8.10
- Title: D44 — `production_model` was declared, gated, and unreachable by any case in the repository
- Active invariant: The release smoke test must prove the product builds in the mode the product actually ships in, on an input the deployment profile DECLARES — not one passed ad hoc on a command line.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`; committed by the coordinator at `57819c2`
- Working tree status: card written by the coordinator. The package stalled on a stream watchdog timeout with its work complete; the coordinator independently verified the package compiles before sealing.
- Observed failure or discovery question: the release package could not be built at all. `compile_skill_release.mjs --development --smoke-case test-fixtures/cases/standard-maximal-v2.json` refused with three missing contract versions, and the refusal reproduced OUTSIDE the harness via `build_dynamic_model.mjs ... --plan-only`, so it was not a harness artefact.
- Earliest responsible layer: `assets/deployment-profile.json` — the profile names 29 Python entry points and their smoke commands but never named the case the release build compiles. The one input deciding whether a release can be built was undeclared.
- Reproduction command: `node scripts/compile_skill_release.mjs --skill . --out <tmp> --development --smoke-case test-fixtures/cases/standard-maximal-v2.json`
- Focused failing test ID: the release compile itself
- Allowed files/modules: `assets/deployment-profile.json`, `assets/evidence-run-v1.schema.json`, `scripts/compile_skill_release.mjs`, `scripts/run_raw_input_black_box_canary.mjs`, NEW `test-fixtures/release-smoke/`.
- Explicitly out of scope: `instrument_period_state.mjs`, `model-case-v2.schema.json`, `support-envelope-v377.json`, `terminal-reason-registry-v1.json` (P7.11 held them).
- Required integration gate: `compile_skill_release.mjs --development` exits 0 and produces an archive and attestation; `run_user_flow_tests` 14/14; certified fixtures accounted for.
- Last known green commit: `8641134`
- Rollback plan: `git revert 57819c2`. Reverting restores a tree in which no release package can be compiled from any in-repo case.
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification
- Intended correction: `production_model` IS reachable — disposition (a). The smoke case is now a declared, hash-pinned release input under `assets/deployment-profile.json#/release_smoke_case`, and the compiler defaults to it.
- Regression: none. Certified fixtures untouched; no golden regenerated.
- Unresolved: the fixture is a build gate, not a corpus — one issuer, IFRS, USD, two instruments, broker lane `explicit_skip`. The profile entry says so under `not_certification_evidence`.

## Completion receipt
- **The shortcut was declined, and that is the finding.** `run_evidence_run_tests.mjs --emit-compiled-case --production` also yields a case that satisfies the builder's gate — but it does so by OVERWRITING `execution_profile` on the compiler's finished output. Declaring `production_model` at the SOURCE instead makes the evidence run refuse the synthetic fixture (`evidence.dcs.production_authority_missing`, `evidence.broker.ingress_missing`). Using the stamped output would have put a case in the release gate declaring a mode whose entry conditions it never met — the exact class this programme keeps finding.
- The case is the COMPILER'S OWN OUTPUT, carrying the three contract versions by construction rather than by stamping. `test-fixtures/cases/**` are pre-compiler artefacts; feeding one to the builder skips the stage that makes it conformant.
- Provenance is the digest-pinned SYNTHETIC donor, not real filings. Verified by the coordinator: no custody markers, and the fixture ships in `test-fixtures/`, never `private-test-custody` or `fixtures/external`.
- Verified by the coordinator rather than taken on report: `--development` produces `archive_sha256 96fe1139…`, `attestation_sha256 ebe61826…`, `package_mode development`.
