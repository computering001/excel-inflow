# Work Package Issue Card

- Work package ID: P2.2
- Title: Structured-fact <-> visible-row reconciler for inline-XBRL filings
- Active invariant: When a filing carries inline XBRL, the extraction lane reconciles structured facts against the visible face-statement rows — each reconciled row period records the XBRL concept, context_ref, unit_ref and decimals in its provenance; a material disagreement between a structured fact and the printed row value is a typed fail-closed finding; rows/filings without XBRL are recorded as typed-unreconciled, never silently skipped.
- Release/phase: v3.7.7 / Phase 2
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (uncommitted working-tree change on top of `4bed9ed`)
- Working tree status: dirty — shared worktree carries other in-flight packages; P2.2 touches only its allowed files
- Observed failure or discovery question: RED PROOF — a synthetic inline-XBRL filing whose tagged `us-gaap:Revenues` fact (4,321m) contradicts the printed revenue row (1,000m) ran `run_filings_pipeline.mjs --responses` to `PASS` with `violation_count: 0` and no XBRL provenance anywhere: `extract_inline_xbrl.py` was never invoked by the pipeline, no concept->role crosswalk existed, and no provenance carried context_ref/unit_ref/decimals.
- Earliest responsible layer: `scripts/run_filings_pipeline.mjs` (the extraction lane's compiler never consulted the structured half of the filing it had already hash-bound)
- Reproduction command: `EXCEL_INFLOW_PYTHON=<venv-python> node scripts/run_xbrl_reconciliation_tests.mjs`
- Focused failing test ID: xbrl-reconciliation (32 checks incl. the red-proof mutation: contradicting material fact must fail closed; concept absent from crosswalk and non-XBRL filings must surface typed-unreconciled)
- Allowed files/modules: scripts/extract_inline_xbrl.py (new `--reconcile` mode; extraction mode byte-identical), scripts/run_filings_pipeline.mjs (wire + fail-closed gate), assets/filings-extraction-response-v1.schema.json (optional additive `xbrl_provenance` row field + `xbrlFactProvenance` $def), NEW assets/xbrl-concept-role-crosswalk-v1.json, NEW scripts/run_xbrl_reconciliation_tests.mjs, this card
- Explicitly out of scope (REMAINING P2.2, on the work order): registering the suite/crosswalk in the test registry and filings-runtime-members closure (registry files forbidden to this package); opening-cash instant-open basis; ESEF dimension-default handling; native HTML statement extraction (extractor remains PDF-only — inline-XBRL runs still require a caller/native response for the visible rows)
- Required integration gate: filings 43, user-flow 14/14, statement-scope-inventory 32, xbrl-reconciliation 32, py_compile — green
- Last known green commit: `4bed9ed`
- Rollback plan: revert the five P2.2 files (working-tree change; no commit was made)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: pipeline now reconciles inline-XBRL facts against selected face rows through the declared crosswalk; material disagreement -> NEEDS_EXTRACTION_REVIEW with typed `xbrl_face_reconciliation_review` task; bundle rows carry per-period concept/context_ref/unit_ref/decimals provenance; non-XBRL filings recorded typed-unreconciled (behavior additive for the legacy PDF lane)
- Regression: none (filings 43 and user-flow 14/14 green; extraction mode of extract_inline_xbrl.py byte-identical for the registered real-corpus suite)
- Unresolved: runtime-members/registry declaration of the new asset and suite (forbidden files; follow-on), opening-cash basis, HTML-native visible-row extraction

## Completion receipt
- xbrl-reconciliation 32 -> 0 (red-proof contradiction now a typed material `XBRL_FACT_FACE_ROW_MISMATCH` finding carrying concept, context_ref, unit_ref, decimals, printed vs tagged value; sign-inversion, instant-basis, dimension-qualified-only, foreign-currency, rounding-tolerance, no-crosswalk and no-XBRL paths all typed)
- Crosswalk declares 75 concepts (us-gaap + ifrs-full) onto 35 statement-semantic-taxonomy roles; no parallel role vocabulary introduced
