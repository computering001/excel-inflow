# Work Package Issue Card

- Work package ID: P7.9
- Title: A transform decides what a quantity means from its declared semantics, not from a regex over its name (defect register D33, its three siblings, and MG-3's corrected attribution)
- Active invariant: A metamorphic transform must decide what a quantity MEANS from its declared semantics, not from a regex over its name; and a transform that changes the economics it claims to preserve must fail the suite rather than be reported as a product defect. Corollary landed with it: a classification that cannot be made total against a declared authority must FAIL LOUDLY on an unrecognised value, never default — and a repaired defect must not leave a loosened comparison standing behind it.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `dc7937b` (working tree; NOT committed, per the work order)
- Working tree status: four files — `assets/metamorphic-relations-v1.json`, `scripts/lib/metamorphic_relations.mjs`, `scripts/run_metamorphic_tests.mjs`, `scripts/run_scale_invariant_convergence_tests.mjs` — plus this card and `programme/gap-reports/DEFECT_REGISTER.md`. The shared worktree carries several concurrent agents' in-flight deltas; two facts about that are recorded rather than glossed, in Unresolved.
- Observed failure or discovery question: `scripts/lib/metamorphic_relations.mjs:733` guarded the commitment fee under the unit-scale transform with `!/rate/i.test(commitment_fee_convention)` — a test on a convention's NAME standing in for a test on what the convention MEANS. Under the x1000 restatement it scaled `rcf_policy.commitment_fee_value` from 35 to 35,000 while the convention was `bps_on_undrawn`, turning a 35bp fee into a 350% fee, and then reported the resulting non-convergence as a scale-invariance defect of the solver (registered as D30/MG-3, SEVERITY HIGH).
- Earliest responsible layer: the transform register. The dimension of `rcf_policy.commitment_fee_value` is a function of a DECLARED convention, and the transform inferred it from the convention's spelling instead of reading a declaration. Every sibling found in the sweep has the same earliest layer: a guard that pattern-matches a name where a schema enum, a `semantic_role`, a `numeric_types` or a declared `error_class` was available.
- Reproduction command: `node scripts/run_metamorphic_tests.mjs` (82 checks) and `node scripts/run_scale_invariant_convergence_tests.mjs` (34 checks)
- Focused failing test ID: metamorphic-relations (the eight new checks are the four SELF-MUTATIONs proving no rename can buy a dimension, the totality-bites check, the register-is-total check, MG-3's corrected attribution and MG-3's measured margin) and scale-invariant-convergence (section A, whose three D33 pins are inverted)
- Allowed files/modules: `assets/metamorphic-relations-v1.json`, `scripts/lib/metamorphic_relations.mjs`, `scripts/run_metamorphic_tests.mjs` (P7.4's layer, this package's remit), `scripts/run_scale_invariant_convergence_tests.mjs` section A only (P4.9's D33 pins, which pin the defect being repaired and must invert with it), `programme/gap-reports/DEFECT_REGISTER.md`, this card. `assets/model-case-v2.schema.json`, `assets/model-case.schema.json`, `assets/case-source.schema.json` and `assets/statement-semantic-taxonomy.v1.json` are READ as authorities and never written.
- Explicitly out of scope: `scripts/lib/solver.mjs`, `scripts/lib/canonical_model_modules.mjs`, `scripts/lib/model_ir_v3.mjs`, the authority graph, `assets/runtime-manifest.json`, anything carrying the `skill_version` literal, `assets/equation-graph.v1.json` and `assets/convergence-contract.v1.json` (P4.10 in flight), `scripts/lib/instrument_period_state.mjs` and `scripts/lib/canonical_sum.mjs` (P7.10), `test-fixtures/**`, every golden and every frozen custody pack. Seven of the ten guards the sweep found live in `case_compiler.mjs`, `forecast_behavior.mjs`, `evidence_resolution_v2.mjs` and two Python oracles — all forbidden here, all registered with reproductions instead (D37..D43).
- Required integration gate: metamorphic 82, scale-invariant-convergence 34, user flow 14/14, behavioural goldens 197, ownership census 10, never-zero and order-invariance 12 (P7.10's D36 lock), programme control 446, `node --check` on all three `.mjs` files, both certified fixtures hash-identical — all met.
- Last known green commit: `dc7937b`
- Rollback plan: `git checkout dc7937b -- assets/metamorphic-relations-v1.json scripts/lib/metamorphic_relations.mjs scripts/run_metamorphic_tests.mjs scripts/run_scale_invariant_convergence_tests.mjs programme/gap-reports/DEFECT_REGISTER.md` and delete this card. Note this also reverts P7.10's handed-over MG-5 retirement, which is why it must be re-applied if a rollback happens.
- Attempt count (1-3): 1
- Status: SEALED for D33 and its three in-remit siblings; MG-3 corrected and re-pinned at its measured margin; MG-5's retirement landed on P7.10's behalf; seven out-of-remit siblings ESCALATED with reproductions (D37..D43).

## Difference classification

- Intended correction:
  - **RED FIRST, and the red proof is stronger than the register's.** The conventions were enumerated from their DECLARED sources — the `commitment_fee_convention` enums in `assets/model-case-v2.schema.json`, `assets/model-case.schema.json` and `assets/case-source.schema.json` — never from a hand-written list. There are **FIVE**, not the three the register recorded: `none`, `bps_on_undrawn`, `captured_in_residual`, plus `percent_of_margin` (legacy schema) and `bps_on_committed` (case-source schema). **`/rate/i` matches none of them**, so the guard was dead code. Four of the five are rates and the fifth is a declared zero placeholder, so there is no admitted value on which the retired guard's behaviour was correct. Measured: the transform scaled 35 -> 35,000 on BOTH revolver archetypes (0.35% -> 350% on an undrawn facility) while `rcf_policy.capacity` scaled correctly, i.e. it moved the rate and the base together.
  - **The repair dispatches on a declaration, and the declaration is checked TOTAL at import.** `assets/metamorphic-relations-v1.json` gains `unit_restatement_dimensions`. Each quantity names its discriminator and the JSON-Pointer into the SCHEMA that admits its values, and classifies every admitted value as `monetary` (scale-covariant), `rate` (scale-invariant), `declared_zero_placeholder` or `non_numeric`, each with a stated reason citing the reader that consumes it (`solver.mjs:2737` divides `bps_on_undrawn` by 10000 and multiplies by a currency balance; `attachment_ingress.mjs:2192` stamps `captured_in_residual` to an explicit 0).
  - **Why a new convention cannot silently misclassify.** `validateUnitRestatementDimensionTotality()` runs at IMPORT, on the same channel P7.4 used for `validateNodeObservableTotality`. It resolves each declared schema pointer and requires: every value the schemas admit is classified (`UNIT_DIMENSION_UNCLASSIFIED_VALUE`); every classification is admitted by some schema (`UNIT_DIMENSION_CLASSIFICATION_ORPHAN`, so the register cannot be padded to launder a future value); every dimension is in the declared vocabulary; every entry carries a reason; a keyed container's authority is CLOSED (`additionalProperties: false`) before its key set may be called exhaustive; and an authority that does not resolve is reported rather than skipped. Any error throws at module load, so the whole metamorphic layer refuses to load until a human classifies the new value. At the call site there is no fallback branch at all: `discriminatedUnitDimension` and `keyedUnitDimension` THROW `UNIT_DIMENSION_UNCLASSIFIED_CONVENTION` / `UNIT_DIMENSION_UNCLASSIFIED_KEY`, naming the file to edit and listing what is declared. An ABSENT convention throws too — the retired `?? ""` default swallowed that case whole.
  - **The sweep found three more siblings inside this layer, and all three are repaired.** (1) `NON_MONETARY_RECONCILIATION_KEY = /percentage|_rate|ratio|tolerance_bps/` over `debt_reconciliation` KEY names — correct today only by a coincidence of spelling. (2) **`key !== "eligible_percentage"` in the cash-bucket loop: a denylist of ONE key that is not a property of `$defs/cashBucket` at all.** The string appears nowhere else in the repository; the real keys are `net_debt_eligible_percentage`, `interest_eligible_percentage` (both schema-bounded to [0,1]) and `cash_yield`. The guard was unconditionally true, so the transform multiplied all three by 1000, producing a case that violates its own schema `maximum: 1`. It never fired in anger only because NO case in the corpus carries `cash_policy.buckets` — dead code guarding a live branch, which is D33 exactly. Both are now total against closed schema key sets. (3) `RATIO_SEMANTIC_ROLES = new Set(["effective_tax_rate"])` — a hand-written set standing in for "the roles that are ratios"; the taxonomy declares that set and it has TWO members, so `margin` was missing and a sourced margin row would have been scaled. Now derived from `roles[].numeric_types`.
  - **Four SELF-MUTATIONS prove the repair has bite, and one proves the old guard was wrong.** Every discriminator value and every container key is renamed through eight spellings that inject or remove the words the retired regexes matched on (`_rate`, `_RATE`, `rate_`, `_percentage`, `_ratio`, `_amount`, underscore-stripped, upper-cased) and every one must produce a NAMED throw. Separately both retired predicates are REPLAYED over the same renames and asserted to disagree with the declared dimension — `/rate/i` scaled all five admitted conventions including two rates and two placeholders; the key regex flips its verdict on `reported_opening_gross_debt` -> `reported_opening_gross_debt_ratio` and on `maximum_residual_percentage` -> `maximum_residual_share`. Without that replay the mutations would prove only that the new guard is strict, not that the old one was wrong. A fifth check bites the totality validator in six directions.
  - **MG-3 corrected, not deleted.** `severity` HIGH -> MEDIUM, `status` still OPEN, and `title`, `reproduction`, `consequence`, `repair_owner` and `retirement_condition` restated. The suite pins inverted: `run_metamorphic_tests.mjs:838` used to assert "MG-3 reproduces: the only case the restatement refuses is the registered one" and now asserts a faithful restatement refuses NOTHING (archetypes_refused 1 -> 0, generated_refused 1 -> 0, archetypes_exact 30 -> 31); `:851`'s "MG-3's cause is an absolute tolerance" is replaced by a live proof of the attribution, which re-injects the contamination by hand and shows the 350% fee still refuses. A third check pins what SURVIVES as a MEASURED margin rather than as prose: the worst faithfully restated period (`lease_only_no_funded_debt.json` period 0 at x1000) lands at residual 9.9035e-9 against the declared 1e-8 ceiling — margin 0.99035 — with `binding_term: "declared_absolute_ceiling"` and `within_declared_envelope: false`. It fails if the margin WIDENS (the mechanism was repaired without retiring the entry) or CLOSES (it became a refusal). MG-3 is deliberately absent from the receipt's `known_defects_reproduced` and appears under a new `known_defects_corrected_not_reproduced`, so no reader can take the old attribution from the receipt.
  - **MG-5's retirement landed on P7.10's behalf, and the epsilon it justified is GONE.** P7.10 closed D32 by construction but owns neither of the two files the retirement needs. Parts 1-4 of its verified patch are applied here. The part that mattered was part 4, and it was CHECKED rather than applied: the refusal plane compared magnitudes at a 1e-9 relative epsilon whose own `why_not_exact` field named MG-5 as the sole justification. Established before removing it — (a) `comparison.relative_epsilon` has exactly one reader in the repository, `compareRefusalVerdicts`; (b) measured across the WHOLE refusal plane with P7.10's repair present, 8 preserving families over 208 refused cases, **1,658 comparisons: not one passes only because of the epsilon, and not one non-zero drift**. The comparison is now `"numeric_comparison": "exact"`, the epsilon is recorded as `retired_relative_epsilon` rather than erased, and the MODE is declared on the plane rather than living as a constant in the comparator — so re-loosening it is a visible register change and an undeclared mode throws. **One dependant P7.10's patch list did not name was found**: the suite's own mutation check asserted `compareRefusalVerdicts(base, base.replace("-943.56", "-943.5600000000002")).equal === true`, a test asserting the loosening. It now asserts `false`, with an added equal-compares-equal case so the comparator is not trivially strict.

- Regression: none. Both certified fixtures are hash-identical over a signature that includes `residual`, `iterations`, `converged` and `convergence_tolerance` (`standard-maximal-v2` e5a5b353…, `standard-net-cash-v2` 0d89110a…), asserted by scale-invariant-convergence and green at `4577dc6`+P7.9. Behavioural goldens 197, user flow 14/14, ownership census 10, programme control 446, never-zero and order-invariance 12. No golden and no frozen custody pack was regenerated. The metamorphic receipt moves in exactly the declared directions: checks 74 -> 82, `archetypes_refused` 1 -> 0, `generated_refused` 1 -> 0, `archetypes_exact` 30 -> 31, `refusal_magnitude_drift_observations` 26 -> 0, `known_defects_open` 5 -> 6 (MG-5 out, MG-6 and MG-7 in).

- Unresolved:
  - **Seven siblings of D33 live outside this package's remit and are registered, not repaired: D37..D43.** Every guard was read at its cited line and every count recomputed against the declared authority. The two that should be read first: **D37** — `case_compiler.mjs:765` computes `numeric_type` with the OLD unanchored `/(?:margin|rate|percent|percentage)/i` and passes it in as a declared field, short-circuiting the word-bounded fix at `statement_classifier.mjs:190` that a regression test already guards; it mislabels 9 aliases across 5 roles declared `numeric_types: ["currency"]`, **and `run_case_compiler_equivalence.mjs:841`, the independent equivalence oracle, replicates the identical regex**, so producer and checker share the defect and the cross-check is blind to it. **D38** — `evidence_resolution_v2.mjs:581` decides source-fault vs engineering-fault ownership by regexing a finding ID, and is wrong in both directions: a tampered source seal is filed as an engineering bug, while an internal ledger fault is blamed on the user because its id begins `source_`.
  - **MG-6 and MG-7 are OPEN by construction, and deliberately not "fixed" with a default.** `operating_metrics` is declared `additionalProperties: {$ref: metricSeries}` and `statementRow.semantic_role` is declared `{"type": "string"}` — both OPEN key spaces. A total classification needs a closed key set, and closing either is a schema change with compiler, solver and golden readers. Adding a declared default would be the very thing this package's invariant forbids, so the transform still scales by omission there and the suite ASSERTS the residual rather than assuming it: a row carrying an untyped `gross_margin` role IS still multiplied by 1000, and that assertion is pinned to MG-7.
  - **D36 is untouched and not contradicted.** P7.10's `reporting_total` order dependence is locked by `run_never_zero_and_order_invariance_tests.mjs` (12 checks, green here). P7.9 asserts nothing about `reporting_total`; the refusal plane now demands EXACT equality of reported magnitudes under a register permutation, which is the direction D36's repair would move toward and never away. The plane is green at exact comparison because `reporting_total` reaches no refusal message it compares, while the opening-debt bridge residual — which it does compare — was canonicalised by P7.10.
  - **The shared worktree cost two things that should be on the record.** (1) `scripts/run_scale_invariant_convergence_tests.mjs` is being edited concurrently by P4.10, which is landing the MG-1 edge; its section D (certified-fixture hash equality) is RED in the live tree because P4.10's graph change redefines the residual over 16 components instead of 13. That failure is P4.10's, in P4.10's own pin, and is theirs to resolve. P7.9's section-A edits were verified green in isolation at `4577dc6` (34/34) before P4.10's edits arrived, and are unaffected. (2) This package's `programme/gap-reports/DEFECT_REGISTER.md` edits were swept into commit `e3ace99` by another agent's `git commit` on the shared tree. P7.9 ran no `git add` and no `git commit`.
  - **The class is not closed by this package.** D33 was the sixth instance; the sweep found ten more, three repaired here and seven registered. The structural observation worth carrying: in D37 and D39 a fix was written, commented and regression-tested at one site while the same expression survived at the site that actually runs — so "we fixed that regex" is not evidence that the defect is gone.

## Completion receipt

- RED, verified before any edit: `/rate/i` matches NONE of the five conventions admitted by the three declared schema enums, so the guard had never fired; `applyTransform("unit_scale_restatement", revolver_undrawn_commitment_fee_only)` returned `commitment_fee_value` 35 -> 35000 (0.35% -> 350%) while the family's own `non_monetary_paths_held_fixed` declared that very path held; two archetypes were corrupted this way. Separately, `eligible_percentage` was confirmed absent from `$defs/cashBucket` and from the entire repository, so the bucket guard was unconditionally true.
- GREEN: metamorphic-relations 82 checks, `refusal_magnitude_drift_observations: 0`, `known_defects_open: 6`, `known_defects_reproduced: [MG-1, MG-2, MG-4]`, `known_defects_corrected_not_reproduced: [MG-3]`, `unit_scale_restatement: {archetypes 31, exact 31, refused 0, generated 92, refused 0}`. scale-invariant-convergence 34 checks (verified in isolation at `4577dc6`).
- MG-3's surviving mechanism, MEASURED and pinned: `economics/lease_only_no_funded_debt.json` period 0, restated x1000 — residual 9.903487807605416e-9 against an applied tolerance of 1e-8, margin 0.9903487807605416, `binding_term: "declared_absolute_ceiling"`, `within_declared_envelope: false`.
- The epsilon removal, MEASURED before it was made: 1,658 refusal-plane comparisons across 8 economics-preserving families and 208 refused cases, `drifted_at_1e_9: 0`, `would_fail_if_exact: 0`. The epsilon had one reader and no dependant.
- Numeric identity: both certified fixtures hash-identical — `standard-maximal-v2` e5a5b35338643623bc4371e88e5ce161b92ca23e105d35d3cfa3ae296b5f3b8c, `standard-net-cash-v2` 0d89110a30f37f491bbd0747433b5e2f24a93937b0a10fd57c7c38d847555693. No golden or frozen custody pack regenerated.
- Gate: metamorphic 82, scale-invariant-convergence 34, user flow 14/14, behavioural goldens 197, ownership census 10, never-zero and order-invariance 12, programme control 446, `node --check` clean on all three `.mjs` files.
