# Work Package Issue Card

- Work package ID: P4.1a
- Title: Close the never-zero coercions in opening-instrument provenance (D11) and type the unsupported-instrument-class refusal (D12)
- Active invariant: A declared amount that carries no number — missing, nil, blank, whitespace-only, boolean, array, object or unparseable text — is a typed never-zero state, is NEVER selected into the opening register with a numeric balance, and contributes nothing to `reporting_total`; and a refused instrument class reaches a lawful terminal through a registered reason code, never a bare throw.
- Release/phase: v3.7.7 / Phase 4 (repair of code sealed as P4.1)
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` at `1267ffe` (verified in a detached worktree at that tip plus only this package's files; the live worktree is being edited concurrently by other agents)
- Working tree status: uncommitted by instruction; three files (two edits, one new suite) plus this card
- Observed failure or discovery question: D11 — the P7.3 generated cohort proved four never-zero coercions inside freshly sealed P4.1 code: `typedDeclaredAmount(" ")`, `(false)` and `([])` returned `reported_zero` (value 0) and `(true)` returned `reported_number` (value 1), because `Number()` maps all of them onto real numbers. All four were then SELECTED into the opening register with a numeric basis amount, the state reported `PASS`, and `true` added a fabricated 1 to `reporting_total`. `instrument_period_state.mjs` carried a NEVER-ZERO comment that named `Number(false)` as a defended coercion, but the guard tested an ALLOW-LIST of two states (`nil`, `reported_blank`) — the defence was incomplete, not absent, which is why P4.1's own 30-check suite passed throughout. D12 — a `lease_liability` instrument class died on a bare `Error: Unsupported debt class lease_liability` with no `code`, no `typed_internal_outcome` and no registered reason, while `validateCaseShape` returned zero errors for the same case (the class is in the model-case schema enum AND the envelope's declared debt matrix), so validator and compiler disagreed and the refusal could not reach a lawful terminal.
- Earliest responsible layer: `opening_instrument_provenance.typedDeclaredAmount` (the typing), `instrument_period_state.compileOpeningInstrumentState` (the selection guard), `instrument_period_state.instrumentStates` (the untyped class refusal)
- Reproduction command: `node scripts/run_opening_amount_typing_defect_tests.mjs`; the cohort originals replay from `node scripts/generate_case_cohort.mjs --count 1 --seed 7730022` (also 7730012 / 7730034 / 7730000)
- Focused failing test ID: opening-amount-typing-defect (new; 31 checks — the four coercions, the four not-selected proofs, the four reporting-total proofs, two independent mutation proofs, a validator-strengthening proof, and the D12 typed refusal with its contract claim mismatch)
- Allowed files/modules: `scripts/lib/opening_instrument_provenance.mjs` (whitespace/non-scalar typing before the coercion; a selected candidate must be value-bearing — a validator STRENGTHENING), `scripts/lib/instrument_period_state.mjs` (the guard becomes `numericValueOf(...) === null`, one reading of the declared amount, and the class refusal is typed on the P3.7 pattern with its message unchanged), the new suite, this card
- Explicitly out of scope: lease-liability support ON the debt-instrument register (the refusal is correct — leases are compiled by the `lease_policy` lane, which reserves the `lease_liability` identity); the schema-enum / envelope-matrix over-claim that makes the refusal reachable (both assets sealed to other owners; reported); the registry addition the refusal deserves (a lane-routing code owned by the case contract; registry is read-only for this package); retirement of the eight cleared cohort pins and the two archetype D12 pins (needs `ci/generated_case_seeds.json`, a forbidden path — see the completion receipt)
- Required integration gate: opening-instrument-provenance 30, opening-debt-bridge 19, schedule-typed-state 13, typed-financial-value 86, this suite 31, user-flow 14/14, acquisition-solver on standard-maximal-v2 PASS, generated cohort — all green except the cohort's pin-retirement signal, below
- Last known green commit: `1267ffe`
- Rollback plan: revert the two library edits; the new suite and this card are additive
- Attempt count (1-3): 1
- Status: OPEN — repair complete and proved; sealing blocked on pin retirement outside this package's allowed surface

## Difference classification
- Intended correction: (D11) a non-numeric declared amount is typed never-zero, refused typed at selection, and absent from every total; (D12) the existing refusal gains `code`, a registered `typed_internal_outcome` and a named contract mismatch — the message and the refusal decision are unchanged
- Regression: none in the gate. One out-of-gate suite regresses BY DESIGN: `scripts/run_archetype_economics_tests.mjs` (598 checks, PASS at `1267ffe`) fails its two D12 *defect pins* at lines 701-702 — `check(compiled.error.code === undefined, "DEFECT: ...")` and `check(compiled.error.typed_internal_outcome === undefined, "DEFECT: ...")`. Both must invert to `!== undefined` (and the catalogue statement at `assets/archetype-catalogue-economics-v1.json:454` must drop its "DEFECT:" framing) now that the throw is typed. Not edited here: not in this package's allowed files.
- Unresolved: the eight cleared cohort pins; the two archetype pins; the missing lane-routing reason code; the schema/envelope over-claim

## Completion receipt
- opening-amount-typing-defect 31 → 0. `" "` → `reported_blank`; `false`, `[]`, `true` → `parse_failure` with a real `raw_text` and `failure_reason` (`[]` records `"[]"`, never `""`). All four read `null` through `numericValueOf`, none is selected, each is registered `opening_balance_unresolved` (reason vocabulary REUSED, not extended), the state's `reporting_total` is `null` and the total replayed from provenance alone is exactly the clean row.
- P4.1's own suite unchanged and still 30/30 — no expectation of it needed revising.
- Generated cohort: known defects 15 → 7, `defects_new` 0, `property_assertions` 5986 unchanged, violation instances across the 300-case ci_default sweep 238 → 54 (the eight cleared signatures accounted for exactly 184 of them; the remaining seven signatures' counts are identical before and after). The runner now fails ONLY its own retirement signal ("retire the pin instead of leaving it standing") on the eight cleared signatures. Retiring them requires re-deriving `assets/generated-case-dimension-space-v1.json` and `ci/generated_case_seeds.json` (the runner binds the registry to the space digest) — `ci/**` is forbidden to this package, and the re-derivation rebinds the other seven pins, owned by the support-envelope agent working concurrently. It must be one serialised step after the wave lands.
