# Work Package Issue Card

- Work package ID: P8.0
- Title: Declare the portable certification tier (PREREQUISITE; native-Excel and visual-review evidence recorded as PERMANENT DECLARED EXCLUSIONS)
- Active invariant: The release vocabulary admits a PORTABLE CERTIFIED tier whose evidence set is exactly the evidence a portable host can produce, with the native-Excel and visual-review classes recorded as permanent declared exclusions — not waivers, not pending items, not silence; a portable-certified package is honestly distinguishable from a natively-certified one, and nothing may present a portable certification as a native one.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: shared worktree with concurrent packages; the three files here are disjoint from theirs, and the gate was re-verified in a detached worktree at the branch tip plus only these three files
- Observed failure or discovery question: The Phase-8 work order proved the freeze was structurally blocked, not merely incomplete. `REQUIRED_EVIDENCE` (release_certification.mjs:19-27) named `native_excel` and `visual_review`, both permanently excluded by standing directive, and `compile_skill_release.mjs:1120-1136` throws unless the evidence receipt is PASS — so `--certify` could never pass for structural reasons. `PACKAGE_MODES` (identity_vocabulary.mjs:7) offered only `development` and `certified`, so there was no third tier to freeze into and no vocabulary in which a portable claim could even be spelled. Measured before the change: `PACKAGE_MODES = ["development","certified"]`; `assertPackageMode("portable_certified")` threw; a dossier of the five portable classes scored FAIL with 25 violations, structurally including `manifest.schema: $.evidence.native_excel is required`, `$.evidence.visual_review is required`, `evidence.native_excel.path`, `evidence.visual_review.path`. P8.1, P8.7 and P8.8 were dead ends behind that.
- Earliest responsible layer: the release vocabulary itself — the package-mode enum and the evidence-set constant. There is no earlier layer: the tier did not exist as a value anywhere, so every consumer downstream was correct to refuse it.
- Reproduction command: `node scripts/run_portable_certification_tier_tests.mjs`
- Focused failing test ID: portable-certification-tier (82 checks, 21 refused mutations; red in two stages before the package — first `SyntaxError: ... does not provide an export named 'CERTIFICATION_TIER_CONTRACT'` from identity_vocabulary.mjs, then `... does not provide an export named 'NATIVE_REQUIRED_EVIDENCE'` from release_certification.mjs)
- Allowed files/modules: scripts/lib/identity_vocabulary.mjs (third package mode + tier contract + the physical-lane terminal declaration), scripts/lib/release_certification.mjs (evidence split, exclusion register, portable dossier contract, tier-aware validator), NEW scripts/run_portable_certification_tier_tests.mjs, this card
- Explicitly out of scope (permanently EXCLUDED, declared in the register as EXCLUDED_NATIVE_HOST): the `native_excel` and `visual_review` evidence classes — P8.5 in full and the native halves of P8.3/P8.4/P8.6. Also out of scope for this package and reported for their owners instead: the `--portable-certify` compile path and the mode selection at compile_skill_release.mjs:277 (P8.1's surface, FORBIDDEN here); the orchestrator's tier reporting (P8.8's surface, FORBIDDEN here); the `package.mode` enum in assets/product-identity-v2.schema.json and the hard-coded `['certified','development']` at release_package_attestation.mjs:158 (both FORBIDDEN here); a `--tier` flag on scripts/validate_release_certification.mjs; a JSON schema asset for the portable manifest; the development-test-registry entry that puts this suite in CI; and renaming the `PASS_PENDING_MANUAL` / `PENDING_NATIVE_EXCEL_AND_VISUAL_REVIEW` tokens, which other suites pin.
- Required integration gate: this suite (82), release-certification-contract 25, source-identity 22, runtime-closure 22, release-identity-governance (6 + 7), release-rollback-policy 159, typed-product-identity-vocabulary 20, programme-control 124→125 (the +1 is this card), `node --check` on all three touched files — green
- Last known green commit: `e31b53e`
- Rollback plan: revert the two library edits and delete the suite and this card. Both edits are purely additive — `REQUIRED_EVIDENCE` keeps its exact membership and order, `PACKAGE_MODES` appends rather than reorders, and the validator's native path is the default — so reverting restores the prior behaviour byte for byte and no caller has to change.
- Attempt count (1-3): 1
- Status: SEALED (prerequisite; the tier is declared and satisfiable, the compile/orchestrate wiring is reported to P8.1 and P8.8)

## Difference classification
- Intended correction: additive vocabulary. `portable_certified` is minted as a THIRD, separately named package mode appended after `certified`, deliberately a distinct string rather than a flag on `certified`, so every pre-existing `=== "certified"` comparison in the repository keeps meaning "natively certified" and a portable package cannot drift into a native claim by default. `REQUIRED_EVIDENCE` is split into `PORTABLE_REQUIRED_EVIDENCE` (five classes) and `PERMANENTLY_EXCLUDED_EVIDENCE` (two), with a load-time assertion that the split exactly exhausts the unchanged native set and that the exclusion register agrees with the physical-lane declaration. The exclusion register reuses P8.6a's idiom — `satisfiability`, `exclusion_reason`, `excluded_from_portable_gate: true` — with `EXCLUDED_NATIVE_HOST` as the native-host sibling of `EXCLUDED_INSTALLED_HOST`. `validateReleaseCertificationEvidence` takes `certificationTier`, defaulting to the native tier, so no existing caller changes behaviour.
- Regression: none. The native tier is byte-for-byte the prior path — same manifest schema, same seven classes in the same order, same per-class validators — and gained one refusal it did not have (a dossier declaring a tier, a package mode or exclusions is refused at the native gate by name, on top of the schema refusal it already earned). `assertPackageMode`'s diagnostic still reads "development, certified, ..." so the two suites pinning `/development, certified/` are untouched.
- Unresolved: nothing COMPILES a portable-certified package yet — compile_skill_release.mjs:277 still selects only `development` or `certified`, and both it and orchestrate_release.mjs are forbidden here, so the wiring is reported rather than done. Two forbidden files will refuse a portable identity until their owners widen them: assets/product-identity-v2.schema.json pins `package.mode` to `["development","certified"]`, and release_package_attestation.mjs:158 hard-codes `['certified','development']`. The suite pins the first residual precisely — a portable product identity may diverge from `product-identity/2.0` on `package.mode` and on nothing else. The suite is not yet in assets/development-test-registry.json, so CI does not run it — and because that asset is forbidden here, `node scripts/run_ci_census_tests.mjs` goes RED on `unclassified run_* scripts: run_portable_certification_tier_tests.mjs` until one registry entry is added (verified in a detached worktree: green at the clean tip, green with the two library edits and this card, red only once the suite file lands). This is the same disposition P8.6a recorded for release-rollback-policy. The two token strings are declared, not renamed.

## Completion receipt
- portable-certification-tier 82 checks, 0 failures. Evidence split: 5 portable-required (`exact_maximal`, `exact_net_cash`, `frozen_cohort`, `finance_proof_mutations`, `source_parity`) + 2 permanently excluded (`native_excel`, `visual_review`). A portable dossier of the five classes, with both exclusions declared, now returns PASS where it returned FAIL/25 before.
- An excluded class cannot be mistaken for a pending one: the register and every dossier record must carry `exclusion_disposition: PERMANENT_DECLARED_EXCLUSION`, `is_pending: false`, `is_waiver: false`, `revisit_condition: null`, `excluded_from_portable_gate: true`, a reason of at least 40 characters, and a `required_host_capability` equal to the code-declared value. Each of those six is independently mutated and refused.
- Mutations refused: 21. A portable dossier naming the native mode; the same dossier submitted to the native gate; each excluded class presented as satisfied evidence (2); an exclusion presented as pending by disposition, by `is_pending`, by `revisit_condition`, or as a waiver (4); an exclusion counted as a portable-gate pass; an exclusion with a token reason; both exclusions omitted; one exclusion omitted; an exclusion renaming the missing host capability; each of the five portable-required classes missing (5); an invented class substituted for a missing one; the native schema_version spoofed onto a portable dossier; an escape-hatch key.
- The portable tier is not an escape hatch, proven against bytes: a tampered rendered page, a dossier bound to the wrong runtime-code closure, and an attempt to override the immutable V4 authority identities outside the adversarial test mode are all still refused inside the portable path. An unknown or `development` tier throws rather than defaulting to portable.
- The native tier did not get easier to claim, proven by four refusals: `assertNativeCertifiedPackageMode("portable_certified")` and `("development")`; `assertCertifiedPackageMode("development")`; and `assertCertifiedProductionIdentity` (P8.2a's, committed and untouched) refusing a fully-populated portable identity with `package_mode=certified`. At the native gate the portable dossier still earns `evidence.native_excel.path` and `evidence.visual_review.path`, and a native receipt carries `declared_exclusions: null`.
- Three self-mutations of the new code were each caught by the suite, so the checks are load-bearing rather than tautological: neutering the `is_pending` refusal, neutering the native-gate portable-dossier guard, and shortening the portable required set.
- The physical lane's permanent terminal state is declared once, in `PHYSICAL_LANE_TERMINAL_DECLARATION` (identity_vocabulary.mjs): `PASS_PENDING_MANUAL` / `PENDING_NATIVE_EXCEL_AND_VISUAL_REVIEW` at exit 0 is `PERMANENT_DECLARED_EXCLUSION` with `is_pending: false`, `revisit_condition: null` and `token_wording_is_historical_misnomer: true`. The tokens and the exit code are unchanged; a portable receipt carries the declaration so the meaning travels with the evidence.
