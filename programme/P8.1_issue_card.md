# Work Package Issue Card

- Work package ID: P8.1
- Title: Prove the package is reproducible (A/B from two clean checkouts, with a SMALLEST-DIFFERENCE report) and bind every build input the manifest was silent about
- Active invariant: Two builds from two independent clean checkouts of the same commit produce byte-identical archives; any difference is reported as a SMALLEST DIFFERENCE — which member, which byte offset, which two byte values, and for a JSON member which JSON path — never as a bare inequality, and never by excluding the member that differs. The release manifest binds every input that can change behaviour, so a package cannot silently claim provenance it does not have.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`; the A/B proof itself was run at `e3f3ba2` (P8.0's seal) in two detached worktrees
- Working tree status: shared worktree with concurrent packages (goldens, economic authority seal, runtime doctor are other agents' in-flight files). The four files here are disjoint from theirs. The A/B proof was run in two `git worktree add --detach e3f3ba2` checkouts, each verified clean immediately after creation, with this package's three source files plus the pending deployment-profile entries applied to BOTH identically as a declared overlay whose own digest is recorded in the receipt.
- Observed failure or discovery question: The determinism INGREDIENTS were all present and good — a real ES-scanner closure (release_js_import_scanner.mjs:101), non-literal dynamic imports refused (compile_skill_release.mjs:375-379 pre-edit), the closure reconciled against `script_allowlist` in both directions (:413-431), a commit-date build timestamp (:288-310), a canonical ustar archive with mode/uid/gid/mtime zeroed (release_package_attestation.mjs:102-144). What did not exist was the PROOF. Measured before the change: `ls scripts/lib/package_ab_comparison.mjs scripts/run_package_ab_proof.mjs` → No such file; a repository-wide grep for `smallest.?diff|byte.?identical|two clean checkouts|reproducib|A/B` over `scripts/` returned only workbook-level and case-generator hits plus one comment at run_ci_census_tests.mjs:93 about caller-built A/B WORKBOOKS — nothing that builds a PACKAGE twice and compares. Second, the compiler read four inputs it never bound: `grep -c` over compile_skill_release.mjs returned 0 for each of `production-contract-v2`, `support-envelope`, `envelope_version`, `goldens`, `toolchain`, `certification_tier`, `declared_exclusions`, `PHYSICAL_LANE_TERMINAL_DECLARATION`. A real baseline build confirmed it empirically: `certification` carried no tier and no exclusions, the whole toolchain record was `nodeVersion` plus `pythonExecutable: "python"` (a BASENAME), and the contract and envelope digests were bound only ANONYMOUSLY, as two rows among 344 in `files` — nothing stated which envelope version the package claimed and nothing compared the claim with the shipped bytes. Third, a THIRD red surfaced that blocks the freeze outright: at `e3f3ba2` the package does not build at all. `--development` in a clean detached worktree fails at compile_skill_release.mjs:421 — `script_allowlist disagrees with the computed import closure. Required by the closure but absent from the profile: lib/economic_ir.mjs, lib/formula_ast.mjs, lib/opening_instrument_provenance.mjs, lib/required_role_closure.mjs, lib/source_tolerance.mjs` — and then at :1014 on `asset_allowlist is missing assets the closure loads at runtime: economic-ir-v1.schema.json, required-role-closure-v1.schema.json`. Seven entries in a FORBIDDEN asset stood between the repository and any reproducibility claim whatsoever.
- Earliest responsible layer: for the proof, a layer that did not exist — there was no A/B driver, so nothing downstream was wrong to be silent. For the bindings, the release compiler itself: it is the only writer of `release-manifest.json` and the only place that reads the contract, the envelope, the goldens and the toolchain at build time. For the closure/profile disagreement, `assets/deployment-profile.json` — five library modules and two schema assets entered the runtime closure in earlier packages (P3.1's Economic IR among them) without the profile being updated, and the compiler was correct to refuse.
- Reproduction command: `node scripts/run_package_ab_tests.mjs` (unit + compile-layer), and for the real thing `node scripts/run_package_ab_proof.mjs --out <receipt.json> --commit <sha> --python <venv python> --soffice <soffice> [--overlay <dir>]`
- Focused failing test ID: package-ab (107 checks; red in three stages before the package — first `ERR_MODULE_NOT_FOUND` for `./lib/package_ab_comparison.mjs`, then `expected FAIL naming inputBindings.*` against a manifest with no `inputBindings` block at all, then the compile-layer refusal that did not exist because `--portable-certify` was not a flag)
- Allowed files/modules: scripts/compile_skill_release.mjs (the `--portable-certify` branch, the tier-aware evidence gate, the recorded-provenance guard, the physical-lane declaration, the build-input bindings), NEW scripts/lib/package_ab_comparison.mjs, NEW scripts/run_package_ab_proof.mjs, NEW scripts/run_package_ab_tests.mjs, this card
- Explicitly out of scope, reported for their owners instead: `assets/deployment-profile.json` — the seven closure entries above, without which nothing builds (REPORTED, applied by the parent). `assets/runtime-manifest.json` — the five `certified_*` fields freeze criterion 11 needs; this compiler now STATES them on stderr and in the manifest but does not write the asset. `assets/development-test-registry.json` — the two registry rows that put this suite in CI. `scripts/lib/release_package_attestation.mjs` — `release_gate_status` reads `DEVELOPMENT_PACKAGE_NOT_CERTIFIED_OR_INSTALLED` for a `portable_certified` package and the two `packageMode === "certified"` assertions do not apply to the portable tier (P8.8/attestation owner). `scripts/lib/release_journal.mjs`, `scripts/orchestrate_release.mjs` — the orchestrator's tier reporting and journal (P8.8). `scripts/lib/identity_vocabulary.mjs`, `release_certification.mjs` (P8.0's, imported not edited), `source_identity.mjs`, `runtime_isolation.mjs` (P8.2a's; `runtimeCodeClosureMembers` imported, never redefined). `goldens/**` (concurrent agent). Also out of scope: a JSON schema asset for the binding record, the portable evidence DOSSIER itself (P8.7 — so `--portable-certify` is wired and refutation-tested but cannot be run end to end until `evidence/release_v377/` exists), and renaming `PASS_PENDING_MANUAL` / `PENDING_NATIVE_EXCEL_AND_VISUAL_REVIEW`, which other suites pin.
- Required integration gate: package-ab 107, source-identity 22, runtime-closure 22, portable-certification-tier 82, release-package-attestation 33, release-identity-governance PASS, runtime-version-contract 10, release-rollback-policy PASS, programme-control 129, `node --check` on all four touched files, a full `--development` compile reaching exit 0 in a CLEAN detached worktree, and the A/B proof itself — green
- Last known green commit: `e3f3ba2`
- Rollback plan: delete the three new files and revert the compiler. Every compiler edit is additive — `--portable-certify` is a new flag whose absence reproduces the old two-mode behaviour exactly, `certificationTier` defaults to the native tier in the validator so `--certify` is byte-for-byte its old path, the recorded-provenance guard is skipped for `--development` and is a no-op when `certified_package_mode` is absent, and the two new manifest keys plus the physical-lane declaration only ADD fields. Reverting restores the prior archive bytes exactly; no caller has to change.
- Attempt count (1-3): 1
- Status: SEALED (A/B byte-identical proven; the deployment-profile entries that make it buildable and the runtime-manifest recording that closes freeze criterion 11 are reported, not applied)

## Difference classification
- Intended correction: (1) A REAL A/B driver. `run_package_ab_proof.mjs` creates two `git worktree add --detach` checkouts of one commit, verifies each is clean immediately after creation and is at the requested commit, generates the smoke case ONCE so both builds are handed the same input bytes, hands both builds an identical scrubbed environment (`EXCEL_INFLOW_BUILD_TIMESTAMP`, `SOURCE_DATE_EPOCH`, `EXCEL_INFLOW_SOURCE_COMMIT`, `EXCEL_INFLOW_SOURCE_TREE` are deleted so a deterministic-timestamp override can never be what makes the proof pass), builds, and compares. Two checkouts rather than two builds in one tree, deliberately: a second build in the same directory shares the absolute paths, the index and the residue of the first, so it can only prove idempotence. (2) A SMALLEST-DIFFERENCE report that parses both ustar archives and names the first differing member in archive order, the first differing byte inside it, both byte values, both archive-level offsets, printable context for each side, and — for a JSON member — the JSON path that moved. Difference classes are typed: `MEMBER_BYTES`, `MEMBER_SIZE`, `MEMBER_ONLY_IN_A`, `MEMBER_ONLY_IN_B`, `MEMBER_HEADER`, `MEMBER_ORDER`, `ARCHIVE_PADDING`, `ARCHIVE_NOT_PARSEABLE_AS_USTAR`. (3) The ANTI-TAUTOLOGY, enforced in the API rather than in a comment: there is no exclusion parameter, and ten spellings of one (`exclude`, `ignore`, `skip`, `allowDifferences`, `tolerate`, …) are REFUSED by both the comparison and the binding validator. (4) `inputBindings` on the manifest: the production contract and the support envelope bound by path, digest, declared version field AND declared version value, each cross-checked against the shipped inventory row for the same path so a named binding and an anonymous file row can never disagree; the behavioural goldens bound by content when present and by a TYPED ABSENCE with a disposition and a 40-character reason when not; and a real build toolchain — Node version, V8, module ABI, OpenSSL, the selected Python interpreter's VERSION and every third-party package version the smoke test ran against — under its own digest, basenames only, never host paths. The record self-hashes over the bindings it covers. (5) `--portable-certify`: a third mode branch minting P8.0's `portable_certified` through `assertPackageMode`, gated by `validateReleaseCertificationEvidence({ certificationTier: packageMode })` so a portable build is held to the portable evidence set with the two native classes declared as permanent exclusions, requiring the same real clean-root workbook smoke as `--certify`, and admitting `v2_development` or `local_production_certified` as source status (declared in code, with the status recorded in the manifest either way — a portable certification is a claim about EVIDENCE, not about a status string). (6) A NEW refusal that did not exist: `assertRecordedCertificationProvenance`, called at the earliest responsible layer — the moment the package mode is minted, before any closure work — refuses to mint the native `certified` mode once `assets/runtime-manifest.json` records `certified_package_mode: portable_certified`, and directs the caller at `--portable-certify`. Claiming LESS than the recording is permitted and is recorded as `REQUESTED_TIER_IS_WEAKER_THAN_RECORDING`. (7) `PHYSICAL_LANE_TERMINAL_DECLARATION` attached ONCE, after the results row, to the evidence object that carries the two pinned tokens — the tokens, their spelling and the exit-0 contract are untouched; what was missing was any statement of what they mean.
- Regression: none. `--certify` passes the native package mode as its certification tier, which is the validator's default, so the native gate is exactly the path it always was and gained nothing but one new refusal (a certification receipt whose `package_mode` disagrees with the mode being minted). `--development` skips the provenance guard entirely. The provenance guard is a no-op when `certified_package_mode` is absent, so every existing runtime manifest keeps its historical native meaning. The manifest changes are additive keys; the physical-lane declaration is a frozen constant, so it cannot make two builds differ. Proven empirically: three independent builds of `e3f3ba2` + overlay — one by hand and two by the A/B driver in separate checkouts — produced the same archive digest `f772fee3f3107d848e662913f2e9d3d8306263fd9db0965434da1bf5ebfb2a6c`, and the pre-edit compiler produced `af2ced7a…` for the same tree, so the only bytes that moved are the ones this package deliberately added.
- Unresolved: (a) **The package does not build at `e3f3ba2` without seven `assets/deployment-profile.json` entries** — five `script_allowlist` and two `asset_allowlist` — so the A/B proof is honestly recorded as being about that commit PLUS a declared overlay whose file digests are in the receipt. Until the parent applies them, freeze criterion 4 is proven-by-construction but not proven-at-HEAD. (b) `assets/runtime-manifest.json` still records no `certified_runtime_code_closure_sha256`; the compiler now emits the exact five fields to record, but recording them is the sealer's act, so freeze criterion 11 is UNBLOCKED, not closed. (c) `--portable-certify` cannot be run end to end until P8.7 produces `evidence/release_v377/`; it is proven at the argv layer, at the tier layer (P8.0's 82 checks) and at the provenance layer, not against a real dossier. (d) `release_package_attestation.mjs` labels a portable package's `release_gate_status` as a development package and applies neither of its two certified-mode assertions to the portable tier — reported, forbidden here. (e) `node scripts/run_ci_census_tests.mjs` is RED on unclassified `run_*` scripts; two of the nine are this package's, seven are concurrent agents'. Same disposition P8.0 and P8.6a recorded. (f) Binding the toolchain makes reproducibility explicitly CONDITIONAL on the toolchain: two builds on different Node or Python versions now differ, and the report says so by name instead of leaving a reader to guess. That was always true; it is now declared. Cross-machine reproducibility is therefore a separate claim this package does not make. (g) 96 of the allowlisted assets remain `declared_only` — shipped and hashed, never proven loaded; untouched here.

## Completion receipt
- **A/B RESULT: BYTE-IDENTICAL.** Two detached clean checkouts of `e3f3ba2` (both at tree `78220a71b9eaa8823ea8f7cffb6440fb8735b708`), same smoke case `0df3cd00…`, 6 seconds per build. Archive A = archive B = `f772fee3f3107d848e662913f2e9d3d8306263fd9db0965434da1bf5ebfb2a6c`, 7,414,784 bytes. Complete-package inventory agrees (`cc12a777…`). Release-package attestation identity agrees (`52467b0a…`). Build-input bindings agree (`205b961b…`) and both validate PASS. `smallest_difference: null`, `findings: []`.
- The smallest-difference report is not a promise about a case that never happens. One byte flipped inside the REAL 7.4 MB archive is reported as: `MEMBER_BYTES`, member `release-manifest.json`, member index 135, offset **811** inside the member, archive offset 1,507,627, byte `56` → `57`, `total_differing_members: 1`, and the JSON path `$.identity.package.runtime_code_closure.sha256` with both digest values quoted.
- package-ab 107 checks, 0 failures. Difference classes proven by construction against archives built by the real `createDeterministicPackageArchive`: one flipped byte (member, exact offset, both byte values, exactly one differing member), an equal-length JSON change (JSON path), a resized JSON member (`MEMBER_SIZE`, still reporting the JSON path), a member present only in A, only in B, a member-order swap with identical bytes, and two buffers that are not archives at all.
- The anti-tautology is enforced against 11 refusals: every spelling of an exclusion option on `archiveDifference` and on `validatePackageInputBindings` throws, and a difference still names its member when the caller asked for none.
- Binding mutations refused: 26. No `inputBindings` block at all; a wrong schema version; a tampered self-hash; a missing self-hash; each of the four required bindings removed (4); a required input declared absent; a contract with no digest; an envelope with no declared version; a binding that does not name itself; a binding with the wrong kind; a named digest disagreeing with the shipped inventory row; a bound asset that does not ship; a toolchain with no digest, no Node version, no Python version, no package versions, or a host PATH where a basename belongs (5); an untyped absence; an absence with a token reason; an absence that still carries a digest.
- Typed absence is real, not theoretical: at `e3f3ba2` in a detached worktree `goldens/` does not exist (it is untracked in-flight work), so the shipped binding records `present: false`, `absence_disposition: TYPED_ABSENCE_NOT_SEALED_AT_THIS_COMMIT`, a reason, and `sha256: null` — and the suite separately proves that a golden tree which DOES exist is bound by content, count and declared version, and that its digest moves when a golden's bytes move.
- A portable build claiming native is refused at the COMPILE layer, by the real compiler: a source tree whose runtime manifest records `certified_package_mode: portable_certified` (assembled from symlinks so the compiler reads this repository's real scripts, references and vendored bytes, with only `assets/runtime-manifest.json` doctored) is refused with the name of the recorded mode and the flag to use instead. Four further argv-layer refusals: `--portable-certify` with `--certify`, with `--development`, with `--skip-smoke`, and without `--certification-evidence`.
- Freeze criterion 11's field is now COMPUTED AND STATED, not yet recorded. A certifying run emits, on stderr and in `manifest.certification.recordingRequired`: `certified_runtime_code_closure_sha256`, `certified_runtime_code_closure_recorded_at`, `certified_package_mode`, `certified_certification_tier`, `certified_runtime_code_closure_file_count` and the per-file `certified_runtime_code_closure_files` map the "changed / added / removed since certification" diagnostic reads. For `e3f3ba2` + overlay the value is `4087fb746603f768de15c20db523e99ca77c9da8e30f87371c41a59df714e265` over 331 closure files.
- Four self-mutations of this package's own code were each caught, so the checks are load-bearing rather than tautological: neutering the exclusion refusal (10 failures), returning `member: null` on a byte difference (4), neutering the typed-absence refusal (1), and dropping the compiler's refusal to ship an unbound package (1).
- Bindings that now travel with the package: `assets/production-contract-v2.json` — digest `b1dcc78f…`, `schema_version` 1, `contract_id` `excel-inflow-production-v2`. `assets/support-envelope-v377.json` — digest `dd13150f8d986187137932542b0d8046e2c58ee2a3d15835a47e3a5da0e78569` (P2.11's revision), `envelope_version` `3.7.7`, `schema_version` `excel-inflow-support-envelope/1.0`, with the record stating explicitly that the declared version and the content digest are separate facts because P2.11 changed the content while deliberately holding the version. Goldens — typed absence. Toolchain — Node `v22.23.2` with V8/ABI/OpenSSL, Python `3.9.6`, `openpyxl 3.1.5`, `fitz 1.26.5`, `PIL 11.3.0`, `numpy 2.0.2`, `jsonschema 4.25.1`, plus `soffice` declared as a host binary the build does not exercise. Also newly on the manifest: `certification.certificationTier`, `certificationPackageMode`, `claimsNativeHostEvidence`, `declaredExclusions`, `physicalLaneTerminalDeclaration`, and `recordingRequired`.
