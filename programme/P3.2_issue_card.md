# Work Package Issue Card

- Work package ID: P3.2
- Title: Forecast completion census scope — every economically-owned cell, owned by an executable producer witness
- Active invariant: The forecast completion census covers EVERY economically-owned cell — including the debt, interest, lease, RCF and acquisition schedule families; ownership is established by an EXECUTABLE PRODUCER WITNESS (the producer that computed the cell, not a method label); the period count derives from the case rather than a hard-wired 3; the parity receipt is VERIFIED after Build, not only sealed before it.
- Release/phase: v3.7.7 / Phase 3
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `b3dd728` + this working tree (NOT committed, per instruction)
- Working tree status: dirty — four files changed by this package (`scripts/lib/forecast_completion_constitution.mjs`, `scripts/lib/forecast_producer_contract.mjs`, `scripts/lib/ownership_census.mjs`, `scripts/run_forecast_completion_tests.mjs`) plus two new files (`scripts/run_completion_census_scope_tests.mjs`, this card). Concurrent agents hold `scripts/lib/forecast_authority.mjs`, `scripts/lib/forecast_ownership_resolver.mjs`, `scripts/lib/economic_ir.mjs` and a new `scripts/lib/release_journal.mjs`.
- Observed failure or discovery question: four proven reds against the census the parity seal binds. (a) SCOPE: on a case with two instruments, an RCF, leases and an enabled acquisition, 75 economically-owned schedule forecast cells existed and **0** appeared in the completion census — `rcf_draw`, `ending_rcf`, `lease_liability`, `lease_interest`, `acquisition_debt`, `gross_interest_expense` and every `instrument.*.ending_balance` were absent, census status PASS. (b) WITNESS: ownership was read off the method label — a `schedule_link` cell whose role named no registered schedule producer was typed `schedule_owned`, a `broker_consensus` cell with no value anywhere was typed `direct_evidence_owned`, a `historical_average` cell with no formula was typed `historical_fallback_owned`; the cells carried no producer field at all and the census read PASS. (c) CARDINALITY: `forecastIndex < 3` at :105 made a fourth declared forecast period invisible (cell_count 9 with 3 periods, 9 with 4), and `run_forecast_completion_tests.mjs:53`'s `cell_count === 18` is a pure function of statement rows — the entire schedule half could vanish without moving it. (d) VERIFICATION: `verifyEconomicStageParity` had **0** production callers; `run_user_flow.mjs` sealed the receipt (1 call at :1911) and never read it again.
- Earliest responsible layer: the completion census itself (`forecast_completion_constitution.mjs`) — a validator that enumerated only the two statements and classified ownership by label; plus the controller's missing post-Build verification call
- Reproduction command: `node scripts/run_completion_census_scope_tests.mjs`
- Focused failing test ID: completion-census-scope (new; 66 checks, 8/8 self-mutations proven caught)
- Allowed files/modules changed: `forecast_completion_constitution.mjs` (schedule-family coverage, producer-witness gate, case-derived period scope with period dates, key injectivity, coverage verifier, post-Build parity verifier + fail-closed assertion), `forecast_producer_contract.mjs` (ADDITIVE witness helpers only — `forecastCellProducerWitness`, `scheduleRegionProducerWitness`, `scheduleProducerForRole`, `INTRINSIC_ROW_HISTORY_OPERATORS`, `SCHEDULE_REGION_WRITERS`; `forecastProducerWitness` behaviour untouched for its two existing callers), `ownership_census.mjs` (`economicRegionDefinitions` exported — one schedule inventory for both censuses, zero behaviour change), `run_forecast_completion_tests.mjs` (cardinality/coverage expectations + two fixture producer corrections, below), the new suite
- Explicitly out of scope (REMAINING, on the work order): the P3.6 identity crosswalk — the census key is still `{section,row_id,forecast_index}` with a period DATE added alongside the index (additive) and injectivity enforced, but no S1–S17 crosswalk; the census cannot verify that the solver ACTUALLY wrote each schedule cell (typed schedule states are minted at `solver.mjs:3055`, i.e. after the pre-Build census — the census witnesses the registered producer that will compute the cell, not the produced number); cash-bucket period quantities are covered only through their statement rows; attaching `formula_spec` / `selected_candidate_id` to authorities at selection time (forecast_candidate_compiler.mjs — FORBIDDEN, in-flight); the controller wiring of the post-Build verifier (reported for the parent to apply, see below)
- Required integration gate: forecast-completion 15, user-flow 14/14, forecast-behaviour 33, internal-outcomes 16, ownership-census 10, etr-receipt-boundary 39, completion-census-scope 66, `node --check` on all touched files — green
- Last known green commit: `b3dd728`
- Rollback plan: revert the four changed files to `b3dd728` and delete the two new files; no artifact, asset or receipt outside the run tree is written by this package
- Attempt count (1-3): 1
- Status: COMPLETE, awaiting the one-line controller wiring (reported, not applied — `run_user_flow.mjs` is outside the allowed set)

## Difference classification
- Intended correction: the census now enumerates the schedule half (debt / interest / lease / RCF / acquisition / leverage-liquidity) from the SAME inventory the ownership census uses; every cell carries a producer witness and an unwitnessed or contradicted claim escalates; the period scope derives from `periods` (else the widest declared authority strip) and each cell binds its period date; duplicate census keys escalate; `verifyEconomicStageParityAfterBuild` / `assertEconomicStageParityAfterBuild` re-verify the sealed receipt against the case Build consumed.
- Regression: none observed. Five real compiled cases (the user-flow fixtures) go 126 → 201 cells with 201/201 executable, 201/201 named producers and status PASS; the post-Build verifier returns PASS on all five.
- LOUD note on the existing suite (13 → 15 checks): the cardinality expectation moved **18 → 84** on its fixture — 18 statement cells (unchanged) plus the 66 schedule cells (22 definitions × 3 periods) the census never enumerated. The fixture also needed two producer corrections, because it previously expressed claims no producer could support: the three `revenue` direct-evidence authorities carried no value anywhere (now `value: 110/120/130`), and the `interest` row claimed `schedule_link` with no semantic role (now `semantic_role: "interest_expense"`, which is a registered producer). Every original assertion is unchanged and still green, including `role_policy_owned === 3`, `captured_by_parent === 3` and `schedule_owned === 3`; two coverage assertions were added. Verified against the real pipeline: all 7 `schedule_link` rows in the compiled case carry registry-bound semantic roles, so the fixture — not the invariant — was the outlier.
- Unresolved: 0 production callers of the post-Build verifier until the wiring line lands; the P3.6 identity crosswalk; solver-side proof that each schedule cell was written.

## Completion receipt
- completion-census-scope 66 → 0. Self-mutation harness: 8/8 caught (schedule half dropped; period count re-hard-wired to 3; witness escalations removed; role-usurpation check removed; injectivity check removed; post-Build census recompile removed; coverage verifier made toothless; kind-contradiction check removed).
- Coverage, fixture of the existing suite: 18 → 84 cells (statement 18; schedule 66 — debt 6, interest 6, lease 6, rcf 21, acquisition 3, leverage_liquidity 24; every family undeclared in that fixture, so all 66 are typed `not_applicable`, never zeroed and never dropped).
- Coverage, real compiled case (`clean-run/model-case.json`): 126 → 201 cells (statement 126; schedule 75 — debt 9, interest 9, lease 6, rcf 24, acquisition 3, leverage_liquidity 24). Witness: 201/201 executable, 201/201 with a named producer, binding sources declared_row_formula 87, declared_schedule_region_writer 48, schedule_role_registry 39, declared_absence 12, instrument_schedule_writer 9, authority_value 6. A case with `acquisition.enabled = 1` types its three acquisition cells `schedule_owned`; `enabled = 0` types them `not_applicable`.
- Escalation vocabulary added (typed `code` on every escalation): `schedule_claim_without_registered_producer`, `unwitnessed_ownership_claim`, `witness_kind_contradicts_disposition`, `schedule_role_usurped_by_direct_claim`, `duplicate_census_key`, `undetermined_forecast_period_scope`, plus codes on the two pre-existing P1.5 ref findings and `no_lawful_disposition` (reason strings unchanged, so the ETR boundary suite's regexes still bind).
- Schema versions bumped honestly: `forecast-completion-census/1.0` → `1.1`, `economic-stage-parity/1.0` → `1.1` (no external pin anywhere in the repo). The parity receipt now also binds `completion_cell_count`, `completion_period_count` and `completion_schedule_cells`.
