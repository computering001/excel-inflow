# P0.1 micro-checkpoint (fresh-model handover paragraph)

On `agent/excel-inflow-v377-behavioural-closure` at its first sealed commit (parent `433bd237b73e15bc844d5cd64175f0758ebe1d9f`, tree `d9c9fa56d3b146251e5dc89625bffee0c9675795`), the invariant "every result ties to one exact source commit, package identity and raw-input hash set" is proven for the baseline itself: a fresh clone checked out at the baseline reproduces the identical tree hash and the identical deterministic archive sha256 (`422fea6b62...`); the pre-existing release-compilation guard (`scripts/run_current_package_source_identity_check.mjs:24`) refuses a dirty worktree with a typed error (verified exit 1); and the closure branch head IS the baseline commit (no synthetic merge). Evidence: `programme/baseline_receipt.json`, `programme/baseline_source_sha256.txt`, read-only archive in private custody. Residual risk: origin-side tag immutability is conventional, but any movement is detectable against the receipt. Next package is P0.2 (immutable blocker replay corpus); its first discovery command is a repository+custody search for existing fixtures covering the six mandatory phase cases before authoring new ones.
