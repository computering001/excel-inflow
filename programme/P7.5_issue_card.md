# Work Package Issue Card

- Work package ID: P7.5
- Title: Mutation adequacy is MEASURED — a computed score over the declared mutation corpus, a generated survivor register, and a zero-survivor gate on the P0 invariants (MEASUREMENT SLICE)
- Active invariant: Mutation adequacy is measured, not asserted: there is a computed mutation score over a declared mutation corpus, every mutation that does not die is registered with an owner and a pointer, and the P0 invariants carry a zero-survivor gate that fails when a mutation survives.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ c26463e (working tree, uncommitted)
- Working tree status: dirty — this slice's five new files plus several unrelated in-flight clusters (P7.7 CI tiers, a user-flow cluster, a release-dossier cluster). None of this package's files overlap them; the registry and the oracle matrix were read only and are modified by OTHER packages, not by this one.
- Observed failure or discovery question: The P7 work order claimed 38 mutation-class suites with "mechanics good" but no score, no survivor register and no gate. Proven red before building: the registry declares 69 `test_class: "mutation"` rows (the work order's 38 is stale); `grep -rn "P0 invariant\|zero.survivor" programme/ assets/ scripts/ ci/` returns exactly ONE hit — the work-order line complaining the gate is missing; no `ci/mutation_survivors.json` and no mutation-score artifact of any kind exist; and a static sweep of the 69 suites' own report lines showed only 10 printing anything resembling a mutation count, the remaining 59 printing `{"status":"PASS","checks":N}` — so a suite that silently stopped mutating anything would still pass. Executing the corpus proved worse than the static read in one direction and better in another: only 24 of 69 suites report a machine-readable mutation population at all.
- Earliest responsible layer: the absent compiler. Each suite privately asserted its own mutations with no consumer, so no layer above the suite could observe the corpus. Nothing was broken; the measurement did not exist.
- Reproduction command: `node scripts/compile_mutation_adequacy.mjs --out ci/mutation_survivors.json` then `node scripts/run_mutation_adequacy_tests.mjs`
- Focused failing test ID: mutation-adequacy (new, 68 checks, 8 adversarial governance mutations on a copy of the real artifact: survivor omitted from the register, stale survivor left standing past its repair, survivor without an owner, P0 gate member with a survivor still claiming PASS, self-fixture kill counted as production coverage, invented population on a suite that reports none, padded production bucket, gate crying FAIL with no P0 survivor)
- Allowed files/modules: NEW scripts/lib/mutation_adequacy.mjs; NEW assets/mutation-adequacy-v1.schema.json; NEW scripts/compile_mutation_adequacy.mjs; NEW ci/mutation_survivors.json (generated only); NEW scripts/run_mutation_adequacy_tests.mjs; NEW programme/P7.5_issue_card.md
- Explicitly out of scope (REMAINING P7.5, on the work order): registering `mutation-adequacy` in assets/development-test-registry.json (forbidden file — flagged for registration); the (e) PR-vs-nightly split, which P7.7 landed independently at c26463e and which this compiler is ready to feed; repairing the ONE registered survivor (cash-fx-identity-workbook) — validators validate, never repair; closing the 38 unreported-population suites by making each print its mutation count (owner-by-owner work, enumerated with remedies in the artifact); the registry-vs-harness argument mismatch on provenance-authority; installing PyMuPDF so prebroker-ownership-controller becomes measurable.
- Required integration gate: mutation-adequacy 68 checks; test-registry contract 985 checks (193 registry tests); programme-control 145 checks; the compiler run over the real registry — all green at c26463e, and the compiler's own score reproduced identically across three consecutive runs.
- Last known green commit: `c26463e`
- Rollback plan: delete the five new files (scripts/lib/mutation_adequacy.mjs, scripts/compile_mutation_adequacy.mjs, scripts/run_mutation_adequacy_tests.mjs, assets/mutation-adequacy-v1.schema.json, ci/mutation_survivors.json) and this card. Nothing existing was modified, so there is nothing to revert.
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: a compiler that walks the registry's mutation-class rows, EXECUTES each suite, reads that suite's own stdout JSON line for a self-reported mutation population, and publishes a score whose denominator is only what was measured — plus a survivor register, a declared P0 zero-survivor gate, and an enumeration of every suite whose population cannot be measured from outside itself. Four structural honesty rules: (1) a suite reporting no count is a GAP with an owner, never a zero and never its `checks` value; (2) a self-fixture mutation is bucketed separately and can never raise `production_mutation_score`, which additionally declares itself an UPPER BOUND because static production reach is necessary, not sufficient; (3) the oracle matrix suite is split by its own per-domain `evidence_scope`/`independence` flags rather than credited whole, so the five domains P7.6a marked NOT_INDEPENDENTLY_PROVEN land in the self-fixture half; (4) a survivor claim must REPRODUCE serially before entering the register, and a suite that never reached its mutations (missing runtime dependency, harness/registry argument mismatch) is UNMEASURABLE, never a fabricated survivor.
- Regression: none. No existing file was touched. No mutation assertion anywhere was lowered — the compiler only reads. Two counting bugs found in the compiler itself were fixed in the conservative direction: same-population field aliases (`mutations` array alongside `mutations_rejected`; `adversarial_mutations` alongside a `mutations` array) are collapsed rather than summed, which UNDER-counts the corpus rather than inflating it. The schema deliberately avoids the `not` keyword, which scripts/lib/json_schema.mjs does not implement, so the owner rules are enforced by an anchored negative-lookahead pattern instead of a silently vacuous constraint (the P1.4 lesson).
- Unresolved: `mutation-adequacy` is not yet a registry row (forbidden file). Measurement coverage is 0.3478 — 38 of 69 suites still keep their mutation population private; each is enumerated with an owner and a remedy, and closing them is the single highest-value follow-on because the score's denominator cannot grow without it. The zero-survivor gate has only two members (one parsed, one derived) because the programme declares no P0-to-mutation-suite binding; six of the seven sealed P0 invariants have no mutation-class prover at all, which the artifact reports as holes rather than hiding.

## Completion receipt
- Red proof: no mutation score, no survivor register, no gate and no P0 mutation binding existed at HEAD — the only repository-wide hit for "zero-survivor" was the work order asking for one.
- ACTUAL COMPUTED SCORE over the real registry at c26463e (portable profile): **mutation_score 0.9919 = 123 killed / 124 measured (1 survived)**. Breakdown: production bucket 120 measured over 66 suites (**production_mutation_score 0.9917, declared UPPER BOUND**); self-fixture bucket 4 measured over 2 suites; mixed oracle-matrix suite 0 measured (custody-BLOCKED portably, declared corpus 17 artifact-scope + 5 synthetic mutations). **measurement_coverage 0.3478 = 24 of 69 suites** — the unflattering headline that sits beside the score.
- Survivor register: 1 OPEN entry — `cash-fx-identity-workbook::suite-failure`, owner release-proof, pointer scripts/run_cash_fx_identity_workbook_tests.mjs, reproduced serially, independently confirmed red in a clean HEAD worktree at c26463e ("Provenance marking contradicts the authority governing the cell"). NOT repaired: validators validate.
- Measurement gaps: 45 — 38 PASSES_WITHOUT_REPORTING_A_MUTATION_COUNT, 4 BLOCKED_ON_CUSTODY_INPUT, 1 ENVIRONMENT_DEPENDENCY_MISSING (prebroker-ownership-controller, PYMUPDF_MISSING at the Stage 4 preflight), 1 HARNESS_INVOCATION_CONTRACT_MISMATCH (provenance-authority requires `--out DIR`; the registry declares a bare positional), 1 REPORT_LINE_IS_NOT_JSON_PYTHON_DICT_REPR (run_source_arithmetic_precision_tests.py prints a Python dict repr, so its "mutations: 4" is unreadable by any external auditor).
- P0 gate: PASS_WITH_UNPROVEN_MEMBERS over 2 members. `release-identity-governance` parsed from programme/P0.3_issue_card.md (7 mutations, all killed); `emitted-candidate-independent-oracle` derived from the oracle matrix's artifact-scope domains and BLOCKED on custody in the portable profile — reported UNPROVEN, never converted to PASS. Six sealed P0 invariants (P0.1, P0.2, P0.4, P0.5, P0.6, P0.7) have no mutation-class prover and are published as holes.
- Suite: 68 checks, all 8 adversarial governance mutations caught, plus a negative self-test proving the audit is not vacuously green.
- Gates: mutation-adequacy 68; test-registry contract 985 (193 tests); programme-control 145.
