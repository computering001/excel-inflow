# Fresh-model handover — 2026-08-19 (session 2 close)

- **Branch/commit**: `agent/excel-inflow-v377-behavioural-closure` @ `9c007eb` (pushed; working tree clean at write time).
- **Active invariant**: none mid-flight — every started package is sealed or sliced-and-sealed with its issue card. The next invariant is P0.8's phase seal, blocked ONLY on the gate run at `9c007eb` concluding (its three predecessor "cancelled" runs were the package job's 35-minute timeout, now raised to 90 — every other job was green).
- **Completed this session**: Phase 0 P0.1–P0.7 sealed; PHASE 1 SEALED (P1.1–P1.8, two declared core-only); P2.6 + P2.8 + P3.7 slices sealed; live wires: support-envelope preflight (typed early stops), ETR receipt boundary, identifier tiers, oneOf/anyOf enforcement, coercion ban, dual-read adapter, typed internal outcomes.
- **Failing/pending command**: `gh run list --limit 1` until the 9c007eb run concludes; on SUCCESS record it in programme/P0.3_ci_run_receipts.json (closure_head_green), flip PHASE0_checkpoint.md to SEALED, write P0.8_issue_card.md, update index.json, commit "P0.8: Phase 0 sealed".
- **Uncommitted changes**: none.
- **Next smallest actions after P0.8** (from programme/gap-reports/PHASE2-5_WORK_ORDERS.md): (1) P3.5 capture single-writer — route row_plan `markForecastCapturedBy`, case_compiler `markCompilerCapture` and the candidate-compiler restore through `capture_transition.mjs`; NOTE the ownership-census suite PINS the multi-writer finding as expected text and must be updated in the same commit; (2) P2.3 classification-resolution ledger mirroring forecast_authority_ledger; (3) P4.2 opening-debt bridge schema+artifact. Sequencing: P3.6 → P3.1/P3.2/P4.6; P4.5 → P4.4 → P4.7 → P4.8.
- **Prohibited actions**: never json.dump-rewrite registry/schema assets (surgical inserts only, then rebind the oracle-matrix registry pin + regenerate audit/v375-governance census via compile_test_registry_census.mjs); never weaken validators; never regenerate goldens to pass; no tree edits while a raw canary runs; every new suite → registry entry + issue card (+ "## Completion receipt") or the programme-control/ci-census linters go red; do not decompose the four giant modules before v3.7.7 freezes; Rogo/native-Excel items stay excluded per user directive.
- **Environment**: venv python /Users/archiepreston/Documents/Codex/excel-inflow-venv/bin/python; soffice /Applications/LibreOffice.app/Contents/MacOS/soffice; custody root /Users/archiepreston/Documents/Codex/private-test-custody (corpus runner takes --custody-root or EXCEL_INFLOW_CUSTODY_ROOT); CI triggers only on PRs (draft PR #13) — pushes to the branch alone start nothing; cancel superseded runs to free runners.
- **Rollback**: tag `excel-inflow-v376-evidence-baseline`; every package is an atomic commit.
