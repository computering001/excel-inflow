# Work Package Issue Card

- Work package ID: P8.2a
- Title: Closure convergence — one runtime-code-closure definition, active closure made load-bearing (CLOSURE-CONVERGENCE SLICE, items a+b)
- Active invariant: The runtime code closure has exactly ONE membership definition (`runtimeCodeClosureMembers`, scripts/lib/source_identity.mjs), consumed by both the release compiler and the runtime-isolation validator; every run-start identity resolution computes the active closure AND asserts it against the declared/pinned package closure when one is present, recording `match` or `development_unpinned` — a pinned package whose live bytes moved cannot start.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`, HEAD `6b76c0d` (uncommitted working-tree slice; other agents mid-flight)
- Working tree status: dirty (deliberate — multi-agent programme; this slice is not committed by its agent)
- Observed failure or discovery question: TWO independent runtime-closure definitions existed — compile_skill_release.mjs:1094-1128 (computed closure ∪ declared assets ∪ resources ∪ vendored trio) and runtime_isolation.mjs:779-818 (declared inventory minus SKILL.md/references, with deployment-profile.json force-included regardless of allowlist) — with no test they agree; and `active_runtime_code_closure` (source_identity.mjs:205-222) was computed and consumed by NOTHING, so no run could prove its live bytes match the package identity it claims.
- Earliest responsible layer: the closure identity definition itself (source_identity.mjs) — packaging and run-start validation are both consumers.
- Reproduction command: `node scripts/run_runtime_closure_tests.mjs`
- Focused failing test ID: runtime-closure (22 checks; RED pre-fix — a divergence probe fixture whose asset_allowlist omits deployment-profile.json produced onlyA=[assets/deployment-profile.json]: the validator's closure contained a member the compiler's definition never had; real-tree agreement was allowlist coincidence guarded by nothing)
- Allowed files/modules: scripts/lib/source_identity.mjs (the single definition `runtimeCodeClosureMembers`, `checkActiveRuntimeCodeClosure`, declared-pin source tracking in resolveSourceIdentity, load-bearing assertion in resolveActiveSourceIdentity), scripts/lib/runtime_isolation.mjs (closure block consumes the shared definition), scripts/compile_skill_release.mjs (closure block consumes the shared definition + one import), NEW scripts/run_runtime_closure_tests.mjs, this card
- Explicitly out of scope (REMAINING P8.2, on the work order): (c) assertCertifiedProductionIdentity production caller, (d) publication.json input/IR hashes, (e) workbook BUILD IDENTITY panel, (f) custody-receipt verification CLI, (g) release_identity five-writer ownership convergence; recording the check into the controllers' run summaries (one-line wiring reported to the programme owner — controllers owned by another agent)
- Required integration gate: runtime-closure 22, source-identity 22, runtime-version-contract 10, release-identity-governance PASS, identity-vocabulary 20, carrier-product-identity 38, release-package-attestation 33 — green; development compile with production smoke case binds a manifest closure sha equal to the live computeDeclaredRuntimeIntegrity closure sha
- Last known green commit: `6b76c0d` (pre-slice; suites above green there minus the new suite)
- Rollback plan: revert the three touched files and delete the new suite + this card (no schema, profile, or controller changes; closure content byte-identical by construction)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked on the P8 work order)

## Difference classification
- Intended correction: one closure definition where there were two; `active_runtime_code_closure` now consumed — resolveActiveSourceIdentity fails closed when a declared/pinned closure does not match live bytes and records `development_unpinned` honestly when nothing is pinned. `resolveSourceIdentity` additionally reports `runtime_code_closure_sha256_source` (which authority declared the pin, or `computed_live`).
- Regression: none — closure CONTENT byte-identical: OLD definition (transcribed) vs NEW shared definition on the same tree both yield sha256 `4569fcfdf45eaa087e3aa4d403ef6386e2f4f0c379bbe2906a685bb38ae78a94` over 324 files; this source checkout pins nothing, so all existing runs resolve exactly as before (`development_unpinned`).
- Unresolved: freeze criterion 6's "…and at delivery" half plus run-summary recording needs the controller owner's one-line wiring (`active_runtime_code_closure_check` is already on every resolved active identity).

## Completion receipt
- runtime-closure 22 → 0: single-definition membership semantics (vendored trio, instruction exclusion, portability, duplicate refusal), validator/compiler convergence on convergent AND historically-divergent profiles, divergence-mutation guard (membership mutation moves definition and validator together), structural pins that both consumers import the one definition and carry no inline rule, unpinned/pinned/mutated run-start behavior end to end, real-tree identity check.
- The former divergence (deployment-profile.json force-inclusion vs allowlist-only) is now decided once, by the compiler's rule; the full declared-runtime-integrity inventory still covers deployment-profile.json (validator not weakened).
- Development compile inside the package now proves the invariant live: the packaged release-manifest pins the closure sha and every packaged entry point that resolves active identity asserts live bytes against it.
