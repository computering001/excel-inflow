# Work Package Issue Card

- Work package ID: P4.4a
- Title: Complete the movement set of the release-grade debt roll-forward invariant (DEFECT D10)
- Active invariant: The expected ending balance of an instrument is opening plus EVERY movement that lawfully changes it — issuance, fair-value movement, other non-cash movement and PIK accretion in, scheduled amortisation and maturity repayment out. A movement the solver can produce but the invariant cannot see is not a tolerance question; it is a missing term.
- Release/phase: v3.7.7 / Phase 4
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `d3a3266` (uncommitted working tree; NOT committed by this package)
- Working tree status: dirty — concurrent packages hold case_compiler.mjs, statement_classifier.mjs, instrument_period_state.mjs, opening_instrument_provenance.mjs, model_ir_v3.mjs and the presentation catalogue. This package's own contribution is exactly two files: `scripts/lib/validation_invariants.mjs` (+18/-1, invariant region only) and the new `scripts/run_roll_forward_invariant_tests.mjs`.
- Observed failure or discovery question: The P7.1b archetype agent proved `validateSolutionInvariants` / `debt.instrument_roll_forward` computed expected ending as `opening + issuance + other_non_cash - amortisation - maturity`, omitting `pik_interest_native` and `fair_value_movement_native`. Every PIK or accreting instrument therefore failed a RELEASE-GRADE invariant by exactly its accretion while the solver's own roll-forward was correct. Unexercised on the certified cohort because neither fixture carries `pik_rate > 0`. Reproduced here: `pik_only_debt` 3 violations of 25 / 27.083 / 29.340; `zero_coupon_accreting_to_par` 2 violations.
- Earliest responsible layer: the invariant's expected-ending identity in `scripts/lib/validation_invariants.mjs` (`validateSolutionInvariants`, the `debt.instrument_roll_forward` block). The solver is NOT at fault — verified independently, not assumed.
- Reproduction command: `node scripts/run_roll_forward_invariant_tests.mjs`
- Focused failing test ID: roll-forward-invariant (120 checks: two archetype reproductions, independent solver agreement, a material-break control, a wrong-PIK-amount control in both directions, a seven-term load-bearing mutation sweep, and a certified-fixture regression pin)
- Allowed files/modules: `scripts/lib/validation_invariants.mjs` (the `debt.instrument_roll_forward` expected-ending identity ONLY — the file's semantic-artifact validation is untouched and unrestructured), the new suite, this card
- Explicitly out of scope (REPORTED, not edited — forbidden to this package): the two DEFECT-marked archetype expectations in `assets/archetype-catalogue-economics-v1.json` and their bound checks in `scripts/run_archetype_economics_tests.mjs`, which still encode the DEFECTIVE behaviour as the expectation. Exact replacement text is in the handover report; prototyped in a detached worktree, where the suite returns to `{"status":"PASS","checks":598,"archetypes":33}` — the headline figure is preserved, no check is lost.
- Required integration gate: layered-graph constitution (0 violations, 8 adversarial mutations caught), user-flow 14/14, acquisition-solver, funded-acquisition 16, economic-IR 79, roll-forward-invariant 120, `node --check` on both touched files — all green. Archetype economics 598 is RED on the two out-of-scope expectations above and on nothing else (proved by prototype).
- Last known green commit: `d3a3266`
- Rollback plan: revert the seven-term identity in `validateSolutionInvariants` to the five-term form and delete the suite. Nothing else consumes the change; the certified cohort's invariant output is unchanged either way (all five added/retained terms are zero there).
- Attempt count (1-3): 1
- Status: COMPLETE, uncommitted — pending the out-of-scope catalogue/runner expectation update by their owner

## Difference classification
- Intended correction: two absent terms added to the expected-ending identity. STRICTLY STRENGTHENING — no tolerance widened (the shipped 1e-8 default is unchanged and the suite passes it explicitly), no instrument skipped, no check made conditional. The invariant now also catches a wrong PIK or fair-value amount, which it was structurally blind to before.
- Regression: none. Certified cohort output is byte-identical (no instrument carries PIK, fair-value or other non-cash movement, so both added terms are zero). `standard-maximal-v2`'s one pre-existing unrelated finding (`acquisition.direct_cash_flow_forbidden`) is present identically before and after, and is now pinned by the suite rather than hidden by it.
- Unresolved: the two archetype expectations named above still assert the defective behaviour, so `run_archetype_economics_tests.mjs` is red until their owner applies the reported text. D11–D19 are separate packages.

## Completion receipt
- roll-forward-invariant 120 → 0. Red first: all seven mutation-sweep discrepancy assertions, both archetype reproductions and the wrong-PIK control failed against the shipped identity before the change.
- Solver agreement verified independently, not taken on trust: `solver.mjs:3144` `debt_roll_forward` already used all seven terms, `solver.mjs:2129` builds `ending_native` from the same set, and the suite recomputes the identity itself from the emitted fields on both archetypes. The register's claim — solver correct, invariant wrong — holds.
- Teeth proved by mutation, not by assertion: dropping any ONE of the seven terms from the repaired identity turns the suite red (7/7); so does skipping PIK-bearing instruments; so does a 1e-3 relative tolerance. A 1e-7 ending-balance break — ten times tolerance — still fails.
