# Work Package Issue Card

- Work package ID: P6.8
- Title: One runtime SLO declaration, evidenced by a cold/warm cohort with percentile aggregation and a hard-ceiling enforcement proof (PHASE-6 RUNTIME SLO SEAL)
- Active invariant: The runtime SLO is ONE declaration, bound by name to the constants the runtime actually enforces, and it is EVIDENCED — a cold/warm cohort measured on the real controller and aggregated to p50/p95, plus a hard-ceiling enforcement proof that FAILS when the ceiling is breached.
- Release/phase: v3.7.7 / Phase 6
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `1d0177d` (worktree `/Users/archiepreston/Documents/Codex/excel-inflow-v377`)
- Working tree status: clean at open. At write time this package holds exactly four paths and nothing else: `assets/performance-policy-v1.json` (modified), `scripts/lib/runtime_slo.mjs`, `scripts/run_runtime_slo_tests.mjs`, `evidence/phase_6/` (new). No commit was made.
- Observed failure or discovery question: FOUR reds, each measured at HEAD before the repair. **(1) Three mutually contradictory SLO declarations.** `assets/performance-policy-v1.json#service_objectives_minutes` said `standard_p50` 35 minutes / `standard_p95` 45 minutes / `no_broker_p95` 25 minutes; `scripts/lib/runtime_budget_policy.mjs:16-17` said `end_to_end_target` 900 000 ms (15 min) / `end_to_end_hard_ceiling` 1 500 000 ms (25 min); the execution pack's P6.8 row said normal < 15 / P95 < 20 / ceiling 25. The asset's pair is not a rival opinion but UNSATISFIABLE — 35 × 60 000 = 2 100 000 > 1 500 000 and 45 × 60 000 = 2 700 000 > 1 500 000, so a run meeting that "objective" would already have exhausted the only clock it has. **(2) No percentile aggregation anywhere:** `grep -rn "function percentile|nearest_rank|quantile" scripts/` returned zero hits, so no runtime claim in the repository was ever a distribution. **(3) No cold/warm cohort:** `grep -rln "cold_p50|warm_p95" scripts/` returned zero hits — and the obvious construction is WRONG, because P6.1's clock is deliberately cumulative, so reading `compute_elapsed_ms` off a warm ledger reports the warm run as SLOWER than the cold run that seeded it (measured on the real controller: cold 2820 ms, warm ledger total 4533 ms, warm invocation 1713 ms). **(4) No hard-ceiling enforcement test:** `run_hard_clock_tests` bounds the floor, but nothing ever stated the resulting envelope as a number, checked a real run's ledger against it, or failed on a breach. The GAP's fifth item — "P6.6's receipt structurally cannot pass warm" — was CHECKED and no longer holds: P6.6's lawful-zero rule makes a fully warm 15-checkpoint resume PASS with 0 MISSING and ratio 1, and `run_performance_receipt_honesty_tests` is 50/0 against this tree. The `programme/checkpoints/` half of the GAP was likewise already closed by the checkpoint generator and its programme-control linter.
- Earliest responsible layer: the DECLARATION layer (`assets/performance-policy-v1.json`), not the enforcement layer. The constants in `runtime_budget_policy.mjs` were right; what was missing was a single declaration that owns them, a percentile vocabulary, and a measurement discipline. No enforcement code was changed.
- Reproduction command: `node scripts/run_runtime_slo_tests.mjs`
- Focused failing test ID: runtime-slo (NEW, `scripts/run_runtime_slo_tests.mjs`, 130 checks). Proven red six independent ways against this repair — see the Completion receipt.
- Allowed files/modules: `assets/performance-policy-v1.json` (the single declaration: `runtime_slo` added, `service_objectives_minutes` removed and recorded in `supersedes`; `trace` and `release_policy` byte-identical so P6.6's threshold reads are unchanged); NEW `scripts/lib/runtime_slo.mjs` (declaration validator, percentile engine, cohort compiler, hard-ceiling enforcement report, seal compiler); NEW `scripts/run_runtime_slo_tests.mjs`; NEW `evidence/phase_6/**`; NEW this card. `scripts/lib/experience_trace.mjs` was NOT changed: it already loads and digests the asset, so the new module reads the policy THROUGH it rather than opening a second reader.
- Explicitly out of scope, reported for their owners instead: `assets/development-test-registry.json` — the row that puts this suite in CI (`{"id":"runtime-slo","phase":"performance","runtime":"node","script":"run_runtime_slo_tests.mjs","timeout_seconds":600,"mutates_product_tree":false,"test_class":"mutation"}`) and `ci/**` — the census regeneration; both FORBIDDEN here, so `run_ci_census_tests` is red on exactly one unclassified script and green with this suite removed. `assets/deployment-profile.json` — the `performance-policy-v1.json` description at :238 still reads "Canonical v3.6 timing classification and promotion policy, including external-wait classification and 30/120/300 second unknown-gap thresholds" and no longer mentions that the file now carries the single runtime SLO; `lib/runtime_slo.mjs` needs a `script_allowlist` entry only if a shipped runtime path ever imports it (nothing does today, and no closure suite asks for it). `scripts/experience_trace.py:10-12` still hardcodes the trace bands (P6.6's carry-over, not an allowed file here). Also out of scope: an `end_to_end_delivery` cohort (not producible at this commit — `evidence/phase_6/certifying-scope-unavailable.json`), span-offset gap localisation (P6.6's carry-over), and any change to `runtime_budget_policy.mjs`/`run_deadline.mjs`, which needed none.
- Required integration gate: this suite 130; `run_performance_receipt_honesty_tests` 50 (9 mutations); `run_hard_clock_tests` 113 (11 mutations); `run_experience_trace_leaf_tests` 7; `run_user_flow_tests` 14/14; `run_evidence_work_graph_tests` 311; `run_differential_invalidation_tests` 371; `run_performance_receipt_tests`, `run_runtime_budget_policy_tests`, `run_run_deadline_tests` 17, `run_test_registry_contract_tests` 1050, `run_stage_recipe_tests` 124, `run_runtime_closure_tests` 22, `run_ownership_census_tests` 10, `run_programme_control_tests` 422; `node --check` on both new scripts and a JSON parse of the asset — all green. `run_ci_census_tests` RED, attributed (see Unresolved).
- Last known green commit: `1d0177d`
- Rollback plan: revert `assets/performance-policy-v1.json` and delete the two new scripts, `evidence/phase_6/` and this card. Nothing in the shipped runtime imports the new module and the asset's `trace`/`release_policy` sections were not touched, so reverting restores the prior state exactly and no caller has to change.
- Attempt count (1-3): 1
- Status: COMPLETE, unsealed (no commit; CI registration owed at seal)

## Difference classification

- Intended correction:
  - **One declaration, and the asset is its home.** `assets/performance-policy-v1.json#runtime_slo` declares p50 900 000 ms, p95 1 200 000 ms, hard ceiling 1 500 000 ms. The asset was chosen because P6.6 had already made it a live policy input with a digest, and because a declaration should not live inside the module that enforces it. **Why the other two were wrong:** the asset's own 35/45 pair was UNSATISFIABLE against the enforced 25-minute ceiling, which makes it stale v3.6 text (`assets/deployment-profile.json` still describes the file as the canonical v3.6 timing policy) rather than a competing objective; and `no_broker_p95` = 25 min merely restated the hard ceiling as an objective, which would make a subset path slower than the whole-run p95. The budget policy's 15/25 were CORRECT but incomplete: one target and one ceiling have no vocabulary for a tail, which is why the pack's p95 was floating with no home. Nothing was averaged and nothing was loosened: the two enforced numbers are carried over unchanged and the only number added is the p95, which sits strictly inside the ceiling.
  - **The declaration is BOUND, not merely adjacent.** `validateRuntimeSloDeclaration` refuses unless `end_to_end_ms.p50 === DEFAULT_RUNTIME_BUDGETS_MS[bound_runtime_budget_keys.p50]` and the same for the ceiling, and unless `enforced_envelope.bounded_floor_allowance_ms === run_deadline.mjs#STAGE_FLOOR_TOTAL_ALLOWANCE_MS`. It also refuses any objective not ordered strictly inside its ceiling, so RED 1's shape can never be re-declared, and it refuses the whole document if `service_objectives_minutes`, `standard_p50`, `standard_p95` or `no_broker_p95` reappears at ANY depth — single has to stay single.
  - **The enforced envelope is declared instead of hidden.** P6.1 bounded the stage floor rather than removing it, so a run may be granted the ceiling PLUS a finite 360 000 ms floor allowance: 1 860 000 ms, 31 minutes. Leaving that undeclared would have created a fourth, silent declaration. It is now in the asset, derived and checked, together with `in_process_preemption: false` — the runtime does not preempt work running inside the controller process, and the declaration says so rather than implying a kill it does not perform.
  - **A sample is a measurement or it is nothing.** `runtimeSloSample` cannot be handed a duration. It takes a real persisted `run-deadline.json`, runs it through P6.1's own `validateRunDeadlineLedger` (with the prior reading as `priorLedger`), and computes an INVOCATION DELTA that must reconcile against the segments that invocation appended. The cohort label is structural: COLD requires no prior ledger and no reused stages, WARM requires both — so a warm cohort cannot be fabricated and a cohort missing one side cannot be dressed up as one.
  - **Percentiles refuse rather than mislead.** Nearest-rank inclusive, declared in the asset, and `percentileMs` refuses below the declared minimum sample. Below 20 samples the p95 is stamped `CONSERVATIVE_UPPER_BOUND` (nearest rank lands on the slowest observation), which is an over-statement of the tail and never an under-statement.
  - **A subset scope cannot certify.** Every sample declares its scope; the report carries `slo_coverage` `CERTIFYING` only when every sample ran the delivered path, and `LOWER_BOUND_ONLY` otherwise, with a sentence saying such percentiles can refute the objective but never certify it.
  - **Hard-ceiling enforcement is a report over a real ledger, and it fails on breach.** Five properties, all computed from the ledger the run persisted: no grant beyond remaining + the floor allowance still available at that moment; floor debt inside the run's finite allowance; grants never zero and never a reissued floor; every past-ceiling grant carrying the registered `INTERNAL.runtime_budget_overrun`; and every millisecond past the ceiling DISCLOSED by a typed receipt, because in-process work is not preempted. Seven mutations of a real ledger are proven to flip the report to `NOT_ENFORCED`.
  - **Validators validate.** Nothing in this package repairs a declaration, adjusts a measurement or relaxes an objective. `compileRuntimeSloCohortReport` REFUSES a cohort it cannot support; it never reports a weaker claim instead.
- Choice on a cohort that exceeds the objective — a recorded FINDING, never a moved objective: the cohort report's `status` becomes `SLO_EXCEEDED`, `breaches[]` names the cohort and the percentile, and the seal's `seal_status` becomes `SEALED_WITH_SLO_FINDING`. The declaration is not touched. `validateRuntimeSloCohortReport` recomputes the aggregation from the samples, so a `WITHIN_SLO` verdict stamped over a breaching cohort is rejected even after the report is re-hashed — the suite pins that mutation.
- Regression: none observed. `assets/performance-policy-v1.json`'s `trace` and `release_policy` sections are byte-identical, so P6.6's thresholds and `run_experience_trace_leaf_tests` are unaffected; the asset's sha256 changes and every consumer derives it at load, so nothing pinned a stale digest. No file owned by P6.1, P6.3, P6.4, P6.5, P6.6 or P6.7 was edited. The exit inventory, the controllers and the terminal-reason registry were not touched.
- Unresolved:
  - `run_ci_census_tests` is RED with exactly one message — `unclassified run_* scripts ... run_runtime_slo_tests.mjs`. Attributed by removing the file and re-running: `{"status":"PASS","checks":431,"scripts":236}`. Registration needs `assets/development-test-registry.json` and `ci/**`, both FORBIDDEN here, so it is owed by the seal commit (the same disposition P6.6, P8.0, P8.1 and P8.6a recorded).
  - **The cohort is a LOWER BOUND, not a certification.** The delivered path does not complete at this commit, so the cohort's scope is `evidence_half_through_decisions`. Two probes are captured verbatim in `evidence/phase_6/certifying-scope-unavailable.json`: with a full Python toolchain and LibreOffice present, the real controller reaches stage 4 and stops at its FIRST checkpoint — gate N8 fails on the acquisition test fixture (`test-fixtures/**` is out of scope here); and one layer down, P8.7 already recorded that `build_dynamic_model.mjs:784` refuses any compiled production case at HEAD, localised to `bd1b973`. `runtime_slo.mjs` already accepts `end_to_end_delivery` and will flip the report to `CERTIFYING` with no code change once a workbook can be built.
  - The p95 of each cohort is computed from five samples, which nearest-rank resolves to the slowest observation. That is declared in the report as a conservative upper bound; a twenty-sample cohort would give a real p95 and costs only wall-clock time.
  - The ceiling is enforceable on GRANTS and terminal at spawn boundaries, but in-process stage overruns are charged and typed rather than interrupted. Preempting in-process work is a controller change and belongs to whoever owns the controllers, not here.

## Completion receipt

- runtime-slo 130 checks / 0 violations. Six independent RED demonstrations captured against the repair: (1) the superseded `service_objectives_minutes` reintroduced into the asset → `rival_declaration:service_objectives_minutes, rival_declaration:standard_p50, rival_declaration:standard_p95`; (2) the p95 set back to 45 minutes → `objective_not_ordered_inside_ceiling`; (3) the declared floor allowance unbound from `STAGE_FLOOR_TOTAL_ALLOWANCE_MS` → `envelope_floor_allowance`; (4) `supersedes` emptied → `supersedes_unrecorded`; (5) the sample switched from the invocation delta to the cumulative clock → "MEASURED FINDING: warm p95 4986 ms exceeds cold p95 3104 ms — enacted reuse made the run slower", i.e. the exact false finding this package exists to prevent; (6) the floor-debt breach check removed → "a breach was accepted — floor debt beyond the run's finite allowance"; and the percentile minimum-sample refusal removed → "a p95 computed from fewer than the declared minimum sample was allowed".
- **The measured cohort**, ten real invocations of `run_user_flow`, scope `evidence_half_through_decisions`, measured from the ledgers those runs persisted: COLD n=5, p50 **2856 ms**, p95 **2901 ms** (min 2843, max 2901); WARM n=5, p50 **1742 ms**, p95 **1774 ms** (min 1737, max 1774). Enacted reuse saves 1114 ms at the median and 1127 ms at the p95 — 39%. Status `WITHIN_SLO`, coverage `LOWER_BOUND_ONLY`, zero breaches. Both cohorts sit three orders of magnitude inside the 900 000 / 1 200 000 / 1 500 000 ms declaration; that is a lower bound on a subset path and is reported as such, not as a certification.
- **The hard ceiling is genuinely enforceable, and it was proven on the real controller.** A run driven against a real sealed 2800 ms ceiling overshoots by 86 ms and DISCLOSES it with three typed `INTERNAL.runtime_budget_overrun` receipts; a second invocation of the same run starts with the ceiling already spent, consults at zero remaining three times and draws 1200 ms of BOUNDED floor debt with three `deadline_exceeded` receipts; and with the floor allowance spent, the real controller's next three grants collapse to **1 ms each** — a hard refusal at every spawn boundary. All eight measured ledgers report `ENFORCED`; seven ledger mutations report `NOT_ENFORCED`.
- **No constant in P6.1's files must change.** `end_to_end_target` 900 000 and `end_to_end_hard_ceiling` 1 500 000 are exactly what the single declaration binds to, and `STAGE_FLOOR_TOTAL_ALLOWANCE_MS` 360 000 is exactly what its envelope declares. The only file that had to move was the one carrying the unsatisfiable numbers.
- Phase evidence sealed at `evidence/phase_6/`: `runtime-slo-declaration.json`, `runtime-slo-cohort.json`, `hard-ceiling-enforcement.json`, `certifying-scope-unavailable.json`, `phase6-runtime-slo-seal.json` (`seal_status: SEALED`, `hard_ceiling_enforced: true`) and a README. Every file is derived — `node scripts/run_runtime_slo_tests.mjs --seal evidence/phase_6` is its only writer.
