# Work Package Issue Card

- Work package ID: P6.1
- Title: One persisted, monotonic, run-identified hard runtime clock (HARD CLOCK SLICE)
- Active invariant: There is ONE persisted, monotonic runtime clock. A resume, a new turn or a retry can never restore budget that was already spent; every stage consults the same remaining budget; a killed or thrown run still records what it consumed; and the ledger identifies the run it belongs to, so a stale or foreign ledger cannot be adopted.
- Release/phase: v3.7.7 / Phase 6
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: NOT quiescent at seal — concurrent packages hold `scripts/lib/layered_graph_constitution.mjs` (+810 lines), `scripts/lib/semantic_graph.mjs`, `scripts/compile_skill_release.mjs`, plus P6.7's untracked runtime doctor. None of them are P6.1 files.
- Observed failure or discovery question: TWO clocks. `scripts/lib/run_deadline.mjs` was the persisted one but was opened at ONE site and consulted at ONE stage boundary (`run_user_flow.mjs` workbook_build_outer); stages 1/2/3/5 never checked the budget. `run_excel_inflow_vnext.mjs` sized every budget with `remainingRuntimeMs({controllerStartedEpochMs: ACTIVE_PROGRESS_STARTED_EPOCH_MS})` — the PROCESS start — so every new invocation silently restored the full 25-minute ceiling. The ledger carried no run_id/version/commit/policy digest, so a foreign ledger in a reused run directory was adopted verbatim. A killed or thrown run recorded no segment. `DEFAULT_RUNTIME_BUDGETS_MS` summed past the ceiling and was never derived from the remaining deadline. `INTERNAL.runtime_budget_overrun` had zero producers. `STAGE_FLOOR_MS` minted a fresh 60 s on every call past an exhausted ceiling. Durations were `Date.now()` only, with no skew detection.
- Earliest responsible layer: runtime_governance (`scripts/lib/run_deadline.mjs`, `scripts/lib/runtime_budget_policy.mjs`) and the two controllers that consulted it
- Reproduction command: `node scripts/run_hard_clock_tests.mjs`
- Focused failing test ID: hard-runtime-clock (new suite; 113 checks incl. 11 rejected ledger mutations, an END-TO-END double invocation of the real `run_user_flow` controller proving the ceiling no longer resets, a genuine SIGKILL of a registered invocation, and a foreign-ledger quarantine driven through the real controller)
- Allowed files/modules: `scripts/lib/run_deadline.mjs` (schema /1.1: run identity, monotonic spans, abandoned-invocation sweep, bounded floor debt, merge-before-append, `validateRunDeadlineLedger`), `scripts/lib/runtime_budget_policy.mjs` (`consumedComputeMs` clock input + non-finite refusal, `USER_FLOW_STAGE_BUDGET_KEYS`, `MANDATORY_SEQUENTIAL_BUDGET_KEYS`, `deriveStageBudgetMs`, mandatory-path ceiling check), `scripts/run_user_flow.mjs` (deadline/budget sites only: shared ledger path, identity bind, five stage consults, thrown-run charge), `scripts/run_excel_inflow_vnext.mjs` (clock/budget sites only: persisted remaining, ledger handed to delegates, terminal charge), new `scripts/run_hard_clock_tests.mjs`, this card
- Explicitly out of scope (REMAINING P6.1, on the work order): deadline receipts are written but still have no READER outside the validator; `run_run_deadline_tests.mjs:101` still proves wiring by regexing controller source (left untouched — not an allowed file, and never weakened); the corrupt-ledger rebuild still restarts at 0 because `run_run_deadline_tests` pins that behaviour; SIGTERM/SIGINT handlers were deliberately NOT added (they would race the process-tree kill discipline) — a killed run is charged by the next open's sweep instead
- Required integration gate: user-flow 14/14, controller-exit-inventory 64 (9 public + 18 delegate exits unchanged), terminal-crash-path 25, performance-receipt-honesty 50, run-deadline 17, runtime-budget-policy 29 (9 mutations), phase5-operational-runtime 11 (2 mutations) — all green; stage4-content-addressed-resume RED on a non-quiescent tree, red IDENTICALLY with all four P6.1 files reverted to HEAD (see Difference classification)
- Last known green commit: `e31b53e`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction (all TIGHTENING; each proved):
  - remaining budget derived from the PERSISTED ledger, never from a process start (removes the whole-ceiling restore per invocation)
  - persisted ceiling ratchets DOWN only: a resume arriving with a larger policy is refused with a typed receipt
  - foreign ledger quarantined, not adopted; its recorded compute is carried forward as SPENT
  - a thrown run charges its consumption; a SIGKILLed run is charged by the next open's abandoned-invocation sweep
  - all five user stages consult the one clock; every stage segment must have an allowance record
  - stage floor past an exhausted ceiling drawn from a FINITE run-wide allowance (6 × 60 s) instead of a fresh 60 s per call; a floor may never exceed the budget the caller stated
  - durations charged at MAX(monotonic, wall); a clock step cannot create budget
  - mandatory sequential stage budgets must fit inside the hard ceiling (throws otherwise)
  - a budget that cannot be computed REFUSES with a typed `INTERNAL.compiler_or_graph_defect` outcome instead of returning NaN into a subprocess spawn
- Loosening: NONE. No change can let a run consume more total compute than the ceiling plus the bounded, ledger-recorded floor allowance, which is strictly smaller than the previously unbounded per-call floor.
- Regression: none attributable. `stage4-content-addressed-resume` is red on `semantic-artifact-completeness(56)` — a `manifest.layered_graph_constitution.closure_hash` mismatch plus node_mismatch rows whose `actual` carries the new `binding_disposition`/`bound_section`/`binding_status` fields introduced by a CONCURRENT package's +810-line edit to `layered_graph_constitution.mjs`. Proof of non-attribution: with all four P6.1 files reverted to HEAD the suite fails on the identical check with the identical 56 violations. The suite's kill half — forced 25 s mid-build interruption, BLOCKED at build_checks, stages 1-3 retained, `semantic_gates`/`plan`/`emit` receipts intact, no premature `recalculate` receipt — passed WITH the bounded floor; the ledger of that run shows `workbook_build_outer` granted exactly 25 000 ms and `floor_granted_ms: 0`.
- Unresolved: deadline-receipt reader; stage-4 resume half of the kill/resume proof pending a quiescent tree.

## Completion receipt
- hard-runtime-clock 113 checks, 11 mutations rejected, 0 violations. Nine independent RED demonstrations captured before/against the repair: (1) `controllerStartedEpochMs` restored in vnext → "must not size any budget from its own process start"; (2) ledger read disabled → "a second invocation must ADD to the clock, not restore it (807 -> 503)"; (3) identity check disabled → "a foreign ledger must be re-identified"; (4) sweep removed → "a SIGKILLed invocation must be charged"; (5) one `enterFlowStage` removed → "stage decisions must consult the one clock"; (6) floor debt uncapped → "ten retries must not mint ten fresh floors ([60000 ×10])"; (7) terminal-catch charge removed → "a thrown run must still record what it consumed"; (8) monotonic reading dropped → "a backwards wall-clock step must not zero out a real compute charge"; (9) non-finite refusal removed → "a budget with no usable input must refuse".
- `INTERNAL.runtime_budget_overrun` now HAS producers: `boundedOuterTimeoutMs` writes a `deadline_exceeded` receipt carrying the registered code and the registry's declared `checkpoint_required`/`evidence_preserved` fields into the persisted `run-deadline.json`, and `endComputeSpan` writes a `stage_budget_overrun` receipt with the same code. The registry file itself was not touched.
