# Work Package Issue Card

- Work package ID: P7.6a
- Title: Oracle-independence census slice — recursive census, shared non-empty AST independence scan, computed detection rates (CHEAPEST HIGH-VALUE CLOSE)
- Active invariant: Every oracle in scripts/verify is visible to the CI census (registered or explicitly dispositioned), proven production-import-free by an AST scan with a NON-EMPTY forbidden list, and the oracle matrix's detection rates are computed from actual kill counts — with the five synthetic_unit_only domains honestly marked NOT_INDEPENDENTLY_PROVEN.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ 6b76c0d (working tree, uncommitted)
- Working tree status: dirty — this slice's edits plus four unrelated in-flight clusters (untouched by this package); gates were run in an isolated HEAD worktree carrying only this slice's files
- Observed failure or discovery question: (1) scripts/run_ci_census_tests.mjs read scripts/ non-recursively, leaving six run_* harnesses in scripts/verify and five lib/run_*.mjs modules invisible to the census — plus the already-committed, never-classified run_terminal_crash_path_tests.mjs (census at pristine HEAD was ALREADY RED on it); (2) the production-import AST scan covered exactly one file and FORBIDDEN_PRODUCTION_IMPORTS was published as an empty (vacuous) list; (3) the matrix report wrote "detection_rate": 1.0 and "mutation_detection_rate": 1.0 as literal constants and claimed the five hand-written synthetic_unit_only domains without marking their lack of independent proof.
- Earliest responsible layer: the census directory walk; the single-file inline AST scan in run_emitted_candidate_independent_oracle_tests.py; the literal rate constants in run_critical_invariant_oracle_matrix_tests.py.
- Reproduction command: `node scripts/run_ci_census_tests.mjs` (red before dispositions); `python scripts/run_critical_invariant_oracle_matrix_tests.py` (red before matrix flags)
- Focused failing test ID: ci-census (recursive scan surfaced verify/ + lib/ scripts as UNCLASSIFIED via the existing negative self-test); critical-invariant-independent-oracle-matrix (strengthened validate_matrix rejected unflagged synthetic domains); oracle_independence scan caught an injected `import emit` in a disposable oracle copy and refuses an empty forbidden list.
- Allowed files/modules: scripts/run_ci_census_tests.mjs; NEW scripts/verify/oracle_independence.py; scripts/verify/emitted_candidate_artifact_oracle.py (FORBIDDEN list + scan call only); scripts/run_emitted_candidate_independent_oracle_tests.py (delegate to helper); scripts/run_critical_invariant_oracle_matrix_tests.py; assets/critical-invariant-oracle-matrix-v1.json (NOT registry_sha256); NEW programme/P7.6a_issue_card.md
- Explicitly out of scope (REMAINING P7.6, on the work order): registering run_terminal_crash_path_tests.mjs in assets/development-test-registry.json (forbidden file — flagged for real registration); run_frozen_cohort.mjs cohort-phase registration + nightly schedule (runner-up item); mutation-score computation over the surfaced harnesses (P7.5); the three untracked in-flight suites (run_acquisition_plan_purity_tests.mjs, run_runtime_closure_tests.mjs, run_xbrl_reconciliation_tests.mjs) whose owning clusters must register them.
- Required integration gate: ci-census exit 0 (335 checks, 189 scripts); emitted-candidate independent oracle suite PASS (19/19 mutations, 22 verify/*.py scanned, zero violations); critical-invariant oracle matrix PASS with COMPUTED rates (22/22, 5 governance mutations); test-registry contract 820 checks — all green in the isolated HEAD+slice worktree
- Last known green commit: 6b76c0d (census red at HEAD on run_terminal_crash_path_tests.mjs; all other gates green)
- Rollback plan: revert this working-tree slice (git checkout -- the six files; delete the two new files)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: census walks scripts/ recursively with scripts-relative keys and 12 new dispositions (ORACLE_MUTATION_HARNESS, SUBORDINATE_ORACLE, PRODUCTION_LIBRARY_MODULE, +1 ORPHANED_BARE_RUNNABLE); independence scan lifted into verify/oracle_independence.py, applied to every verify/*.py, forbidden list populated (9 tokens + exact-component `lib`), negative self-test baked into the runner; matrix rates computed from detected/total with artifact-scope rates asserted >= 1.0, five synthetic domains marked NOT_INDEPENDENTLY_PROVEN in asset and report, fifth governance mutation (synthetic_claims_independence) added.
- Regression: none — census negative self-tests, matrix per-mutation asserts and the original single-file scan semantics all preserved or strengthened; matrix bindings.oracle_sha256 rebound to the edited oracle, registry_sha256 untouched.
- Unresolved: run_terminal_crash_path_tests.mjs deserves registry registration (dispositioned ORPHANED_BARE_RUNNABLE meanwhile); the two same-language .mjs verify checkers (recalc_second_opinion.mjs, run_linked_debt_addback_proof_test.mjs) remain NOT_INDEPENDENT by construction.

## Completion receipt
- Census: red (14 UNCLASSIFIED incl. 6 dark verify/ harnesses, 5 lib modules, 1 orphaned committed suite) → green, 335 checks over 189 scripts; snapshot ci/test_registry_census.json regenerated via the suite's own output path.
- Independence: 22 verify/*.py scanned against a 9-token forbidden list, zero violations; injected `import emit` and `from lib import row_plan` both caught; empty forbidden list now a hard ValueError.
- Matrix: 22/22 kills computed per domain (mutation_detection_rate 1.0 computed, not literal); not_independently_proven_domains = broker_period, ebitda_basis, instrument_identity, typed_unresolved_state, workbook_error_scan; governance mutations caught 4 → 5.
