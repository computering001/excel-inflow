# Work Package Issue Card

- Work package ID: P7.2
- Title: Freeze v3.7.7 behaviour as goldens over the canonical Economic IR, with a declared difference taxonomy, a hash-chained approval ledger and an audited regeneration command
- Active invariant: v3.7.7 behaviour is frozen as a set of GOLDEN records whose primary economic truth is the canonical Economic IR (not the workbook); every golden difference is classified by a declared taxonomy; regeneration is an audited, single documented command that records WHO approved and WHY, against WHICH commit; and a golden can never be silently updated to make a test pass. A never-acceptable difference — a never-zero collapse, a shadow-boundary violation, record corruption, a coverage gap, an uncertified record, or an unclassified difference — can never be approved by any actor for any reason. The FREEZE ITSELF is a human act: only a named human may perform the genesis freeze or approve a regeneration, so PRE-FREEZE is a lawful, loudly-declared state and the mechanism is verifiable before anyone has signed for it.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`; built at `e31b53e` and re-verified green as the tip advanced under concurrent packages to `e3f3ba2` and then `c39672e`. Work NOT committed — no-commit instruction.
- Working tree status: dirty — shared worktree; twelve `scripts/lib/**` files and two assets carry other packages' in-flight edits. Mid-package, a concurrent agent left `scripts/lib/semantic_graph.mjs` importing `economicStatementBindings` from a `scripts/lib/layered_graph_constitution.mjs` that does not yet export it, breaking the module graph for every suite that touches the semantic graph. Every gate below was therefore re-verified in a DETACHED worktree at the branch tip plus only this package's files. The FROZEN half of the gate was likewise verified in a disposable worktree at `c39672e` that was destroyed afterwards, so the real `goldens/` was never frozen.
- Observed failure or discovery question: The Phase-7 gap agent proved "NO goldens anywhere. No goldens/ directory; the registry doctrine hard-declares golden_actions_performed: false; the cohort runner lists golden_update under prohibitions. The only committed expected-contract is a custody-gated real-filings classification receipt. No difference taxonomy, no approval ledger, no audited regeneration command, no certification history." Reproduced against the branch tip: `goldens/` returned ENOENT, as did all five mechanism files; `assets/development-test-registry.json:6` declared `golden_actions_performed: false`, mirrored at `scripts/run_development_gate.mjs:377`; `scripts/run_frozen_cohort.mjs` listed `golden_update` under `prohibitions` and `golden_or_baseline_promotion` under `not_claimed`; FIVE files referenced the Economic IR seal (`economic_ir.mjs`, `model_ir_v3.mjs`, `economic_authority_seal.mjs` and their two suites) and NONE compared it to a frozen expected value — `grep -rl 'expected_seal|frozen_seal|golden_seal' scripts/ assets/` returned nothing, so no comparison was possible at all; and no golden approval ledger existed anywhere (the sole `approved_by` in the tree is `scripts/render/baseline.py:43`, an unrelated render-baseline field).
- Earliest responsible layer: none — greenfield. There was no layer that owned a frozen behavioural expectation. The ORDERING TRAP recorded on the work order (PHASE7_WORK_ORDERS.md:18-22) was the real blocker: the phase wants canonical-Economic-IR goldens and until P3.1 there was no Economic IR, so freezing would have pinned the whole-workbook artifact the register warns against. P3.1 landed `scripts/lib/economic_ir.mjs` + `assets/economic-ir-v1.schema.json`, and its closing section "What a Phase-7 golden would pin from this IR" is the specification this package implements.
- Reproduction command: `node scripts/run_behavioural_golden_tests.mjs`
- Focused failing test ID: behavioural-goldens (NOT registered — `assets/development-test-registry.json` is a forbidden file for this package; 197 checks unfrozen / 249 frozen incl. the seven pinned fact families per fixture, seven taxonomy-asset weakening refusals, the five required mutations, the PRE-FREEZE/FROZEN discrimination mutations, sixteen ledger-chain refusals, ten regeneration refusal codes and the structural no-update-flag scan). Red in two stages before the package: first `ERR_MODULE_NOT_FOUND` for `scripts/lib/behavioural_golden.mjs`, then `ENOENT goldens/`.
- Allowed files/modules: NEW `goldens/**` (currently `README.md` only, stating what is and is not pinned, and that the tree is PRE-FREEZE; the records and `approval-ledger.jsonl` appear when a human runs `freeze`), NEW `scripts/lib/behavioural_golden.mjs` (record compiler + comparator + taxonomy contract + ledger), NEW `assets/behavioural-golden-v1.schema.json`, NEW `assets/golden-difference-taxonomy-v1.json`, NEW `scripts/regenerate_behavioural_goldens.mjs` (the audited command), NEW `scripts/run_behavioural_golden_tests.mjs`, NEW this card. No existing file touched. `scripts/lib/economic_ir.mjs` is IMPORTED and not edited (held by another agent).
- Explicitly out of scope (REMAINING P7.2, for the parent to schedule): flipping `golden_actions_performed` in `assets/development-test-registry.json` and its mirror in `scripts/run_development_gate.mjs:377` (both forbidden files here) and registering this suite in CI — reported with the exact required wording rather than performed; adding the replacement prohibition that keeps golden REGENERATION prohibited inside every gate; the `scripts/run_frozen_cohort.mjs` `prohibitions`/`not_claimed` entries (forbidden file); goldens for the 32-recipe synthetic cohort (P7.3's generator owns those fixtures); pinning any solved forecast or schedule number (the IR compiles before the solver, so those slots are correctly `unresolved` — closed by passing `solvedSchedules` in, never by weakening a state); any workbook-shaped golden layer, now permanently unnecessary and structurally refused by `golden_kind`.
- Required integration gate: this suite green in BOTH declared states (197 unfrozen / 249 frozen), economic-ir 79 (unchanged), user-flow 14/14, programme-control (this card must lint), `node --check` on every touched file
- Last known green commit: `e31b53e`
- Rollback plan: delete `goldens/`, `scripts/lib/behavioural_golden.mjs`, `scripts/regenerate_behavioural_goldens.mjs`, `scripts/run_behavioural_golden_tests.mjs`, `assets/behavioural-golden-v1.schema.json`, `assets/golden-difference-taxonomy-v1.json` and this card. Nothing existing was modified and nothing existing imports the new module, so removal is total and leaves no residue.

- Attempt count (1-3): 2 (attempt 1 shipped the mechanism but wrote a genesis freeze attributed to the programme owner "on the owner's instruction" — a FALSE AUDIT RECORD, withdrawn on coordinator review; attempt 2 makes PRE-FREEZE a lawful declared state instead)
- Status: MECHANISM SEALED, UNCOMMITTED (no-commit instruction). NOTHING IS FROZEN — the genesis freeze is pending a named human. Registry/gate declaration change reported, not performed.

## Difference classification
- Intended correction: greenfield declaration. A golden record pins the seven facts P3.1's card named, all DERIVED by walking the IR's own typed slots — never hand-transcribed. Two lawful states are declared, discriminated ONLY by the approval ledger's existence: PRE-FREEZE (nothing pinned, reported loudly, mechanism still fully asserted) and FROZEN (every rule at full strength). The difference taxonomy's class set and each class's acceptability live in CODE (`GOLDEN_DIFFERENCE_CLASS_CONTRACT`), in the discipline of P8.6a's `ROLLBACK_POLICY_CLAUSE_CONTRACT`, so the asset can neither drop a class nor promote a never-acceptable one; the asset must even mirror each class's `detects` text verbatim. The approval ledger reuses P8.6a's hash-chain discipline exactly (`record_hash` = canonical digest of the body sans `record_hash`; `previous_record_hash` = the preceding record's hash; append-only, byte-prefix-verified on disk). Regeneration is structurally hard in four independent ways: it lives in a separate explicitly-invoked command that no gate may call; it demands an approval record naming a human, a ≥40-character reason, the fixtures, the classes and the commit; the approval is single-use and commit-bound; and a never-acceptable difference refuses regeneration BEFORE anything is written and INDEPENDENTLY of what the approval says. `createGoldenApprovalRecord` additionally throws when asked to declare a never-acceptable class, so such an approval cannot be constructed at all.
- Regression: none — no existing file was modified and nothing existing imports the new module. The two seals the freeze will pin (`284a00d3…`, `126bc386…`) are byte-identical to the ones P3.1 recorded independently at `b3dd728` and reproduce in a clean detached worktree, so they are stable facts about the code rather than artefacts of the dirty shared tree. No validator was weakened to reach the PRE-FREEZE state: the human-actor rule and the coverage-gap rule both keep their exact strength, and PRE-FREEZE adds a SIXTH never-acceptable class rather than relaxing any existing one.
- Unresolved: the suite is not in `assets/development-test-registry.json`, so CI does not run it yet, and `golden_actions_performed: false` remains declared (both forbidden files — the required wording is in the completion receipt below). NOTHING IS FROZEN: the genesis freeze awaits a named human, so no v3.7.7 behaviour is pinned yet and the goldens cannot catch a regression until someone runs `freeze`. That is the correct trade — a real pin signed by nobody is worse than no pin. Separately observed and not fixed: a concurrent agent's half-written `layered_graph_constitution.mjs`/`semantic_graph.mjs` pair currently breaks the module graph in the shared worktree.

## Completion receipt
- **THE MECHANISM IS SEALED; NO GOLDEN IS FROZEN.** `goldens/` contains only `README.md`. There is no `approval-ledger.jsonl` and no record under `goldens/economic-ir/`, so the tree is in the declared PRE-FREEZE state and no v3.7.7 behaviour is pinned. The genesis freeze is a HUMAN act awaiting a named human: `node scripts/regenerate_behavioural_goldens.mjs freeze --actor <id> --role <role> --reason "<why>"`. Nothing automated will perform it — `createGoldenApprovalRecord` requires `actor.kind === "human"` for freeze AND approve, `assets/behavioural-golden-v1.schema.json` pins `certification.frozen_by.kind` at `const: "human"`, and the CLI offers no `--actor-kind`. A golden pins what "correct" means, so automation that could freeze could pin its own defects as correct.
- WITHDRAWN in attempt 2: attempt 1's genesis freeze, attributed to `prestonarchie@icloud.com` as a human acting "on the owner's instruction". The owner never ran the command and never reviewed the pinned facts; their instruction was to implement the phases. A ledger whose FIRST entry misattributes a human approval undermines the mechanism it exists to provide, so the records and ledger were removed rather than re-signed.
- behavioural-goldens green in BOTH declared states: **197 checks / `"frozen": false`** in the committed PRE-FREEZE tree, and **249 checks / `"frozen": true`** when frozen (verified by freezing inside a disposable git worktree at `c39672e`, running the full gate there, then destroying the worktree — the real `goldens/` was never written to, proven by the suite's own final check that the run left the real state exactly as it found it). The frozen half is exercised at full strength on EVERY run regardless, via a guarded throwaway temp freeze; the guard asserting that target is under `os.tmpdir()` and is not the real `goldens/` is itself a checked structural proof.
- The facts the freeze WILL pin, compiled from the live pipeline on every run: `standard-maximal-v2` seal `284a00d3…`, 336 typed slots (153 historical / 138 forecast / 45 schedule), census 45 `reported_number` / 24 `derived_number` / 12 `captured` / 12 `not_applicable` / 12 `missing` / 231 `unresolved` and **0 `reported_zero`, 0 bare numbers**, 51 nodes over a 15/28/4/4 `historical_basis` histogram, 3 schedule periods. `standard-net-cash-v2` seal `126bc386…`, 36 / 9 / 24 / 12 / 12 / 27 / 216 with its 15 acquisition quantities `not_applicable_in_every_period`, 0 value-bearing and 0 `reported_zero` — a real fixture with no acquisition, never a fabricated zero. Both seals match P3.1's independent record at `b3dd728`.
- PRE-FREEZE cannot hide a real gap, proven by mutation in suite section (F). The discriminator is the LEDGER'S EXISTENCE alone, never record readability: a frozen set with `standard-net-cash-v2` DELETED is still `FROZEN` and still raises the never-acceptable `golden_coverage_gap` naming that fixture; deleting EVERY record while the ledger stands yields a full set of coverage gaps rather than PRE-FREEZE; a corrupted-but-present record is `golden_record_corruption` and is not misreported as uncertified; an unparseable record raises rather than silently reading as pre-freeze. The reverse hole is closed too — records present with NO ledger are the new never-acceptable `uncertified_golden_present`, so the certification history cannot be shed while the pins are kept.
- Taxonomy: 13 classes, 7 requiring approval (`fixture_identity_drift`, `contract_version_bump`, `economic_drift`, `coverage_census_drift`, `slot_shape_drift`, `historical_basis_drift`, `schedule_shape_drift`) and 6 NEVER acceptable (`never_zero_collapse`, `shadow_boundary_violation`, `golden_record_corruption`, `golden_coverage_gap`, `uncertified_golden_present`, `unclassified_difference`). Seven asset-weakening attempts refused: dropped class, flipped acceptability, uncertified-goldens made approvable, paraphrased `detects`, invented class, shortened never-acceptable list, in-gate regeneration permitted.
- The five required mutations: (1) economic drift → `DIFFERENT`, `economic_drift` alone, requires approval; (2) coverage-census drift → `DIFFERENT`, `coverage_census_drift`, approvable, naming the exact census facts; (3) never-zero collapse (9 `unresolved` → `reported_zero`) → `never_zero_collapse`, NEVER acceptable, and the collapsed census facts are claimed by the collapse class so it can never be downgraded to the approvable one — plus `bare_number_slots` off 0 and an acquisition family turning `not_applicable` into zeros, both never-acceptable; (4) `regenerate` with no `--approval` → exit 3, `no_approval_record_named`, nothing written; (5) an approval granted against `HEAD~1` → exit 3, `approval_commit_mismatch`, naming both commits.
- Refusals beyond the five: a never-acceptable difference under an otherwise perfect approval → `difference_never_acceptable` ("Fix the code, not the golden"), nothing written and nothing repaired; an approval not naming the observed class → `approval_class_not_declared`; a no-op → `nothing_to_regenerate`; a spent approval → `approval_already_consumed`; an unknown hash → `approval_record_absent`; the genesis freeze used as an approval → `approval_not_an_approve_event`; a tampered ledger → `approval_ledger_invalid` and an append onto it rejected rather than repaired; `freeze` re-run once a ledger exists → refused, so it is not a back door. Ledger chain refusals: edit-after-sealing, sequence gap, reorder, replay, non-genesis first record, empty ledger, double-consumption, backdating, automation actor, sub-40-character reason, no declared class, and an approval declaring any of the five never-acceptable classes (impossible to construct).
- Append-only proven on disk, not asserted: after the lawful regeneration the ledger's earlier bytes are byte-identical, the chain still validates, and the appended record consumes its approval. The lawful path also proves the regenerated record carries the seal the live pipeline actually produces and records WHO approved it, WHY and under which approval hash.
- Structural proof that the gate cannot launder a failure: the comparison suite imports neither `regenerateBehaviouralGoldens` nor `freezeBehaviouralGoldens`; no `--update`, `--fix`, `--accept`, `--force`, `--yes` or `--bless` flag exists anywhere in the command; the comparator writes no file; `status` exits 1 and changes nothing; and the bare command regenerates nothing and says so.
- REPORTED, NOT PERFORMED (forbidden files): `assets/development-test-registry.json:6` must change `golden_actions_performed: false` → `true` (with `scripts/run_development_gate.mjs:377`'s mirror), and the honest replacement prohibition is `golden_regeneration_inside_gate: prohibited` — comparison is in the gate, regeneration never is, and `assets/golden-difference-taxonomy-v1.json` already declares `regeneration_prohibited_inside_any_gate: true` for the registry to bind to. `scripts/run_frozen_cohort.mjs` should keep `golden_update` prohibited and may keep `golden_or_baseline_promotion` under `not_claimed` — the cohort runner performs neither.
