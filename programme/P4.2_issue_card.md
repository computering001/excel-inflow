# Work Package Issue Card

- Work package ID: P4.2
- Title: Opening-debt reconciliation as a structured, schema-validated bridge artifact (no scalar-into-pool absorption)
- Active invariant: The opening-debt reconciliation is a structured bridge artifact — every residual decomposed into taxonomy lines (instrument openings, FX translation, classification moves, residual pool, stated unexplained残) — and an unexplained residual above tolerance refuses with the registered terminal reason code SOURCE.opening_debt_unresolved, never silently absorbs into a pool row.
- Release/phase: v3.7.7 / Phase 4
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `3d4e2ef`; not committed per work-order constraint)
- Working tree status: P4.2 delta uncommitted (4 new files + 1 modified), everything else clean
- Observed failure or discovery question: The P4 gap agent proved there was no reconciliation bridge — one signed-residual scalar compared against one pool row; no schema, no artifact, no taxonomy, no FX line; years 1–2 asserted not reconciled; and the failure surfaced as an untyped coverage BLOCK, not the registry reason code.
- Earliest responsible layer: debt_reconciliation (opening-instrument-state / opening-debt boundary in solveCase)
- Reproduction command: `node scripts/run_opening_debt_bridge_tests.mjs`
- Focused failing test ID: opening-debt-bridge (19 checks incl. the red-proof pool-sneak mutation, an FX taxonomy-line case, and schema validation of every emitted artifact via lib/json_schema.mjs)
- Allowed files/modules: assets/opening-debt-bridge-v1.schema.json (NEW), scripts/lib/opening_debt_bridge.mjs (NEW pure compiler), scripts/lib/solver.mjs (opening-debt residual site only — bridge call + artifact attachment + typed refusal, additive), scripts/run_opening_debt_bridge_tests.mjs (NEW), this card
- Explicitly out of scope (REMAINING P4.2, on the work order): rewiring coverage.mjs debt_reconciliation checks to consume the bridge artifact (coverage is a forbidden module for this package); true instrument-level reconciliation of historical years 1–2 (no instrument-level history exists in the case contract — the bridge now DECLARES them asserted_no_instrument_history instead of leaving the assertion invisible)
- Required integration gate: user-flow 14/14, case-compiler equivalence byte-identical to pre-change baseline (pre-existing cohort reds unchanged, zero new diffs, solved numbers hash-identical), acquisition solver case tests (standard-maximal-v2) green, funded-acquisition 16, opening-debt-bridge 19 — met
- Last known green commit: `3d4e2ef` (baseline for this delta; equivalence gate already exited 1 there with 55 cohort economic diffs and 1 failed compiled solve — other_interest_authority — untouched by this change)
- Rollback plan: revert the working-tree delta (delete the three new scripts/assets files, restore solver.mjs from 3d4e2ef, delete this card)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: opening-debt reconciliation compiled into a schema-validated bridge artifact attached to every solution; unexplained residual above money tolerance now a typed refusal (SOURCE.opening_debt_unresolved) at the solve boundary; behavior additive — RECONCILED / NO_REPORTED_ANCHOR / NOT_EVALUABLE verdicts leave the numeric solve path byte-identical (proved by pre/post solution hashes and a byte-identical equivalence run)
- Regression: none (all four external gates match or beat their pre-change baselines)
- Unresolved: coverage.mjs still runs its own scalar reconciliation checks in parallel (forbidden file here; migration to consume the bridge artifact is follow-on work); years 1–2 remain asserted — now declared as such on the artifact face

## Completion receipt
- opening-debt-bridge 19 → 0 (red-proof: +50 sneaked into smc_other_debt_pool caught with reason code, verdict REFUSE_UNEXPLAINED_RESIDUAL, unexplained_residual stated as a line; FX translation a named line per foreign instrument tying to period-end rates; schema bites on invented line kinds, string totals, missing/alien verdicts and wrong refusal codes)
- Numeric identity: solveCase(standard-maximal-v2) and solveCase(standard-net-cash-v2) solution hashes identical pre/post change (86ce1ad4…, 588ab64c…); case-compiler equivalence output byte-identical to baseline
- No new terminal reason code needed: SOURCE.opening_debt_unresolved (registry, owner debt_reconciliation, terminal SOURCE_REQUIRED) is the refusal code and is asserted against the sealed registry by the suite
