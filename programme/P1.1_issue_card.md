# Work Package Issue Card

- Work package ID: P1.1
- Title: Choose and install the canonical contract source
- Active invariant: Every producer emits an object that validates immediately against the same canonical contract used by consumers.
- Release/phase: v3.7.7 / Phase 1
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: clean at seal
- Observed failure or discovery question: 83 hand-maintained schemas, 34 JS validation call-sites and separate Python hand validators with no single source. DISCOVERY FINDING: the repository JSON-Schema validator enforces only {type, enum, const, pattern, required, properties, additionalProperties, items} — `oneOf`/`anyOf`/`$ref` in 10+ shipped schemas are silently unenforced wherever validateJsonSchema is the validator. Recorded for P1.4 (producer-boundary validation) to close per schema.
- Earliest responsible layer: contract toolchain
- Reproduction command: `node scripts/run_canonical_contract_tests.mjs --python <venv-python>`
- Focused failing test ID: canonical-contracts (registered; 9 checks). The union negative test FAILED against the schema-based binding — that failure is what surfaced the validator gap; the binding now validates definition semantics directly in both languages.
- Allowed files/modules to change: `assets/contracts/*` (new), `assets/generated/*` (generated), `scripts/lib/generated/*` (generated), `scripts/generated/*` (generated), `scripts/compile_canonical_contracts.mjs` (new), `scripts/run_canonical_contract_tests.mjs` (new), registry + pins + audit census regeneration
- Explicitly out of scope: migrating the 83 existing schemas (P1.2-P1.8 domain work); touching validateJsonSchema (its gap is P1.4's seam); shipping generated dirs in the release closure (wired when a shipped script consumes them)
- Required independent/physical proof: deterministic generation A/B (byte-identical hashes across two runs — proven); JS→Python→JS byte-preserving round trip (proven)
- Required integration gate: registry contract 740 PASS; census 303 PASS; governance 0; ownership 0
- Raw input/evidence hashes: assets/generated/contract-generator-receipt.json binds definition sha → output shas
- Last known green commit: `6a96ecf`
- Rollback plan: revert this commit; nothing consumes generated bindings at runtime yet
- Attempt count (1-3): 2 (attempt 1: schema-backed JS binding — rejected by its own negative test on the oneOf gap; attempt 2: definition-walking bindings — green)
- Status: SEALED_SHADOW (authority not switched; formal Phase 1 activation follows the P0.8 phase seal per sequencing)

## Difference classification

- Intended correction: none to runtime — toolchain + first migrated contract (support-envelope classification), consumed only by tests until activation
- Presentation-only: none
- Accepted normalization: canonical IDL is repository-owned JSON (sealed release closure forbids new external dependencies; format documented in the generator header)
- Regression: none
- Unresolved: the validateJsonSchema oneOf/$ref enforcement gap (named above, owned by P1.4); field taxonomy documented per contract in the definition (source_facts/derived_facts/receipts/references/presentation_only)

## Completion receipt

- Commands and exit codes: generator write/check → 0/0; determinism A/B → identical; canonical-contracts 9 → 0; registry 740 → 0; census 303 → 0
- Evidence paths/hashes: assets/contracts/support-envelope.contract.json; assets/generated/{support-envelope.generated.schema.json, contract-generator-receipt.json}; scripts/lib/generated/support_envelope_contract.mjs; scripts/generated/support_envelope_contract.py
- Residual risks: only one contract migrated (by design — the spec's step 4); the version-negotiation migration table starts empty (supported_versions ["1.0.0"]) and unknown versions are refused in both languages
- Next package first action (P1.2): typed financial value model — discovery: inventory the value-state vocabulary already shipped (extractor value_states: reported_number, period_support_required, prior_filing_support...) against the pack's minimum twelve states and define the TypedFinancialValue contract in the P1.1 toolchain.
