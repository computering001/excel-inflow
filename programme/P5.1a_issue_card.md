# Work Package Issue Card

- Work package ID: P5.1a
- Title: Acquisition plan purity — retire post-serialisation label-matching injection; recorded plan operations + plan-contract suite (SLICE of P5.1)
- Active invariant: Every economic value the funded-acquisition overlay contributes enters through the PLAN — recorded plan operations derived from (rowPlan, modelCase) alone, each carrying provenance — never by post-serialisation label matching over sheet XML/plan rows; the plan-contract suite proves the emitted acquisition content is derivable from the plan operations alone and rejects a post-hoc injection mutation.
- Release/phase: v3.7.7 / Phase 5
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `c36b56c`; slice delivered uncommitted per work order)
- Working tree status: dirty — this slice plus other agents' in-flight slices (forecast-ownership, XBRL, controllers); this slice touches only its allowed files
- Observed failure or discovery question: GAP verdict — `funded_acquisition_plan.mjs` injects economics at plan time AND post-serialisation by label matching (`applyFundedAcquisitionPlan` over the extracted captured plan at the `build_dynamic_model.mjs` L5-PLAN site: label-row discovery, adjacent-control geometry search, header-scanned period years), with no registered plan-contract suite. The label pass also rewrote the consolidated Change in Debt parent into a direct `$P$8` proceeds hardcode the workbook itself never carries — a second financing writer nothing audited.
- Earliest responsible layer: scripts/lib/funded_acquisition_plan.mjs (single overlay authority) + the captured-plan call site in scripts/build_dynamic_model.mjs
- Reproduction command: `node scripts/run_acquisition_plan_purity_tests.mjs`
- Focused failing test ID: acquisition-plan-purity (21 checks; red pre-repair on the missing recorded-operations API and on the wiring check proving `applyFundedAcquisitionPlan(` still ran over the serialised plan; includes the post-hoc-injection mutation — the historical label pass run against a pure plan must be rejected by `verifyFundedAcquisitionPlanPurity`)
- Allowed files/modules: scripts/lib/funded_acquisition_plan.mjs (new `planFundedAcquisitionOperations` / `applyFundedAcquisitionPlanOperations` / `verifyFundedAcquisitionPlanPurity`; workbook hook refactored to consume the recorded operations; legacy `applyFundedAcquisitionPlan` retained as tested legacy surface, no longer wired), scripts/build_dynamic_model.mjs (funded-acquisition call sites only: captured plan now receives recorded operations + purity verify; purity verify added on both synthesised-plan sites), scripts/lib/plan_builder.mjs (permitted additive — no change proved necessary), NEW scripts/run_acquisition_plan_purity_tests.mjs, NEW programme/P5.1a_issue_card.md
- Explicitly out of scope (REMAINING P5.1, on the work order): builder-capability gaps — images/page_setup/defined_names still bolted on post-`toPlan` (broker page images in `synthesisePlan`); plan-y vs plan diff wiring (`emit compare` exists unused); registering the new suite in the development-test registry (registry files forbidden to this slice)
- Required integration gate: acquisition-plan-purity 21; funded-acquisition transaction 16; acquisition solver case (standard-maximal-v2) PASS; acquisition valuation contract PASS (19, 4 mutations rejected); workbook byte identity pre/post (plan-only build + `python3 -m emit build`) — standard-maximal-v2 (acquisition enabled=1) xlsx `ce465d37…b69330` and plan.json `c3bb575c…ca168` identical pre/post; standard-net-cash-v2 (enabled=0) xlsx `7e0d4b79…b64564` and plan.json `6bd76daa…6b6b` identical pre/post; `node --check` on touched files. user-flow 5/14 — pre-existing/independent: identical failure (`$.forecast_ownership_preflights.period_scoring is not an allowed property`, plus `Immutable skill runtime changed during the run: scripts/lib/forecast_ownership_resolver.mjs`) reproduced with this slice's edits stashed; owned by the concurrent forecast-ownership slice.
- Last known green commit: `c36b56c`
- Rollback plan: revert the two edited files and delete the two new files (slice is uncommitted)
- Attempt count (1-3): 1
- Status: DELIVERED (slice; remainder tracked on the work order)

## Difference classification
- Intended correction: the captured plan's funded-transaction cells are now written address-keyed from the recorded plan operations (identical formula text to the portable route: literal period years, controls resolved from `rowPlan.controls`) instead of label-matched header-derived formulas; the consolidated Change in Debt parent keeps its schedule link in the captured plan (the direct `$P$8` hardcode the label pass injected is now a detected purity violation); every shipped plan (captured and synthesised) passes `verifyFundedAcquisitionPlanPurity`.
- Regression: none — workbook and plan bytes on the live (plan-only) route are hash-identical pre/post for both certified fixtures; transaction suite unchanged at 16.
- Unresolved: builder capabilities and plan-y diff wiring (out-of-scope list above); the captured `--out` route is unexecutable in this environment (private `scripts/build_package.mjs` writer absent), so its repair is proven at suite level, not by a package build.

## Completion receipt
- acquisition-plan-purity 21 → 0 (recorded-operations derivability, provenance fields, consolidated-debt skip, address-keyed application fail-closed, purity verify green on pure plans in both plan shapes, and four rejected mutations: the historical label pass itself, a proceeds hardcode on the consolidated parent, a tampered cache, a non-derivable formula rewrite)
- funded-acquisition transaction 16 → 0; solver case (maximal) PASS; valuation contract 19 PASS
- Byte identity: pre/post sha256 equal for both fixtures' .xlsx and .plan.json (hashes above)
