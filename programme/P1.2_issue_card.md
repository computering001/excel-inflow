# Work Package Issue Card

- Work package ID: P1.2
- Title: Implement the typed financial value model
- Active invariant: Blank, nil, missing, parse failure and unresolved are never treated as zero.
- Release/phase: v3.7.7 / Phase 1
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (shadow work staged with P1.1)
- Working tree status: clean at commit
- Observed failure or discovery question: The shipped value-state vocabulary carries 8+ of the twelve minimum states as strings on extractor rows, but no typed model exists — nullable numbers still flow through the compiler, and the missing-to-zero class remains structurally possible.
- Earliest responsible layer: typed value parser (target sole writer for raw value interpretation)
- Reproduction command: `node scripts/run_typed_financial_value_tests.mjs --python <venv-python>`
- Focused failing test ID: typed-financial-value (registered; 86 checks)
- Allowed files/modules to change: `assets/contracts/typed-financial-value.contract.json` (new), generated bindings, `scripts/lib/typed_financial_value.mjs` (new), `scripts/run_typed_financial_value_tests.mjs` (new), registry + pins + audit census
- Explicitly out of scope (REMAINING P1.2 WORK, not silent): migrating the extractor, evidence run and compiler to EMIT typed values — that migration follows P1.3 (decimal/unit arithmetic) and P1.4 (producer boundaries) and switches authority only after dual-read proof on the blocker corpus
- Required independent/physical proof: cross-language verdict agreement on 18 shape cases (proven)
- Required integration gate: registry contract 745 PASS; census 305 PASS; governance 0
- Raw input/evidence hashes: generator receipt binds definition → outputs
- Last known green commit: `b989037`
- Rollback plan: revert; nothing consumes the typed model at runtime yet
- Attempt count (1-3): 1 (one generator repair: union roots dispatch — caught by the suite before commit)
- Status: IMPLEMENTING (core contract + accessor sealed in shadow; producer migration outstanding)

## Difference classification

- Intended correction: none to runtime yet
- Presentation-only: none
- Accepted normalization: the stray "blank" string variant found in discovery maps to reported_blank at migration time (migration table entry when producers move)
- Regression: none
- Unresolved: producer migration (the bulk of P1.2), sequenced after P1.3/P1.4

## Completion receipt (core)

- Commands and exit codes: typed-financial-value 86 → 0; canonical-contracts 9 → 0; registry 745 → 0; census 305 → 0
- Evidence: the never-zero invariant is STRUCTURAL — absence states cannot carry a value field (smuggled-value negatives pass in both languages); numericValueOf returns null for all eight absence states and throws on unvalidated shapes
- Residual risks: none new
- Next action: P1.3 discovery (centralised decimal/unit/currency/period arithmetic) after the P0.8 seal
