# Work Package Issue Card

- Work package ID: P0.2
- Title: Build the immutable blocker replay corpus
- Active invariant: Every known blocker reproduces deterministically or is explicitly classified as non-reproducible with missing evidence named.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure`, parent `6a64f74` (P0.1 seal)
- Working tree status: clean before package; only corpus/registry/programme files changed
- Observed failure or discovery question: No versioned corpus existed; blockers lived in narrative memory. Discovery additionally surfaced a live defect: the registered identity-governance suite fails at exact head (stale `3.7.5` pin vs `3.7.6` manifest).
- Earliest responsible layer: test/evidence custody (corpus); the surfaced version-pin defect's earliest layer is the identity-governance test literal — repair deliberately deferred to P0.3 (exact-head CI truthfulness) to keep one invariant per package.
- Reproduction command: `node scripts/run_blocker_corpus_tests.mjs`
- Focused failing test ID: blocker-replay-corpus (registered; 80 checks)
- Allowed files/modules to change: `test-corpus/blockers/*`, `scripts/run_blocker_corpus_tests.mjs`, `assets/development-test-registry.json` (+ oracle-matrix registry pin rebind), `programme/*`
- Explicitly out of scope: all product source; run_release_identity_governance_tests.mjs (P0.3); goldens
- Required independent/physical proof: n/a (evidence-custody package); heavy replays hash-verified with execution owned by their registered suites
- Required integration gate: registry contract (695 checks) + oracle matrix (1.0 detection) after registration
- Raw input/evidence hashes: Apple PDF `10859005…aab`, request `f93cb8a4…183`, expectations `0403f05e…731`, astrazeneca-v2 `877724f8…f39`, standard-maximal `8b941b4e…f48`, standard-net-cash `c5716611…2ff`
- Last known green commit: `6a64f74`
- Rollback plan: revert this commit; the corpus adds no product behaviour
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification

- Intended correction: none to product source
- Presentation-only: none
- Accepted normalization: registry gained one typed entry; oracle-matrix registry_sha256 rebound accordingly (established procedure for registry growth)
- Regression: none introduced; one PRE-EXISTING head failure surfaced and documented (stale version pin — P0.3)
- Unresolved: package-identity-discrepancy expected result stays KNOWN_FAILING until P0.3 seals

## Completion receipt

- Commit: (this commit)
- Commands and exit codes: `run_blocker_corpus_tests.mjs` → 0 (80 checks; 7 fixtures verified; tamper self-test refused as typed; known-failing signature reproduced); `run_test_registry_contract_tests.mjs` → 0 (695); oracle matrix → 0 (PASS, 1.0)
- Evidence paths/hashes: `test-corpus/blockers/corpus_manifest.json`, `test-corpus/blockers/README.md`, `programme/P0.2_reproduction_report.json`
- Residual risks: heavy replays (canary, AZ equivalence, WC decisions-stage) are hash-verified here but executed by their own suites; a host without private custody sees typed CUSTODY_ABSENT rather than verification.
- Next package first action (P0.3): repair the stale `3.7.5` literal in run_release_identity_governance_tests.mjs so it derives the expected version from assets/runtime-manifest.json (or pins 3.7.6 with the bump discipline documented), prove the suite passes at exact head, flip the corpus case expectation to PASS in the same commit, then audit the CI workflow for continue-on-error/merge-ambiguity per the package spec.
