# Work Package Issue Card

- Work package ID: P0.9
- Title: A gate run is side-effect free — stop a governance validator regenerating the artifact it verifies (D20)
- Active invariant: A gate run is side-effect free. Verification compares committed against computed and REFUSES on drift; regeneration happens only under an explicit flag, never implicitly.
- Release/phase: v3.7.7 / Phase 0
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `a66ed54` (uncommitted working tree — this package is NOT committed)
- Working tree status: dirty, and dirty from other hands too — concurrent agents hold `scripts/lib/flow_runtime.mjs` + `scripts/lib/user_flow_controller.mjs` modified and `assets/ci-gate-tiers-v1.json`, `ci/mutation_survivors.json`, `scripts/compile_mutation_adequacy.mjs`, `scripts/lib/mutation_adequacy.mjs` untracked. Every verdict below was therefore re-taken in a detached worktree at `a66ed54` plus only this package's files.
- Observed failure or discovery question: D20 reported that `node scripts/run_programme_control_tests.mjs` rewrites `ci/test_registry_census.json` as a side effect of running. Measured, that attribution is WRONG: programme-control is side-effect free over all 803 tracked files. The write originates in `scripts/run_ci_census_tests.mjs`, which defaults its output path INTO the tracked tree (`scripts/run_ci_census_tests.mjs:28`) and writes it unconditionally at the end of every run (`:221-222`). The defect is real, the named owner was not.
- Earliest responsible layer: `scripts/run_ci_census_tests.mjs` — the default value of `outPath`. Nothing upstream asks it to write; it simply has no verify mode, so its only mode is regenerate.
- Reproduction command: `node scripts/run_gate_side_effect_tests.mjs`
- Focused failing test ID: gate-side-effect (NOT registered — `assets/development-test-registry.json` is a forbidden file for this package; 258 checks). Red proof before the package: `shasum` over `ci/test_registry_census.json` moved `96928d13a0eb4af7…` → `e5687380f31d98aeb…` (1 of the 169 tracked files under `ci/ audit/ architecture/ assets/`) across one `node scripts/run_ci_census_tests.mjs`, with `source_commit` rewritten `73c3401…` → `a66ed54…` and the file left ` M` in `git status`.
- Allowed files/modules: NEW `scripts/run_gate_side_effect_tests.mjs`, NEW `programme/P0.9_issue_card.md`. `scripts/run_programme_control_tests.mjs` and `scripts/compile_test_registry_census.mjs` were in scope but needed NO change — the write is in neither, and `compile_test_registry_census.mjs`'s `--summary-out` behaviour is untouched.
- Explicitly out of scope (REMAINING P0.9, for the parent to schedule): the actual repair of `scripts/run_ci_census_tests.mjs` (read-only for this package by directive — reported, not performed, with the exact shape below); registering `gate-side-effect` in `assets/development-test-registry.json`; regenerating `ci/test_registry_census.json` to absorb that registration; the structural question of whether run provenance (`source_commit`, `source_tree`, `toolchain`) belongs in a committed, verified artifact at all — it is what makes the census unmatchable by construction and is the reason the generator was written to overwrite; the CI workflow's own `--out "${{ runner.temp }}/…"` redirect, which is already correct and should become the only shape.
- Required integration gate: programme-control 144 (and provably side-effect free; 143 at `a66ed54`, +1 for this card), test-registry-contract 985 (registry 193), ownership-census 10 without `--write`, ci-census 401 (223 scripts, with `gate-side-effect` registered), gate-side-effect 258 — all green in the detached worktree at `a66ed54` + this package's files.
- Last known green commit: `a66ed54`
- Rollback plan: delete `scripts/run_gate_side_effect_tests.mjs` and `programme/P0.9_issue_card.md`. Nothing else was modified, so there is no revert to perform — the package adds two files and changes none.
- Attempt count (1-3): 1
- Status: COMPLETE, NOT COMMITTED (the offender's repair is reported, not performed — it is a forbidden file here)

## Difference classification
- Intended correction: a new verification-only suite that pins side-effect freedom for the governance gate family, proves committed-vs-computed drift on the census still refuses at full strength, and quarantines the one real offender in a way that cannot outlive the defect. No validator was weakened and no artifact was repaired by a validator.
- Regression: none — no existing file was modified.
- Unresolved: `run_ci_census_tests.mjs` still writes `ci/test_registry_census.json` on its default path. It is quarantined and re-proven to still be an offender on every run, not exempted. Also unresolved and NOT caused here: `coercion-ban` is red at `a66ed54` (48 unsafe nullable-to-number coercions outside the baseline inventory) — this package only asserts that it does not WRITE, never that it passes.

## Completion receipt
- gate-side-effect 258 checks → 0. Six external mutations each refused for the right reason: promoting the quarantined offender into the swept subjects (`ci/test_registry_census.json [rewritten-identical]`); a write reintroduced into programme-control (`[content-changed]`); the same write with byte-identical content (`[rewritten-identical]`); substantive drift in the committed census; a blanked `scripts` array (non-vacuity); a stale quarantine where D20 looks repaired; and erosion of the reference pattern (`WRITE = true` in ownership-census, caught live via `architecture/current_mutation_map.md`).
- The digest sweep was found insufficient mid-package and strengthened: content digest ALONE goes blind exactly when the committed artifact is already current, which is the CI case. The manifest now stamps `sha256:mtimeMs`, so a byte-identical regeneration is still caught as a write. M1 and M2b are the proofs; both survive a digest-only sweep.
- Side-effect sweep across the WHOLE registry: all 192 registered suites run individually in a detached worktree at `a66ed54`, `git status` diffed after each. Exactly ONE offender — `ci-census` → `ci/test_registry_census.json`. No suite created an untracked file. Exit histogram 161 PASS / 18 exit-1 / 13 exit-2; no timeouts. Every one of the 192 declares `mutates_product_tree: false`, so `ci-census` is a declared-contract violation, not a matter of taste.
- One sweep false positive is recorded so it is not rediscovered as a defect: `prebroker-ownership-controller` appeared to write `scripts/lib/flow_runtime.mjs` in the SHARED worktree. It does not — a concurrent agent's edit landed inside that suite's measurement window, and the sweep's auto-restore reverted their in-flight work once before the sweep was moved to a detached worktree. Attribution sweeps must not run in a shared tree.
- REPORTED, NOT PERFORMED (forbidden files): (1) `assets/development-test-registry.json` must gain, after the `ci-census` entry, `{"id": "gate-side-effect", "phase": "workflow", "runtime": "node", "script": "run_gate_side_effect_tests.mjs", "timeout_seconds": 120, "mutates_product_tree": false, "test_class": "cross_layer_integration"}` — until then `run_ci_census_tests.mjs` correctly refuses the new suite as unclassified, and `gate-side-effect` reports that refusal as its own failure rather than working around it. (2) `ci/test_registry_census.json` must then be regenerated through its own generator (`node scripts/run_ci_census_tests.mjs`) to absorb the new row: 222 → 223 scripts, 399 → 401 checks. (3) The repair of `scripts/run_ci_census_tests.mjs` itself: gate the final `fs.writeFile` behind `const WRITE = process.argv.includes("--write")` and, without the flag, compare the computed report against the committed `ci/test_registry_census.json` and THROW on drift — the exact shape `run_ownership_census_tests.mjs` already uses. Note while doing it that `source_commit`/`source_tree`/`toolchain` make a strict comparison unsatisfiable, so either drop them from the committed artifact or stop committing the artifact; do not weaken the comparison to absorb them.
