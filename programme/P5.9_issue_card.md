# Work Package Issue Card

- Work package ID: P5.9
- Title: The provenance exemption tested a filed record's EXISTENCE, not its VALUE (D35)
- Active invariant: A cell painted as a filed source must state the figure that source declares. An exemption granted because a filed record exists, without checking that the cell agrees with it, is the same misrepresentation the exemption was created to permit an exception to.
- Release/phase: v3.7.7 / Phase 5
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` at `aa715ca`; committed by the coordinator at `5fd1b2a`
- Working tree status: one production file (`scripts/build_dynamic_model.mjs`). The package ran under heavy concurrency; all gate evidence was taken in an isolated worktree at `aa715ca` plus this change, because another package's in-flight `canonical_sum` edit transiently broke imports in the shared tree.
- Observed failure or discovery question: the brief asserted D23 left the compiled-case build BLOCKED and instructed the package to give `ending_cash` a formula. **Both were wrong.** `run_cash_fx_identity_workbook_tests.mjs` is GREEN at `aa715ca`; the D23 repair landed at `102ada3` (`git log -S filedHistoricalObservation`). The brief was written from two sealed issue cards (P8.1's and P8.7's) that were accurate when written and never revised after the fix landed.
- Earliest responsible layer: `scripts/build_dynamic_model.mjs`, `refuseContradictedProvenance` at `:812` — the named exception admitting a blue derived cell where a filed historical record exists.
- Reproduction command: build a case declaring `cash_policy.historical_year_end_cash = [815.593, ...]` with row values `[999.111, ...]`; before the repair it builds CLEAN and ships `G64 = 999.111` in blue.
- Focused failing test ID: cash-fx-identity-workbook (the probe is a scratch case, not a committed fixture)
- Allowed files/modules: `scripts/build_dynamic_model.mjs`, `scripts/lib/row_plan.mjs` (read, not edited), new test files, this card. Forbidden and untouched: `solver.mjs`, `canonical_model_modules.mjs`, `model_ir_v3.mjs`, the authority graph, the metamorphic layer, `runtime-manifest.json`, anything carrying the `skill_version` literal.
- Explicitly out of scope: giving `ending_cash` a formula — REFUSED, and the refusal is the finding. Under the legacy single-bucket cash policy (`build_dynamic_model.mjs:4649`, `sourcedHistoricalEndingCash`) the historical closing balance is a FILED OBSERVATION the workbook "is not entitled to replace with a reconstructed cash-flow identity", because reported cash-flow components differ from the filed balance by presentation, translation and source rounding. The correct shape is historical = filed value (blue), forecast = formula (black), and forcing a formula would have made the workbook state a reconstructed number in place of the filed one. Also out of scope: the forecast-authority channel, which remains oracle-only.
- Required integration gate: `run_cash_fx_identity_workbook_tests`, `run_user_flow_tests` 14/14, `run_reconciled_parent_portable_workbook_tests`, `run_broker_consensus_workbook_tests`, `run_phase9_broker_e2e_scenario_tests`, `run_portable_finance_workbook_mutation_tests` (15 finance + 8 semantic mutations rejected), `run_provenance_authority_tests` (130) — all green.
- Last known green commit: `aa715ca`
- Rollback plan: `git checkout aa715ca -- scripts/build_dynamic_model.mjs`. The change is refusal-only: nothing was repainted, no exemption widened, no formula given a colour it had not earned. Rolling back restores a build that ships a blue figure contradicting its declared filed source.
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification
- Intended correction: `refuseContradictedProvenance` now compares the STATED figure to the FILED one within `1e-6 * max(1, |filed|)`. Blank/nil/non-finite is SKIPPED, never coerced to zero.
- Regression: none. Certified fixtures byte-identical across all five artefacts (`model.xlsx`, `.plan.json`, `.provenance-authority.json`, `.row-map.json`, `.model-ir-v3.json`). No golden moved.
- Unresolved: the value-equality check covers the historical hardcode channel only.

## Completion receipt
- Class scope MEASURED, not assumed: the refusal was instrumented (scratch patch, reverted) and 12 workbook-producing suites swept — **51** blue + derived + case-declared historical cells across 5 cases, ALL `ending_cash`, all legitimately exempt, zero unexempted. The exemption is exactly one semantic role.
- The shipped Python oracle already caught the defect (`PROV_HARDCODE_VALUE_NOT_FILED`, `999.111 vs 815.593`), so the emitter was shipping packages a shipped oracle would block — two validators disagreeing about the same artifact, the D5 pattern again.
- Delivery verified end to end: a real workbook builds (JS plan -> `emit/__main__.py` -> `.xlsx`), opens in openpyxl, 3 sheets, foots per the independent finance proof. Verified shape: `G64 815.593 FF0000FF` / `J64 =SUM(J63,J62,J61) FF000000`.
- **Recorded for the programme:** a sealed issue card is a statement about its own moment, not a live status. This brief cost a full package because two were read as current.
