# Work Package Issue Card

- Work package ID: P3.1
- Title: Canonical Economic IR compiled in SHADOW mode beside the proof projection
- Active invariant: A canonical Economic IR is compiled, sealed and hashed alongside model_ir_v3's proof projection and gates nothing; every economic value slot is a TYPED financial value (typed_financial_value v1.0.0, twelve states) and the IR carries the per-forecast-period typed SCHEDULE state (P4.3) the proof projection omits; the IR is deterministic (same case → same seal) and its seal is recorded in the transformation receipt. Blank / nil / missing / unresolved NEVER become zero.
- Release/phase: v3.7.7 / Phase 3
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `b3dd728` (work NOT committed — no-commit instruction)
- Working tree status: dirty — P3.1 files uncommitted, alongside several other agents' in-flight edits to `scripts/lib/**`
- Observed failure or discovery question: The P3 gap agent proved `model_ir_v3` is "a blocking, value-free proof projection with no shadow mode, no typed values, no schedules, no registered suite". Reproduced against the branch tip: `compileModelIrV3(standard-maximal-v2)` returned exactly `[schema_version, case_id, case_sha256, evidence_epoch, planes, proof]` — no economic IR under any key; the serialised projection contained ZERO typed_financial_value objects (no `contract_version`, no `reported_number`, no `derived_number`) and no `schedule-typed-states` artifact; `transformationReceipt()` carried no economic-IR hash under any key. Phase 7's freeze (P7.2 goldens) and Phase 9's parity harness both need a canonical Economic IR object to name, hash and diff (PHASE8_WORK_ORDERS.md, "What Phases 9-10 need", item 1) and no such object existed.
- Earliest responsible layer: `scripts/lib/model_ir_v3.mjs` — the projection compiled five identity/authority/graph/presentation planes and stopped short of the economic object; there was no module that owned economic values at all.
- Reproduction command: `node scripts/run_economic_ir_tests.mjs`
- Focused failing test ID: economic-ir (NOT yet registered — the registry is out of scope for this package; 79 checks incl. determinism, twelve-state census, bare-number refusal, unresolved-never-zero on a real fixture, schema validation and five seal mutations)
- Allowed files/modules: NEW `scripts/lib/economic_ir.mjs` (the compiler + validator + shadow attach), NEW `assets/economic-ir-v1.schema.json` (the closed contract), `scripts/lib/model_ir_v3.mjs` (ONLY the additive shadow attach in `compileModelIrV3` and the seal record in `transformationReceipt` — every existing export signature and finding unchanged), NEW `scripts/run_economic_ir_tests.mjs`, NEW `programme/P3.1_issue_card.md`
- Explicitly out of scope (REMAINING P3.1, on the work order): registering the suite in `assets/development-test-registry.json` (forbidden file this package); wiring the IR into any gate or decision (it is shadow by contract, and promoting it would be a new schema version); passing solved schedule numbers in — `compileEconomicIr({ solvedSchedules })` is the declared seam and is unused in shadow mode, so every present-but-unsolved schedule quantity is `unresolved`; the forecast lane's value states (the solver has not run at IR-compile time); persisting the IR object itself to a sidecar (the model-ir-v3 schema is closed and is a forbidden file, so only the seal is emitted)
- Required integration gate: user-flow 14/14, statement-family-residual 44 (P2.5 lives in the same file), layered-graph-constitution on standard-maximal-v2, phase9-broker-e2e, economic-ir 79 — green; `node --check` on all touched files
- Last known green commit: `b3dd728`
- Rollback plan: delete `scripts/lib/economic_ir.mjs`, `assets/economic-ir-v1.schema.json`, `scripts/run_economic_ir_tests.mjs`, this card, and revert the two additive hunks in `scripts/lib/model_ir_v3.mjs` (the import, the `attachShadowEconomicIr` call, the two receipt fields). Nothing else reads the IR.

- Attempt count (1-3): 1
- Status: COMPLETE, UNCOMMITTED (no-commit instruction); suite registration tracked as remainder

## Difference classification
- Intended correction: a new canonical Economic IR object, compiled beside the proof projection and attached NON-ENUMERABLY so `JSON.stringify` skips it; the transformation receipt gains `economic_ir_sha256` + a compact `economic_ir` binding (mode/status/seal/node count/typed-slot count/twelve-state census).
- Regression: none. Proven byte-identical before vs after on both certified fixtures: the serialised model-ir-v3 sidecar hash, `transformationReceipt().output_sha256`, the workbook proof contract's `model_ir_sha256` and `physical_formula_graph.closure_sha256`, the whole workbook proof contract, the forecast-decision receipt and the shadow-semantic comparison are all unchanged, with blockers 0 and warnings 0 unchanged. The ONLY changed artifact is `.transformation-receipt.json`, which is where the seal is required to be recorded. The independent Python OOXML oracle (`run_reconciled_parent_portable_workbook_tests.mjs`, `semantic_oracle: PASS`) still binds its own `_canonical_sha256(model_ir)` of the emitted sidecar to the contract's `model_ir_sha256`.
- Unresolved: the forecast lane holds no numbers (correctly typed `unresolved`, reading null) because the IR compiles before the solver; schedule quantities likewise. Both are closed by passing `solvedSchedules` / a solved forecast projection in a later package, not by weakening a state. Suite registration is a remainder.

## Completion receipt
- economic-ir 79 → 0. standard-maximal-v2 seals 51 economic nodes / 3 schedule periods / 336 typed slots: 45 `reported_number`, 24 `derived_number`, 12 `captured`, 12 `not_applicable`, 12 `missing`, 231 `unresolved`, **0 bare numbers**; seal `284a00d3d6ad39b6da501637f70f4516b847886d50bac822f2a9eb71785bdfe1`.
- standard-net-cash-v2 seals `126bc386…`: 36 `reported_number`, 9 `reported_zero`, 27 `not_applicable` — the 15 acquisition quantities of a case with no acquisition are `not_applicable` reading null, in a real fixture, never a fabricated zero.
- The seal is recorded at `transformationReceipt().economic_ir_sha256` and lands in the emitted `.transformation-receipt.json` with no change to `build_dynamic_model.mjs`.
