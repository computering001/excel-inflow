# Work Package Issue Card

- Work package ID: P7.11
- Title: D10, D12, D14 — and the untyped-throw check generalised from one enum value to all 371
- Active invariant: Every value the product DECLARES it admits must produce either a supported outcome or a lawful, registered, typed refusal. An untyped throw is neither, and a release-grade identity must account for every term the instrument space actually produces.
- Release/phase: v3.7.7 / Phase 7
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` at `2d0f2c8`; committed by the coordinator at `8641134`
- Working tree status: card written by the coordinator. The package stalled on an infrastructure error before writing it — the third such loss in this session — with its repairs complete and every gate suite green in place. A first attempt at this package died the same way earlier, having written nothing.
- Observed failure or discovery question: three defects found by P7.1b's archetype catalogue and encoded there as expected-broken behaviour. D10 — `validateSolutionInvariants`' `debt.instrument_roll_forward` identity omits `pik_interest_native` and `fair_value_movement_native`, so every PIK or accreting instrument fails a release-grade invariant; the certified fixtures never exercise those instruments. D12 — instrument class `lease_liability` is in the model-case schema enum AND the support envelope's declared debt matrix, so the product declares it supported, yet compiling it throws a bare `Error` with no code, no `typed_internal_outcome` and no registered terminal reason. D14 — the EXPERIMENTAL tier reaches three real outcomes with no lawful terminal to express them.
- Earliest responsible layer: `scripts/lib/validation_invariants.mjs` (D10), `scripts/lib/instrument_period_state.mjs` (D12), `scripts/lib/support_envelope.mjs` + `assets/support-envelope-v377.json` + `assets/terminal-reason-registry-v1.json` (D14)
- Reproduction command: `node scripts/run_declared_value_typed_outcome_tests.mjs`
- Focused failing test ID: declared-value-typed-outcome (767 checks over 82 declared enums and 371 declared enum values)
- Allowed files/modules: `scripts/lib/validation_invariants.mjs`, `scripts/lib/instrument_period_state.mjs`, `scripts/lib/support_envelope.mjs`, `assets/support-envelope-v377.json`, `assets/terminal-reason-registry-v1.json`, `assets/model-case-v2.schema.json`, `assets/archetype-catalogue-economics-v1.json`, `scripts/run_archetype_economics_tests.mjs`, `scripts/run_opening_amount_typing_defect_tests.mjs`, and the new suite.
- Explicitly out of scope, held by concurrent packages: `solver.mjs`, `canonical_model_modules.mjs`, `model_ir_v3.mjs`, `equation_graph.mjs`, the graph and convergence assets, the metamorphic layer, `compile_skill_release.mjs`, `deployment-profile.json`, `development-test-registry.json`, `programme/index.json`, `ci/**`. The registry row for the new suite was added by the coordinator (`phase: proof` — `contract` has no declared owner).
- Required integration gate: declared-value-typed-outcome 767; archetype-economics 604 over 33 archetypes; opening-amount-typing 31; never-zero-and-order-invariance 12 (the D36 lock); user flow 14/14 — all green.
- Last known green commit: `2d0f2c8`
- Rollback plan: `git revert 8641134`. The repairs are additive typing and identity completion; reverting restores an untyped throw on a declared-supported instrument class and an incomplete release-grade roll-forward.
- Attempt count (1-3): 2 (attempt 1 died to an infrastructure error having written nothing)
- Status: SEALED

## Difference classification
- Intended correction: D10's roll-forward identity accounts for PIK accretion and fair-value movement — a COMPLETENESS repair, not a relaxation; the identity still refuses a genuinely broken roll-forward. D12/D14 give declared values lawful typed outcomes with registered terminal reasons.
- Regression: none. The D36 lock is intact — `instrument_period_state.mjs` was edited without disturbing the deliberately-reverted `reporting_total` drift on seeds 700563/700569/700577.
- Unresolved: see the sweep note below — the suite measures the class, and what it now enforces is the floor, not a proof that every enum value has been exercised end to end.

## Completion receipt
- **The brief asked for one instrument class; the package enumerated the class.** Rather than repair `lease_liability`, it enumerated every declared enum value in the product FROM ITS DECLARING SCHEMA — 82 enums, 371 values — and required each to produce a supported outcome or a lawful typed refusal. This is the correct generalisation of D12: the defect was never about leases, it was about the product declaring support it could not lawfully express.
- **The archetype catalogue is flipped, not deleted.** `run_archetype_economics_tests` moves 598 -> 604: the entries that ENCODED these defects as expected-broken behaviour now assert the repaired behaviour. An archetype documenting a defect must become an archetype proving the fix.
