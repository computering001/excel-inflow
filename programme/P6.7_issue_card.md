# Work Package Issue Card

- Work package ID: P6.7
- Title: Runtime doctor — typed host pre-flight before expensive work (GREENFIELD SLICE)
- Active invariant: Before expensive work begins, a runtime doctor verifies that this host can actually complete a run; every declared precondition is checked, each result is TYPED (satisfied / unsatisfied / not-applicable-with-reason / unknown-with-reason / excluded-installed-host), and an unsatisfied or unknown mandatory precondition produces a typed refusal naming a registered reason code rather than a failure discovered forty minutes later.
- Release/phase: v3.7.7 / Phase 6
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `e31b53e`
- Working tree status: dirty at authoring (concurrent Phase-3/6/8 packages in flight); the four files of this package are new and untracked, and no file outside them was modified
- Observed failure or discovery question: The Phase-6 gap agent proved NO runtime doctor existed — zero occurrences of doctor/health_canary/preflight_host anywhere in `scripts/` or `assets/`, and therefore no pre-flight check of installed hash, active pointer, Node/Python availability, required Python modules, LibreOffice, fonts, permissions, working directories or a rollback package. The only environment probe in the system is stage-4-internal `exactEnvironmentProbe` (orchestrate_release.mjs:262, called at :602), and stage 4 is spawned from the controller at run_user_flow.mjs:1881 — after acquisition, ingress, broker evidence, case compilation, the decisions stage, the forecast plan and the solver have all already been paid for. A missing `soffice`, an interpreter without openpyxl, or an unwritable work root was therefore discovered only after the whole expensive upstream half.
- Earliest responsible layer: runtime host pre-flight (a layer that did not exist; this package creates it)
- Reproduction command: `node scripts/run_runtime_doctor_tests.mjs`
- Focused failing test ID: runtime-doctor (102 checks, incl. the red proof that stage 4 — the only environment probe — is spawned strictly after intake and case compilation, and an end-to-end doctor run under a poisoned PATH)
- Allowed files/modules: NEW scripts/lib/runtime_doctor.mjs (18-precondition register, typed-check constructor, report compiler, typed refusal), NEW assets/runtime-doctor-report-v1.schema.json, NEW scripts/run_runtime_doctor.mjs (standalone CLI; exit 0 ready / 1 refused / 2 doctor broke), NEW scripts/run_runtime_doctor_tests.mjs, NEW programme/P6.7_issue_card.md. process_tree.mjs, attachment_ingress.mjs, source_identity.mjs and json_schema.mjs are imported READ-ONLY and unmodified.
- Explicitly out of scope (REMAINING P6.7, on the work order): the controller invocation itself (run_user_flow.mjs / run_excel_inflow_vnext.mjs are held by P6.1 — the exact insertion line is reported, not applied); the fault-injection breadth beyond the stage-4 recalculation boundary; a general `CANCELLED.user_or_system_stop` producer; verifying that a checkpoint actually exists on `checkpoint_required` exits; the outcome-compiler rewiring of vnext statuses (TOC.A/TOC.B precede it); registering the suite in assets/development-test-registry.json (forbidden file).
- Required integration gate: runtime-doctor 102, runtime-custody (unchanged), user-flow 14/14, portability contract, programme control — green
- Last known green commit: `e31b53e`
- Rollback plan: delete the four new files (scripts/lib/runtime_doctor.mjs, scripts/run_runtime_doctor.mjs, scripts/run_runtime_doctor_tests.mjs, assets/runtime-doctor-report-v1.schema.json) and this card; nothing else was touched, so removal is a complete revert with no behavioural change to any existing path
- Attempt count (1-3): 1
- Status: SEALED (greenfield slice; controller wiring and fault-injection breadth tracked on the work order)

## Difference classification
- Intended correction: a new report-first pre-flight layer with no production caller yet. Purely additive: no existing code path changes behaviour, and the doctor is invoked only by its own CLI and suite.
- Regression: none. The stage-4 `exactEnvironmentProbe` is untouched and remains the authority at its own boundary; the doctor is a strictly earlier, cheaper, typed check, not a replacement.
- Unresolved: the doctor is not yet CALLED by a controller, so the invariant is provable but not yet enforced on a real run — the insertion line is reported for the P6.1 owner to apply. Four preconditions are declared EXCLUDED_INSTALLED_HOST and are unsatisfiable here by construction. Two preconditions type as `unknown` on any host for want of a declared floor (no Node minimum version and no temp free-space floor exist in any shipped asset) — declared, not skipped, and both are optional so neither refuses.

## Completion receipt
- runtime-doctor 102 → 0. Four mutations each proven to bite by temporarily weakening the code and observing red: (1) preferring PATH over the resolved selection → "a poisoned PATH replaced the resolved interpreter selection"; (2) making an unsatisfied mandatory precondition non-blocking → "an unsatisfied mandatory precondition did not refuse"; (3) removing the exclusion guard → an installed-host exclusion could claim satisfied; (4) dropping `unknown` from the blocking predicate → "an UNKNOWN mandatory precondition was rounded up into a pass".
- 18 preconditions declared: 10 mandatory, 4 optional, 4 excluded-installed-host. Report closure is enforced — a precondition that is not reported on makes the compiler throw, so none can be silently skipped.
- The doctor found a REAL defect on the authoring host in under three seconds: the default `python3` satisfies openpyxl and PyMuPDF but not Pillow or numpy, so no single interpreter satisfies the declared workbook-lane closure. Verdict REFUSED, naming the missing distributions and the packages that need them — the exact condition the documented trap describes, and previously discoverable only deep inside a render.
- Refusal carries `INTERNAL.compiler_or_graph_defect` (registered) with all five registry-required internal-failure payload fields, plus a DECLARED fidelity statement: the sealed registry has no host/environment code, the code actually needed is `INTERNAL.host_precondition_unsatisfied` (owner_layer runtime_governance), and the registry was NOT edited.
- Validators validate, never repair: proven that a missing run root is reported and not created, that the temp probe removes its own scratch directory, that the source hardcodes no absolute host path, and that it neither installs nor mutates PATH.
