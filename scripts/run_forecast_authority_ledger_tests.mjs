#!/usr/bin/env node
import assert from 'node:assert/strict'; import {sealForecastAuthorityLedger,verifyForecastAuthorityLedger,buildForecastAuthorityLedger} from './lib/forecast_authority_ledger.mjs';
const periods=['2023-12-31','2024-12-31','2025-12-31','2026-12-31','2027-12-31','2028-12-31'].map((date,i)=>({date,status:i<3?'historical':'forecast'}));
const auth=(value)=>({method:'company_guidance',value,source_kind:'company_guidance',source_id:'g',material:true,status:'PASS'});
const c={case_id:'ledger',periods,statement_structure:{income_statement:[{row_id:'revenue',semantic_role:'revenue',values:[1,2,3,4,5,6],forecast_period_authorities:[auth(4),auth(5),auth(6)]}],cash_flow:[{row_id:'acq',semantic_role:'acquisitions_net_of_cash',values:[-1,-2,-3,0,0,0],forecast_period_authorities:[{method:'explicit_zero',value:0,source_kind:'historical_inference',source_id:'semantic-event-backstop:acq'},{method:'explicit_zero',value:0,source_kind:'historical_inference',source_id:'semantic-event-backstop:acq'},{method:'explicit_zero',value:0,source_kind:'historical_inference',source_id:'semantic-event-backstop:acq'}]}]}};
const l=sealForecastAuthorityLedger(c); assert.equal(l.status,'PASS'); assert.equal(l.rows.length,6); verifyForecastAuthorityLedger(c); const before=l.ledger_sha256; c.statement_structure.income_statement[0].forecast_period_authorities[0].value=99; assert.throws(()=>verifyForecastAuthorityLedger(c),/drift/); const bad=structuredClone(c); delete bad.forecast_authority_ledger; delete bad.forecast_authority_ledger_version; bad.statement_structure.cash_flow[0].forecast_period_authorities[0].method='historical_average'; assert.equal(buildForecastAuthorityLedger(bad).status,'BLOCK'); console.log(JSON.stringify({status:'PASS',checks:5,sha:before}));
