# Work Package Issue Card

- Work package ID: P4.1
- Title: Opening instrument source inventory — candidate ids, source-row provenance, crosswalk binding and a typed NOT-SELECTED register at the opening-debt boundary
- Active invariant: Every opening instrument state records WHERE it came from — the source rows considered (candidate ids), which was selected, and a NOT-SELECTED register naming every rejected candidate with a typed reason — and the selection is reproducible from the recorded provenance alone.
- Release/phase: v3.7.7 / Phase 4
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `b3dd728`; not committed per work-order constraint)
- Working tree status: P4.1 delta uncommitted (2 new files + 1 modified + this card); other agents' in-flight deltas (forecast_authority.mjs, forecast_ownership_resolver.mjs, run_period_scoring_tests.mjs) present in the shared tree and untouched
- Observed failure or discovery question: The P4 gap agent proved a source inventory existed only for the DCS lane (dcs-source-tables → dcs-candidate-manifest → dcs-crosswalk, where every candidate row and cell carries a typed disposition, a rationale and a zero-unowned-cell receipt). `compileOpeningInstrumentState` carried no source-row provenance, no candidate ids and no crosswalk hash, and there was no not-selected register anywhere at the opening-debt boundary: a register row skipped as a duplicate id, one that failed to translate, or one whose declared amount was blank simply vanished from the compiled state behind a prose string in `errors`.
- Earliest responsible layer: the opening-instrument-state compiler (`compileOpeningInstrumentState`), the boundary both coverage/release and the P4.2 bridge consume
- Reproduction command: `node scripts/run_opening_instrument_provenance_tests.mjs`
- Focused failing test ID: opening-instrument-provenance (30 checks; red proof "compileOpeningInstrumentState carries no source inventory" against the pre-change compiler; mutations: a selected instrument with no recorded source, an empty source-row list, a rejected candidate removed from the register, a candidate double-claimed by both registers, an alien rejection reason, a tampered recorded basis amount, a tampered translation rate, a re-stamped digest, a dropped field-authority rejection)
- Allowed files/modules: scripts/lib/opening_instrument_provenance.mjs (NEW: candidate-id minting, typed declared amounts, rejection classifier, field-authority precedence, sealing, replay, validator), scripts/lib/instrument_period_state.mjs (`compileOpeningInstrumentState` only — candidate universe, provenance on each row, typed not-selected register, sealed inventory; additive), scripts/run_opening_instrument_provenance_tests.mjs (NEW), this card
- Explicitly out of scope (REMAINING P4.1, on the work order): the P4.2 bridge still writes prose `source_ref` strings on its taxonomy lines instead of citing the candidate ids now available on `row.provenance.candidate_id` (opening_debt_bridge.mjs is read-only for this package — a one-line change per line kind would carry them); the instrument-period states (`state_id` minting, the per-period `translation` object) remain provenance-free because that artifact is hash-pinned into solveCase output; coverage.mjs and release_nodes.mjs echo the inventory in their diagnostics but neither validates it yet
- Required integration gate: user-flow 14/14, opening-debt-bridge 19, schedule-typed-states 13, acquisition solver case (standard-maximal-v2) PASS, funded-acquisition 16, opening-debt-reconciliation PASS, instrument-period-state PASS, ownership-census 10, economic-IR 73, row-identity 21, statement-family-residual 44, opening-instrument-provenance 30, `node --check` on all three touched files — met
- Last known green commit: `b3dd728` (baseline for this delta; the shared tree's ownership-census red is caused by another agent's uncommitted forecast_authority.mjs / forecast_ownership_resolver.mjs edits — re-verified green in a detached worktree at `b3dd728` plus only this package's three files)
- Rollback plan: revert the working-tree delta (delete scripts/lib/opening_instrument_provenance.mjs and scripts/run_opening_instrument_provenance_tests.mjs, restore scripts/lib/instrument_period_state.mjs from b3dd728, delete this card)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: `compileOpeningInstrumentState` now mints a candidate at every register position BEFORE anything is decided about it, records on each compiled row a `provenance` block (candidate id, register ordinal, source lane, source-row refs, upstream source line ids, the selected opening-balance authority, and the selection record naming the basis field, basis amount, balance-basis authority, translation method, translation rate and translation source ref), and seals a `source_inventory` (opening-instrument-provenance/1.0) carrying the candidate universe, the selected ids, a typed NOT-SELECTED register and a canonical digest — the ledger discipline of forecast_authority_ledger.mjs applied to the opening-debt register. Ten typed rejection reasons replace the silent drops. One never-zero hole closed: a `nil` or blank declared `opening_balance` used to become a real zero opening row through `Number(null) === 0`, and is now a typed `opening_balance_unresolved` rejection (unreachable for a shape-valid case — the schema requires `number, minimum 0` — but the opening state is also compiled by coverage and release gates on unvalidated cases).
- Regression: none — solveCase solution hashes byte-identical pre/post on both maintained fixtures with NO field stripped (the inventory never reaches solveCase output), and the opening state's own legacy fields (status, rows with `provenance` stripped, reporting total, error strings) are byte-identical too
- Unresolved: the bridge's taxonomy lines do not yet cite candidate ids (forbidden file here); the instrument-period-state artifact remains provenance-free; no consumer validates the inventory yet — the validator is called only by this suite

## Completion receipt
- opening-instrument-provenance 30 → 0 (red proof at the exact GAP assertion; all nine mutations caught; the replay is handed the inventory and nothing else and reproduces every row's instrument id, basis amount, translation rate, gross-debt membership and the reporting total)
- Numeric identity: solveCase(standard-maximal-v2) `98572a39b18a9cc598f168a15a5af5680b148b80425603e55bbd675ad563c337` and solveCase(standard-net-cash-v2) `b01b3b63690a5feb4deca0672580df5b3a2a7af8a6301db9902a3b4ce96ba7da` identical pre/post, unstripped; opening-state legacy-field digests `fa184b17…` / `b46a7b72…` identical pre/post with `source_inventory` and `rows[*].provenance` stripped (P4.2 technique)
- Not-selected reason vocabulary (10, sealed in `OPENING_INSTRUMENT_NOT_SELECTED_REASONS`): duplicate_instrument_id, instrument_record_malformed, balance_basis_not_declared, opening_balance_unresolved, opening_balance_negative, translation_authority_absent, translation_rate_not_positive, superseded_by_declared_precedence, forecast_boundary_absent, unresolved_compile_defect (the typed fallback that stops a NEW throw becoming an untyped silent drop)
- No new terminal reason code needed: the inventory is additive and never refuses a solve; the opening-debt boundary's refusal remains P4.2's `SOURCE.opening_debt_unresolved`
