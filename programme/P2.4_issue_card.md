# Work Package Issue Card

- Work package ID: P2.4
- Title: Required-role closure artifact sealed before forecast compilation
- Active invariant: A required-role CLOSURE artifact is compiled and sealed BEFORE forecast compilation begins — it enumerates every required semantic role from the declared taxonomy (never a hardcoded subset), records for each whether it is present/absent/aliased with provenance, honours `role_aliases` and the canonical alias table, refuses in typed form when a required role is missing, and enforces role uniqueness over the FULL required set rather than the 14-entry literal.
- Release/phase: v3.7.7 / Phase 2
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `85ac6f2` (working tree, NOT committed)
- Working tree status: dirty — this package's files only (`scripts/lib/case_compiler.mjs`, `scripts/lib/coverage.mjs` modified; `scripts/lib/required_role_closure.mjs`, `assets/required-role-closure-v1.schema.json`, `scripts/run_required_role_closure_tests.mjs`, this card new). Other agents' concurrent edits to `experience_trace.mjs`, `formula_dsl.mjs`, `performance_receipt.mjs`, `run_excel_inflow_vnext.mjs`, `formula_ast.mjs`, `case_generator.mjs` and `test-fixtures/archetypes/**` are present in the same worktree and are NOT part of this package.
- Observed failure or discovery question: the P2 gap agent proved there was no required-role closure anywhere before `compileForecastPlan`. Reproduced directly: with the required cash-flow role `capex` unmapped, `compileCase` minted 36 rows of `forecast_period_authorities`, sealed `forecast_authority_ledger` and `ownership_census`, and emitted exactly ONE finding — `coverage.statement_role.cash_flow.capex` — from the Phase-H battery that runs after the forecast lane closed. Uniqueness was enforced only by the 14-entry `UNIQUE_VISIBLE_ROLES` literal in `statement_topology.mjs:16`, which reads `node.semantic_role` alone: 11 of the 25 contract-required roles were outside it (`depreciation_and_amortisation`, `cash_flow_net_income`, `change_in_working_capital`, `capex`, `debt_issuance`, `debt_repayment`, `dividends`, `fx_effect_on_cash`, `reported_ebitda`, `net_interest_expense`, `other_non_operating`), and even for `ebit` — a role the literal DID contain — a duplicate authority declared through `role_aliases` produced 0 `DUPLICATE_VISIBLE_AUTHORITY` errors and 0 coverage blockers.
- Earliest responsible layer: `case_compiler.mjs` Phase F/G boundary (no pre-forecast role closure) + the role-uniqueness set (`statement_topology.mjs` literal, alias-blind)
- Reproduction command: `node scripts/run_required_role_closure_tests.mjs`
- Focused failing test ID: required-role-closure (38 checks; three mutation families — a missing required role must refuse, an alias must resolve, a duplicate over the FULL set must be caught — plus the seal, drift, waiver, `one_of` group and non-mutation pins)
- Allowed files/modules: NEW `scripts/lib/required_role_closure.mjs` (pure closure compiler: reads only `assets/production-contract-v2.json` + `assets/statement-semantic-taxonomy.v1.json`, never mutates the case), NEW `assets/required-role-closure-v1.schema.json`, `scripts/lib/case_compiler.mjs` (the Phase F.9 mint + seal at :3840-3906, before the `compileForecastPlan` call at :3919; artifact attached to the compile report at :4266), `scripts/lib/coverage.mjs` (`requiredRoleClosureChecks` at :399, appended after the untouched `requiredRoleChecks` at :2366), NEW `scripts/run_required_role_closure_tests.mjs`, this card
- Explicitly out of scope: `statement_topology.mjs`'s `UNIQUE_VISIBLE_ROLES` literal is left in place unchanged (never weakened — the closure is an additional, wider gate); registering the suite in `assets/development-test-registry.json` (forbidden file this package); the `net_interest_expense` contract/taxonomy vocabulary gap (recorded as a WARN note on the artifact, not fixed here); `model_ir_v3.mjs` (P2.5)
- Required integration gate: user-flow 14/14, filings 43, classification-resolution 18, policy-registry 286, statement-classifier (maximal), completion-census-scope 66, required-role-closure 38, `node --check` on every touched file — all green; portable equivalence over `test-fixtures/cases` 0/0/0/0/0; full custody-cohort equivalence UNCHANGED at the pinned KNOWN RED 55/90/1/0/0
- Last known green commit: `85ac6f2`
- Rollback plan: delete the three new files and revert the two hunks in `case_compiler.mjs` (import at :42, Phase F.9 block, report field) and the two hunks in `coverage.mjs` (import at :16, `requiredRoleClosureChecks` + its one call site)
- Attempt count (1-3): 1
- Status: COMPLETE (uncommitted, per work order)

## Difference classification
- Intended correction: a sealed, schema-validated closure artifact minted before the forecast lane opens; typed refusal codes (`REQUIRED_ROLE_ABSENT`, `REQUIRED_ROLE_GROUP_UNSATISFIED`, `REQUIRED_ROLE_DUPLICATE_AUTHORITY`) surfaced as BLOCK findings ahead of Phase G; uniqueness widened from 14 alias-blind roles to the full 25-role contract-required set, honouring `role_aliases` and `SEMANTIC_ROLE_ALIASES`
- Regression: none. Every pre-existing check still runs and still blocks (the suite pins that `coverage.statement_role.cash_flow.capex` survives alongside the new refusal); both certified cases compile with 0 blocks; the custody cohort's pinned totals are byte-identical
- Unresolved: the closure does not gate execution (the compiler still completes the report whole, matching `compileStructuralOwnershipPreflight` above it) — an early-exit outcome belongs to the Phase-6 outcome compiler; `net_interest_expense` is required by the production contract but absent from the semantic taxonomy; the suite is not yet in the development test registry

## Completion receipt
- required-role-closure 38 → 0. Mutation validity proved in a detached worktree at the branch tip: the unwired tree fails `closure-mint-precedes-forecast-compilation-in-source`; narrowing the uniqueness set back to the 14-role literal fails `duplicate-outside-the-legacy-literal-is-caught`; dropping `role_aliases` fails `alias-resolves-a-required-role`; suppressing the absence refusal fails `missing-required-role-refuses`
- Closure covers 49 declared role/section entries, of which 25 are contract-required (vs the 14-entry legacy literal); certified cases seal `status: PASS` with 22 present, 3 `GROUP_SATISFIED_ELSEWHERE`, 0 refusals, 0 violations
- `run_ownership_census_tests.mjs` is red at the clean branch tip `85ac6f2` before any of this package's files are applied (verified in a detached worktree) — pre-existing, and its FAMILIES map at :107 already declares `lib/required_role_closure.mjs` as this package's canonical writer
