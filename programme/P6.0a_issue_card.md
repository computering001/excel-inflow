# Work Package Issue Card

- Work package ID: P6.0a
- Title: Terminal-outcome crash paths (Phase 6 carry-in: TOC.D crossing 1 + TOC.C vnext terminal catch)
- Active invariant: No controller path terminates in an uncaught throw or an unlawful category crossing — an internal decision-graph failure surfaces as an internal-owned status (never user-owned ACTION_REQUIRED), and the OUTER controller serialises a typed internal-failure payload on its terminal catch exactly as the delegate already does (P3.7 pattern), with one typed summary line — never a stack — on the public stderr.
- Release/phase: v3.7.7 / Phase 6 carry-in
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` @ `c36b56c` (uncommitted working-tree repair)
- Working tree status: modified `scripts/run_user_flow.mjs`, `scripts/run_excel_inflow_vnext.mjs`; new `scripts/run_terminal_crash_path_tests.mjs`; NOT committed (per work order)
- Observed failure or discovery question: (1) GUARANTEED CRASH — run_user_flow.mjs minted `ACTION_REQUIRED` for `decision_replay_blocked`/`decision_graph_blocked` while `blockerForStoppedOutcome` owns them as `INTERNAL_WORK`; the workflow-state contract admits ACTION_REQUIRED only with USER_DECISION, so `assertWorkflowState` threw uncaught on EVERY decision-graph failure (`user_flow.ACTION_REQUIRED cannot own blocker class "INTERNAL_WORK"`). (2) `run_excel_inflow_vnext.mjs` main().catch printed a raw stack to public stderr, wrote no internal-failure.json, and the progress-heartbeat interval kept the process alive indefinitely (the run had to be killed; it never exited 1).
- Earliest responsible layer: the delegate decisions-stage stop mint (run_user_flow.mjs, stoppedStage === "decisions" branch) + the outer terminal catch (run_excel_inflow_vnext.mjs main().catch)
- Reproduction command: `node scripts/run_terminal_crash_path_tests.mjs`
- Focused failing test ID: terminal-crash-paths (25 checks incl. an END-TO-END drive of run_excel_inflow_vnext into a real early internal failure reading the serialised artifact, and the decision-graph lawful-mint proof against the live contract)
- Allowed files/modules: run_user_flow.mjs (the decisions-stage mint branch only), run_excel_inflow_vnext.mjs (the terminal catch only), the new suite, this card. Contract, validators, assets, registries, exit inventory: UNCHANGED (validators validate, never repair).
- Explicitly out of scope (REMAINING, on the Phase 6 work orders): P6.7's outcome-compiler rewiring of vnext statuses onto the registry (the typed context attached here is its input); TOC.D crossings (2) and (3); the delegate's own full-stack stderr print at its terminal catch (TOC.F sibling); internal-failure.json consumers.
- Required integration gate: user-flow 14/14, controller-exit-inventory 64 (finish counts pinned 18 delegate / 9 vnext), internal-outcomes 16, workflow-state 24/24, terminal-crash-paths 25, `node --check` on both controllers — green
- Last known green commit: `c36b56c` (tree before this repair; the crash paths were red by construction, all suites green)
- Rollback plan: revert the two working-tree edits and delete the new suite + this card
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: the internal decision-graph outcomes now mint the contract-lawful internal-owned state `BLOCKED` + `blocker_class INTERNAL_WORK` (user_blocking false by derivation; constitution binding equation_system_unsolved/equation_graph attaches) carrying `typed_internal_outcome` (INTERNAL.equation_system_unsolved + replay/plan detail) for P6.7 to consume; the stage receipt records `blocked`, not `action_required`. NOTE — deviation from the work-order wording: the order named status `NEEDS_INTERNAL_WORK`, but the user_flow contract layer declares no such state (it exists only in the attachment/vnext layers) and the contract is both correct and frozen; minting it would merely swap one uncaught `assertWorkflowState` throw for another. `BLOCKED`+`INTERNAL_WORK` is the only lawful internal-owned pairing at this layer, and at the public boundary the failing delegate (exit 1) lands in the vnext controller's EXISTING `NEEDS_INTERNAL_WORK` mint — so the order's public-surface intent is met without touching contract, schema or exit inventory. The vnext terminal catch now mirrors the delegate's P3.7 catch: internal-failure.json (schema excel-inflow-internal-failure/1.0) with all five registry payload fields into the resolvable `--out`, honoring `error.typed_internal_outcome`, defaulting to INTERNAL.compiler_or_graph_defect; the stack goes into the artifact; public stderr gets exactly one typed summary line; the heartbeat is stopped so the process actually exits 1.
- Regression: none — finish counts unchanged (18/9), exit-inventory signatures intact (the ASK exit at the decisions site remains live for genuinely user-owned outcomes), public schema and exit inventory untouched, no validator weakened.
- Unresolved: P6.7 rewiring; the delegate parent still maps delegate exit≠0 to NEEDS_INTERNAL_WORK by exit code and discards the typed code (TOC.C, declared); delegate terminal catch still prints its stack (TOC.F sibling site).

## Completion receipt
- terminal-crash-paths 25 → 0 (both former reds proven still-illegal at the contract, repaired mint lawful, end-to-end vnext internal-failure.json with all five fields, exit 1 without hang, one-line typed stderr with zero stack frames)
- Gate: user-flow 14/14; exit-inventory 64; internal-outcomes 16; workflow-state 24/24; both controllers `node --check` clean
