# Work Package Issue Card

- Work package ID: P8.6a
- Title: Declare the release rollback procedure and the append-only release journal (PORTABLE HALF; installed half permanently EXCLUDED)
- Active invariant: A release rollback is a DECLARED, executable, evidence-preserving procedure — a versioned policy asset names the trigger conditions, the human authorisation, the exact package identity rolled back TO, the in-flight-run package-pinning rule and the evidence that must be preserved; every release-affecting event is an append-only hash-chained release-journal record; and a rollback target that is not attestable is refused, never assumed.
- Release/phase: v3.7.7 / Phase 8
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: shared worktree with concurrent packages; these files are disjoint from them
- Observed failure or discovery question: the Phase-8 work order proved `rollback` existed only as vocabulary — the bare `DEPLOYMENT_STATUSES` enum member (identity_vocabulary.mjs:13), two authority-projection-override notes, and the issue-card linter's mandatory field. Nothing declared what triggers a rollback, who authorises one, which package identity you return TO, what happens to runs in flight, or what evidence must survive; nothing recorded that a release had ever been compiled, promoted or withdrawn. `ReleaseCheckpointStore` was the nearest thing and it is per-RUN resumability, not release rollback — the conflation this package exists to close.
- Earliest responsible layer: release governance assets + a release-journal library; there is no earlier layer because the procedure did not exist at any layer
- Reproduction command: `node scripts/run_release_rollback_policy_tests.mjs`
- Focused failing test ID: release-rollback-policy (159 checks; red in two stages before the package — first `ERR_MODULE_NOT_FOUND` for scripts/lib/release_journal.mjs, then `Release rollback policy is absent at assets/release-rollback-policy-v1.json. A release rollback is undeclared until the policy asset exists.`)
- Allowed files/modules: NEW assets/release-rollback-policy-v1.json + .schema.json, NEW assets/release-journal-v1.schema.json, NEW scripts/lib/release_journal.mjs, NEW scripts/run_release_rollback_policy_tests.mjs, NEW references/release-rollback-runbook.md, this card. No existing file touched.
- Explicitly out of scope (permanently EXCLUDED, declared in the asset as EXCLUDED_INSTALLED_HOST): `active_pointer_repoint`, `installed_identity_readback`, `post_rollback_installed_parity` — the Rogo installed-host leg. Also out of scope for this package: deployment automation of any kind, the deployment-profile allowlist entries the three new assets need, and the development-test-registry entry that registers this suite in CI.
- Required integration gate: this suite (159), source-identity 22, release-identity-governance, runtime-closure 22, programme-control 86 (was 84; the +2 is concurrent packages' cards, proven by running the linter with and without this card), `node --check` on every touched file — green
- Last known green commit: `d627849`
- Rollback plan: delete the six new files and this card; nothing existing was modified, so removal is total and leaves no residue
- Attempt count (1-3): 1
- Status: SEALED (portable half; installed half permanently excluded, not deferred)

## Difference classification
- Intended correction: greenfield declaration. The policy binds to the EXISTING vocabularies rather than minting new ones — `DEPLOYMENT_STATUSES`/`PACKAGE_MODES` for statuses and modes, `product-identity/2.0` for the rollback target, `verifyReleasePackageAttestation` for target attestability, and the already-registered `CANCELLED.user_or_system_stop` for a rollback-stopped run (no new terminal reason code; the registry is untouched). The clause set and which clauses are installed-host-excluded live in CODE (`ROLLBACK_POLICY_CLAUSE_CONTRACT`), so the asset can neither drop a clause nor claim an excluded one is satisfied.
- Regression: none — no existing file was modified and nothing imports the new module yet.
- Unresolved: the journal is not yet WRITTEN by anything (compile/tag/promote do not append records — that is P8.8's tag+seal wiring); the three new assets are not in the deployment-profile allowlists, so the compiler does not ship them yet; the suite is not in assets/development-test-registry.json, so CI does not run it yet. Separately observed and not fixed here: the programme-control card linter's `^P\d+\.\d+_issue_card\.md$` pattern silently skips every letter-suffixed card (P3.6a, P5.1a, P6.0a, P7.6a, P8.2a and this one), so those cards are never actually linted.

## Completion receipt
- release-rollback-policy 159 → 0. Policy: 12 clauses, 9 portable + 3 EXCLUDED_INSTALLED_HOST, 7 declared trigger conditions, 5 never-delete evidence classes, 1 retained previous-known-good package.
- Mutations refused: 9 policy mutations (missing clause; no trigger; an excluded clause claiming portability; a status outside DEPLOYMENT_STATUSES; retention with no count; a minted terminal reason code; automation authorising; attestation waived; in-flight runs re-pointed) and 17 journal mutations (missing/stray rollback payload; wrong withdrawn status; undeclared trigger; failed target attestation; rollback with no earlier retain; untyped stop; invented disposition; target == withdrawn; re-promotion of a withdrawn identity; foreign policy digest; out-of-chain record; sequence gap; edited sealed record; reorder; fork; backdating; missing genesis).
- Append-only proven on disk, not asserted: after a second append the first record's bytes are byte-identical; an append onto a tampered journal is refused rather than repaired; a rollback with an unattestable target (or none supplied) never reaches the file.
- Attestability proven against real bytes: a temporary package is inventoried, archived deterministically and attested, then accepted as a target; the same package mutated after attestation, and a target bound to a different closure, are both refused.
