# Work Package Issue Card

- Work package ID: P4.5
- Title: Formula AST — typed expression tree, deterministic A1 renderer, computed `ast_depth`, typed refusal for the four operators with no emitter (AST-LAYER SLICE)
- Active invariant: Workbook formulas are built from a typed expression AST (ref / range / literal / unary / binary / call / conditional / group), the AST renders deterministically to A1 text through a single function, `ast_depth` is COMPUTED from the tree rather than declared, and every declared DSL operator is either rendered from a tree or refused with a typed refusal naming the missing emitter — never silently mis-rendered.
- Release/phase: v3.7.7 / Phase 4
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `85ac6f2`; slice delivered uncommitted per work order)
- Working tree status: dirty — this slice plus three other agents' in-flight slices (generated-case cohort/`case_generator.mjs`, archetype economics fixtures, performance-receipt honesty); this slice touches only its allowed files
- Observed failure or discovery question: GAP verdict — no Formula AST. `formula_dsl.mjs` assembled Excel text by string concatenation, one flat level per operator (`refs → A1` at :186-191, then a 16-arm `switch` of template literals), `formulaRuleComplexity` returned `ast_depth: 1` unconditionally for every formula including the seven-level historical trend, the declared `max_ast_depth: 4` was never consulted by `within_budget`, and four of the twenty declared operators (`average_balance_interest`, `roll_forward`, `scenario_overlay`, `toggle_gate`) fell through to an untyped `default:` throw carrying no reason code, no arity and no repair. One production call site against 264 raw template-literal formulas in `build_dynamic_model.mjs`.
- Earliest responsible layer: scripts/lib/formula_dsl.mjs — the module that owns the only closed formula vocabulary; there was no expression layer beneath it to own the syntax
- Reproduction command: `node scripts/run_formula_ast_tests.mjs`
- Focused failing test ID: formula-ast (33 checks; red pre-repair on the absent `scripts/lib/formula_ast.mjs`, and substantively red on the DSL as it stood — `historical_trend` emitted `=F11+((E11-D11)+(F11-E11))/2*3` while reporting `ast_depth: 1`, and all four unimplemented operators threw a bare `Error` with `typed_refusal` undefined)
- Allowed files/modules: scripts/lib/formula_dsl.mjs (operators now build trees; `compactA1ReferenceAsts` / `sumExpressionAst` / `compileStatementFormulaAst` / `formulaOperatorSupport` / `SUPPORTED_FORMULA_OPERATORS` / `UNSUPPORTED_FORMULA_OPERATORS` / `UnsupportedFormulaOperatorError` added; `compileStatementFormula` retained unchanged in shape and delegating), NEW scripts/lib/formula_ast.mjs (node vocabulary, renderer, `astDepth`, `astReferences`, `verifyFormulaAst`), NEW scripts/run_formula_ast_tests.mjs, NEW programme/P4.5_issue_card.md
- Explicitly out of scope (REMAINING P4.5, on the work order): migrating the 264 raw template-literal formulas in `build_dynamic_model.mjs` onto the AST (inventoried below — the file is forbidden to this slice); registering the new suite in `assets/development-test-registry.json` (assets forbidden to this slice; `run_ci_census_tests.mjs` is consequently red on it, alongside the three concurrent agents' unregistered suites); extending `FORMULA_FUNCTIONS` with the `AND` / `OR` / `COUNT` the raw literals also use; no decomposition of `build_dynamic_model.mjs` or `row_plan.mjs` was attempted
- Required integration gate: formula-ast 33; user-flow 14/14; layered-graph constitution (standard-maximal-v2) 11 positive checks / 8 mutations caught / 0 violations; acquisition-plan-purity 21 (it reads formula text); workbook + plan byte identity pre/post (plan-only build + `python3 -m emit build`, both fixtures forced to `execution_profile: reference_parity` as the archived fixtures require) — standard-maximal-v2 xlsx `ce465d37…b69330` / plan.json `7ccbdf7e…ec4e1` and standard-net-cash-v2 xlsx `7e0d4b79…b64564` / plan.json `1c16a68c…59a2`, all four `cmp`-identical pre/post; `node --check` on the three touched files — all green
- Last known green commit: `85ac6f2`
- Rollback plan: `git checkout -- scripts/lib/formula_dsl.mjs` and delete the three new files (slice is uncommitted)
- Attempt count (1-3): 1
- Status: DELIVERED (slice; remainder tracked on the work order)

## Difference classification
- Intended correction: every statement operator now builds a typed tree and the Excel text is produced only by `renderExpression`, which inserts exactly the parentheses precedence demands; where a formula's meaning carries grouping precedence would flatten (the paired period-over-period deltas of `historical_trend`) the tree says so with an explicit `group` node, so parentheses have two declared sources and no third. `ast_depth` is measured on that tree (1 for `link`, 2 for `subtract`/`multiply`/`average`, 3 for `ratio`/`historical_average`, 4 for `tax`/`growth`/`negated_ratio`/`prior_period_scaled_by`, 7 for `historical_trend`, 0 where a rule emits no formula) and `max_ast_depth` is raised from the decorative 4 to an enforced 8 — enforced because chained associative operators now render as one flat n-ary level, so depth no longer moves with the reference count. The four operators with no statement emitter are refused with `UnsupportedFormulaOperatorError` carrying `{operator, reason_code: formula_operator_requires_schedule_emitter, declared_arity, emitter, repair}` while keeping the exact message the pre-AST `default:` threw; an import-time check fails the module if any declared operator is neither rendered nor refused, so the fall-through cannot return.
- Regression: none — the emitted workbook and plan bytes are hash-identical pre/post on both certified fixtures, and the shipped formula text is proven byte-identical shape-by-shape against a frozen verbatim copy of the pre-AST emitter over a 65-shape matrix (every supported operator at its arity extremes, contiguous and scattered reference runs, with and without a prior column, with and without `ROUND`).
- Unresolved: the 264 raw literals and the registry entry (out-of-scope list above). The single production call site (`build_dynamic_model.mjs:96`/`:1252`) is in a file forbidden to this slice, so it was migrated by delegation rather than rewiring: it still imports `compileStatementFormula`, which is now a two-line wrapper over `compileStatementFormulaAst`. No wiring change is required for correctness; switching the call site to `compileStatementFormulaAst({...}).formula` would be cosmetic and behaviourally identical.

## Completion receipt
- formula-ast 33 → 0 (closed eight-kind node vocabulary and closed function/operator sets, four constructor-level refusals; precedence and declared-group rendering; determinism; computed depth on the seven-level trend and per-operator depth variance; the declared depth budget proven to actually bound the emitted trees and to reject an over-deep one; 65-shape render equivalence against the frozen emitter with `formula === renderFormula(ast)` asserted on every shape; compaction/`canonicalSumExpression` parity over 11 reference patterns including opaque pass-through terms; the four typed refusals with preserved messages; total operator classification with `unknown` for undeclared; and four mutations — a flipped chain operator, a swapped reference, a dropped declared group, and three structurally invalid nodes — every one caught, not rendered)
- Byte identity: pre/post `sha256` equal and `cmp`-identical for both fixtures' `.xlsx` and `.plan.json` (hashes above). The gate has teeth: with range compaction temporarily disabled in the renderer — a formula-text-only change that leaves every value identical — all four hashes moved (`6fc1c48e…`, `03a7daad…`, `f753260…`, `39d94a61…`), confirming the DSL's rendered text reaches both the plan and the workbook. The plan carries 1458 formula cells on the maximal fixture, including 12 `ROUND(SUM(` cells reachable only through this module's `roundSumDigits` path.
- Raw-template-literal inventory for the follow-on package (`scripts/build_dynamic_model.mjs`, 264 lines carrying 266 backtick-`=` formulas; `scripts/lib/row_plan.mjs` carries none):

| count | line span | enclosing function |
| --- | --- | --- |
| 225 | 3744-8710 | `configureOperatingModel` |
| 10 | 2073-2636 | `buildBrokersSheet` |
| 7 | 872-1029 | `applyConditionalState` |
| 7 | 3282-3334 | `acquisitionAdjustmentFormula` |
| 4 | 10821-11201 | `solverFormulaCaches` |
| 3 | 10081-10096 | `patchWorkbookProperties` |
| 1 each | 555, 654, 1831, 2847, 2860, 3056, 9928, 10308, 10749, 11975 | `sumCellFormula`, `setFormula`, `brokerEvidenceFormula`, `brokerLink`, `signedBrokerLink`, `acquisitionRatioRowFormula`, `attachInputProvenance`, `forecastInput`, `statementSolverValues`, `applyPlanChrome` |

  Excel functions those literals use: `IF` 36, `SUM` 14, `AVERAGE` 13, `MAX` 12, `IFERROR` 10, `ABS` 3, `MIN` 2, `OR` 1, `COUNT` 1, `AND` 1. `FORMULA_FUNCTIONS` already covers all but `AND`, `OR`, `COUNT` — three entries in the closed list. The 225 in `configureOperatingModel` are the whole cost of the follow-on; the other 39 are cheap and independent.
- Registry entry the follow-on (or the parent) must add to `assets/development-test-registry.json` — assets are forbidden to this slice, and `run_ci_census_tests.mjs` fails until it lands:
  `{ "id": "formula-ast", "phase": "proof", "runtime": "node", "script": "run_formula_ast_tests.mjs", "timeout_seconds": 300, "mutates_product_tree": false, "test_class": "mutation" }`
