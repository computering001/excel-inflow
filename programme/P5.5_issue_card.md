# Work Package Issue Card

- Work package ID: P5.5
- Title: Physical authority and capture mutations — the claim is proven against the emitted CELL, not against a receipt the same build wrote
- Active invariant: An authority or capture claim is proven against the EMITTED WORKBOOK — mutating the physical cell (its value, its formula or its provenance marking) must be caught — not merely against a JSON receipt that the same build produced; and every mutated address is DERIVED from the emitted artifact, never written down.
- Release/phase: v3.7.7 / Phase 5
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `b92eeee`; slice delivered uncommitted per work order)
- Working tree status: dirty — this slice plus four other agents' in-flight slices (P5.8 rendered-geometry pairing, bounded parallelism, metamorphic relations, differential invalidation) sharing the worktree; this slice touches only its allowed files. The shared worktree moved underneath this package once mid-run (a transient stash/restore cycle by another agent briefly removed the untracked files); every gate below was re-run afterwards against the restored tree.
- Observed failure or discovery question: GAP verdict — "authority and capture mutations edit JSON receipts not cells; capture has no adversarial workbook mutation; 5 of 15 matrix domains are toy string comparisons yet report detection 1.0; one hardcoded-address representative." Proved mechanically before any code was written. `verify/emitted_candidate_artifact_oracle.py:198` (`inspect_forecast_ownership`) takes a RECEIPT PATH as its only argument and never opens a workbook; its three codes are keyed off `display_id`/`forecast_index`/`captured_by` read back out of `<workbook>.forecast-receipt.json`, and the incumbent mutation for the `forecast_ownership` domain (`run_emitted_candidate_independent_oracle_tests.py:238`) appends a duplicate entry to that JSON. `inspect_protected_cash_and_rcf` at `:213` opens the workbook only for `protected_formula_identities`; its capture leg at `:225`–`:231` reads `contract["semantic_scope_captures"]` and compares the child labels with the literal set `{"RCF draw", "RCF repayment"}`, and the incumbent mutation for `rcf_financing_cash` (`:232`) deletes the `RCF draw` entry from the build's proof contract. THE RED PROOF, run before the oracle existed: give the captured `RCF draw` row a live formula in the emitted `.xlsx` (`Operating Model!J58 = "=J115"`) and `inspect_protected_cash_and_rcf` returns `{'protected_cash_periods_checked': 15, 'rcf_captures_checked': 2}` with `findings: []`, and `inspect_forecast_ownership` returns `findings: []` — while the SAME claims mutated in the JSON return `['RCF_CAPTURE_CONTRACT', 'RCF_FINANCING_INCLUSION']` and `['FORECAST_OWNERSHIP_DUPLICATE_WRITER']` immediately. The two capture channels are therefore proven only against documents the build wrote, and the workbook's four captured child rows (56–59 × J/K/L, twelve cells) carry no adversarial mutation at all. The fifth defect: `run_critical_invariant_oracle_matrix_tests.py:31`'s `detected()` decided `workbook_error_scan` by scanning a three-entry Python dict (`{"A1": 1, "B2": "=A1", "Z999": "#VALUE!"}`) — a toy that would report a full kill rate on a workbook it never opened.
- Earliest responsible layer: the mutation layer itself — for these two domains the artifact under mutation was a build receipt, so no defect in the emitted package could be reached by any of their mutations.
- Reproduction command: `/Users/archiepreston/Documents/Codex/excel-inflow-venv/bin/python scripts/run_physical_authority_mutation_tests.py test-fixtures/cases/standard-maximal-v2.json --out <scratch>`
- Focused failing test ID: physical-authority-mutation (60 checks; red pre-repair on the absent `verify/physical_authority_mutation_oracle` module, then red on the red-proof group itself until the physical oracle existed to catch the cell the two incumbent inspectors cannot see); critical-invariant-independent-oracle-matrix (strengthened `validate_matrix` rejected the four unproven domains until each carried a stated reason, and rejected the new emitted-cell scope until the physical suite was bound and driven)
- Allowed files/modules: NEW scripts/verify/physical_authority_mutation_oracle.py (a JUDGE, not a reader — it imports P5.4's `reconstruct_plan` and parses no XML of its own; it lives under `scripts/verify` solely so the shared independence AST scan covers it), NEW scripts/run_physical_authority_mutation_tests.py (registered-suite candidate), scripts/run_critical_invariant_oracle_matrix_tests.py, assets/critical-invariant-oracle-matrix-v1.json (NOT `registry_sha256`; additive edits only, and P5.8's concurrently-added `rendered_geometry_scope` block preserved byte-for-byte), NEW programme/P5.5_issue_card.md
- Explicitly out of scope (REMAINING P5.5, on the work order): REPAIRING the two incumbent inspectors in `verify/emitted_candidate_artifact_oracle.py` and re-pointing their mutations in `run_emitted_candidate_independent_oracle_tests.py` (both files are forbidden here — the blind spot is instead PINNED by this suite's red proof, which fails the moment either incumbent is repaired and forces the two authorities to be reconciled rather than duplicated); DE-HARDCODING the incumbent's own literals (`J152`/`$D73` for interest lineage, `=J53+J54` for protected cash, `D5`/`D10`/`D11`/`D18`/`J18` for the consensus block) — same forbidden file, so the interest-lineage literal is instead extracted from the incumbent's source and pinned against the emitted cell it means; registering `run_physical_authority_mutation_tests.py` in the development-test registry and giving it a CI census disposition (registry and `run_ci_census_tests.mjs` are forbidden — the matrix asset therefore binds the suite by PATH and carries `registry_status: PENDING_REGISTRATION`, which `validate_matrix` refuses to keep claiming once the registry names the script); the three domains that remain synthetic for corpus reasons (see below) and `broker_period`, which needs a demand ledger emitted beside the workbook.
- Required integration gate: physical-authority-mutation 60 checks PASS (23 cell mutations, 19 receipt-blind red-proof and derivation checks, 0 failures); critical-invariant oracle matrix PASS with COMPUTED rates (31/31 mutations, up from 22/22; 9 governance mutations, up from 5; 4 NOT_INDEPENDENTLY_PROVEN domains, down from 5, each carrying a stated reason); plan-reconstruction 153 PASS; emitted-candidate independent oracle PASS (19/19 mutations, 26 `verify/*.py` scanned including the new oracle, zero production imports anywhere); `node scripts/run_user_flow_tests.mjs` 14/14; both certified workbooks byte-identical before and after; `py_compile` clean on all three touched Python files. `node scripts/run_ci_census_tests.mjs` RED as expected on `run_physical_authority_mutation_tests.py` (and on four other agents' in-flight scripts); `node scripts/run_mutation_adequacy_tests.mjs` RED before and independently of this slice (`the artifact claims 71 mutation-class suites; the registry declares 76`, measured while both `assets/development-test-registry.json` and `ci/` were still pristine at HEAD; another agent has since edited the registry to 77 mutation suites, so the count moves but the mismatch is not this package's and no allowed file of this package feeds it).
- Concurrency note on the matrix gate: `assets/development-test-registry.json` is being edited by another agent in this shared worktree and its digest moved twice while this package ran (HEAD `edcc409d…` -> `4f0615a8…` -> `331e8ad4…`). `bindings.registry_sha256` is the parent's field, not this package's, so it was left alone; in the shared tree the matrix therefore stops at `assert candidate["bindings"]["registry_sha256"] == sha256(REGISTRY_PATH)`. Proven to be the ONLY obstruction: in an isolated copy of the tree with that single field rebound to the current registry digest and NOTHING else changed, the matrix is PASS — 31/31 mutations, 9 governance mutations, 23/23 physical cell mutations, four unproven domains each with a reason. Rebinding that one field is the parent's call.
- Last known green commit: `b92eeee`
- Rollback plan: delete the two new scripts and this card, and `git checkout --` the matrix asset and the matrix oracle (the slice is uncommitted)
- Attempt count (1-3): 1
- Status: DELIVERED (slice; registration, the incumbent repair and three corpus-blocked domains tracked on the work order)

## Difference classification
- Intended correction: the two receipt-scope claims are now ALSO proven against cells, by a judge whose observation is the `.xlsx` package and whose expectation is the authority record. CAPTURE: for every declared `semantic_scope_captures` entry, the declared parent and child rows must physically carry the declared labels, the child cell must be physically silent in every declared forecast column, the parent cell must carry a formula, and the captured scope must reach the statement through the protected identity whose `member_rows` contain the capture's parent row — four codes, killed by four mutation families. AUTHORITY: every forecast-ledger entry that resolves to a unique emitted row is answered by its cell — a `CapturedBy` record's cell holds nothing, a live record's cell is non-empty, a `Derived`/`ScheduleLink` record's cell carries a formula, a `BrokerInput` record's cell carries a cross-sheet reference AND the green mark that claims one — five codes, killed by four mutations, with a per-authority vacuity guard so a rule that stops being exercised is a finding rather than a silence. ADDRESSES ARE DERIVED: rows come from a UNIQUE match of the record's label in the emitted label column (108 of 138 entries resolve; 27 ambiguous and 3 absent labels are counted and refused rather than guessed), columns from the declared forecast columns, capture rows from the capture record verified against the emitted labels, and the absorbing identity from the one whose member rows contain the capture parent — and the harness AST-scans its own source to prove no A1 address is written down in it. `workbook_error_scan` is PROMOTED out of the synthetic scope: its toy three-entry dict is deleted and the scan now reads all 6,822 cells of all three sheets out of the file, killed by a genuine `t="e"` error injected into the remotest populated cell of every sheet plus a cell that merely READS as an error.
- Regression: none. The matrix's mutation count rises 22 → 31 and its governance mutations 5 → 9; no existing check is removed or weakened; the artifact-scope full-kill assertion is EXTENDED to the new emitted-cell scope rather than relaxed; the two incumbent inspectors are untouched and still run. Both certified workbooks are byte-identical before and after (`standard-maximal-v2` xlsx `ce89b6db…`, plan `a5d058dc…`; `standard-net-cash-v2` xlsx `3605f07a…`, plan `80d05061…` — the same digests P5.4 recorded), which is expected because this slice touches no emission code at all.
- Unresolved: three of the four remaining synthetic domains are blocked by the CORPUS, not by the method, and the matrix now says so in `unproven_reason` — `ebitda_basis` (the representative emits no impairment line: zero cells on any sheet carry an impairment label and the case text never names one, so there is no cell whose inclusion in the D&A addback could be mutated), `typed_unresolved_state` (the build reports `unresolved_caches: 0` and all 138 ledger entries are `status: resolved`, so no cell holds an unresolved state), and `instrument_identity` (the debt schedule prints the revolver's FACILITY CAPACITY, 2500, not its opening balance, 725, and one instrument's opening balance, 312.5, collides with an unrelated acquisition cell — so the only unambiguous id-to-balance binding is the build's own row map, which is exactly the forward expectation P5.4 exists not to consume). The fourth, `broker_period`, is blocked by the ARTIFACT: the proof contract's `broker_evidence` is `null` and the workbook prints only the SELECTED period headers, so demand-to-selection parity has no counterpart cell. None was quietly re-marked; `validate_matrix` now REFUSES a `NOT_INDEPENDENTLY_PROVEN` flag that carries no reason, so the four cannot drift back into an unexplained claim.

## Completion receipt
- physical-authority-mutation 60 → 0. 23 cell mutations, each producing EXACTLY its expected code set and no collateral: 12 × `capture-child-regains-live-writer` (every declared capture child in every declared column → `CAPTURE_CHILD_HAS_LIVE_WRITER` + `AUTHORITY_CAPTURED_ROW_HAS_LIVE_CELL`), `capture-parent-cell-blanked` (→ `CAPTURE_PARENT_NOT_WRITTEN` + `AUTHORITY_LIVE_ROW_EMPTY_CELL`), `capture-scope-dropped-from-absorbing-identity` (→ `CAPTURE_SCOPE_NOT_IN_FINANCING`), `capture-child-row-relabelled` (→ `CAPTURE_ROW_LABEL_MISMATCH`), `derived-authority-cell-hardcoded` (→ `AUTHORITY_DERIVED_CELL_NOT_A_FORMULA`), `broker-authority-cell-loses-its-link` (→ `AUTHORITY_BROKER_CELL_NOT_LINKED`), `broker-authority-cell-marking-repainted` (→ `AUTHORITY_MARKING_CONTRADICTS_RECORD`), `live-authority-cell-blanked` (→ `AUTHORITY_LIVE_ROW_EMPTY_CELL`), 3 × `remote-cell-carries-an-error` (`Brokers!F129`, `Forward Curves!H13`, `Operating Model!U182` → `WORKBOOK_ERROR_VALUE`) and `cell-reads-as-an-error-without-the-error-type` (→ `WORKBOOK_ERROR_TEXT`).
- The red proof is kept, not spent: the suite asserts that both incumbent inspectors return ZERO findings on the mutated cell AND that both catch the same claim mutated in the JSON, so the blind spot can neither reopen nor close in silence — if either incumbent is repaired this suite goes red and forces the reconciliation.
- Non-vacuity, tested rather than asserted: baseline metrics are pinned (4 captures, 24 capture cells, 12 absorption cells, 108 authority entries, every one of the five authority rules exercised at least once, 6,822 cells error-scanned across 3 sheets); an unmutated openpyxl round trip must stay clean so a mutation's findings can never be the vehicle's; and six self-confirmation negatives prove the oracle REFUSES rather than passes when handed an empty ledger, the proof contract in place of the ledger, a row-map-shaped list, the ledger in place of the contract, an empty contract, or a contract declaring no captures.
- The oracle grew no reader: its entire import surface is `{__future__, re, sys, pathlib, verify.plan_reconstruction_oracle}`, asserted by AST, and the suite additionally asserts the token `zipfile` never appears in it — P5.4's reconstruction is the only reader of the bytes.
- The incumbent's hardcoded literal is pinned without editing its file: the suite EXTRACTS `Operating Model!J152` and `$D73` from `run_emitted_candidate_independent_oracle_tests.py`'s own source by regex, then asserts that `J152` still reads `$D73` and that rows 152 and 73 still describe the same instrument (`Senior Notes 4.875% due Nov-29`). If the emission drifts, the pin fails and the literal must be derived instead of silently pointing at nothing; if the incumbent is de-hardcoded, the pin retires itself.
- Matrix: 31/31 kills computed per domain (`mutation_detection_rate` 1.0 computed, not literal), `physical_cell_domains` = forecast_ownership, rcf_financing_cash, workbook_error_scan; governance mutations 5 → 9 (`unproven_without_reason`, `physical_scope_without_binding`, `physical_suite_path_absent`, `physical_oracle_is_its_own_reader` added, each proven to fail the exact validator used for the durable artifact); `not_independently_proven_domains` 5 → 4, each now carrying its reason in the report; `bindings.oracle_sha256` rebound to the edited oracle; `registry_sha256` NOT written by this slice — it appears in the working-tree diff because another agent modified `assets/development-test-registry.json` and rebound it concurrently while this package ran, and the matrix's own binding assertion passes against that pair.
