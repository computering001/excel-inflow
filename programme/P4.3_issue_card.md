# Work Package Issue Card

- Work package ID: P4.3
- Title: Typed schedule states — RCF draw/repay/ending, acquisition debt/cash and cash-bucket amounts carry typed financial values (ADDITIVE DUAL-READ SHADOW)
- Active invariant: Every RCF period quantity (draw, repayment, ending balance), acquisition debt/cash bucket amount and cash-bucket balance the solver produces carries a TYPED state from the sealed P1.2 vocabulary with provenance — never a bare number whose absence/zero/unresolved are indistinguishable; the forecast period count derives from the case's own forecast periods at the schedule sites; the cash definition-basis selectors this shadow evaluates are declared data, not prose string matching.
- Release/phase: v3.7.7 / Phase 4
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (working tree on top of `7b2dc7e`; not committed per work-order constraint)
- Working tree status: P4.3 delta uncommitted (2 new files + solver.mjs additive edits + this card); other agents' in-flight deltas (case_compiler, semantic_graph, row_plan, assets schemas) present in the shared tree and untouched
- Observed failure or discovery question: The P4 gap agent proved RCF draw/repay/ending, acquisition debt and cash buckets had no typed states — bare numbers where absence, zero and unresolved were indistinguishable; definition-basis selectors were prose strings; three periods hard-wired at the schedule sites.
- Earliest responsible layer: solver schedule result assembly (RCF/cash/acquisition sites in solveCase)
- Reproduction command: `node scripts/run_schedule_typed_state_tests.mjs`
- Focused failing test ID: schedule-typed-states (13 checks; red proof: "forecast 2026-12-31 carries no typed_states shadow" against the pre-change solver; mutations: bare-number smuggle caught, alien reported_* state caught, unresolved input never surfaces as typed zero, typed/numeric drift caught)
- Allowed files/modules: scripts/lib/schedule_typed_states.mjs (NEW pure compiler + validator + declared selectors), scripts/lib/solver.mjs (additive: typed-shadow attachment at the schedule result-assembly site, case-derived forecast period count at the loop/converged sites), scripts/run_schedule_typed_state_tests.mjs (NEW), this card; instrument_period_state.mjs was permitted but needed no edit (its P4.2-adjacent regions untouched)
- Explicitly out of scope (REMAINING P4.3, on the work order): consuming the typed shadow anywhere downstream (workbook emitter, coverage) — P4.4/P4.7 own solveCase restructuring; typing the instrument-period-state basis/reporting amount pairs themselves; migrating the definition-basis graph's prose source_selector strings to the structured form (the graph artifact is hash-pinned by its own validator)
- Required integration gate: user-flow 14/14, opening-debt-bridge 19 (P4.2 intact), acquisition solver case (standard-maximal-v2) PASS, funded-acquisition 16, typed-financial-value 86, typed-arithmetic 20, schedule-typed-states 13, node --check on touched files — met; solveCase solution hashes for standard-maximal-v2 (f313c476…) and standard-net-cash-v2 (c5b6ecc0…) IDENTICAL pre/post with the additive typed_states field stripped (P4.2 technique)
- Last known green commit: `7b2dc7e` (baseline for this delta; the custody-cohort equivalence full run remains the KNOWN RED 55/90/1 per programme/gap-reports/EQUIVALENCE_COHORT_BASELINE.md and was not re-run here because other agents' in-flight case_compiler deltas would contaminate the measurement; numeric identity is proved by the solution-hash equality instead)
- Rollback plan: revert the working-tree delta (delete scripts/lib/schedule_typed_states.mjs and scripts/run_schedule_typed_state_tests.mjs, restore solver.mjs hunks from 7b2dc7e, delete this card)
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: every solved forecast period now carries `typed_states` (schedule-typed-states/1.0) alongside the numeric fields — RCF opening/draw/repayment/ending, five acquisition amounts, per-bucket opening/ending balances and five cash aggregates as derived_number with derivation provenance; disabled facility/acquisition typed not_applicable (reads null, never zero); non-finite inputs typed unresolved (reads null, never zero); cash aggregate membership/weighting declared as CASH_AGGREGATE_SELECTORS data tied to the definition-basis DAG node ids and cross-checked structurally at compile; forecast period count at the loop, converged and shadow sites derives from the case's forecast periods
- Regression: none — solution hashes byte-identical pre/post on both maintained fixtures with the additive field stripped; all gate suites at or above their baselines
- Unresolved: downstream consumers still read only the untyped numbers (dual-read migration is P4.4+); instrument-period-state amount pairs remain untyped; one transient user-flow 13/14→12/14 observation traced to a concurrent agent writing assets/filings-extraction-response-v1.schema.json mid-run (runtime-integrity guard, working as designed) — clean re-run is 14/14

## Completion receipt
- schedule-typed-states 13 → 0 (red proof at the exact GAP assertion; smuggle/unresolved/drift mutations all caught; declared selectors reproduce the solver's aggregates over an explicit two-bucket case; period count checks derive from case data)
- Numeric identity: solveCase(standard-maximal-v2) f313c47605b1028dd96d49236550c7695273bbeeb8233b78c424c1cc34c941b3 and solveCase(standard-net-cash-v2) c5b6ecc0c5d0af5ca8b9d9f83b154900050a0c7d9b5a790d6f3ecd982332e85d identical pre/post change (typed_states stripped, P4.2 hash technique)
- No new terminal reason code needed: the shadow is additive and never refuses a solve; invalid shadows fail loud at compile inside the solver (construction validates against the sealed typed_financial_value contract)
