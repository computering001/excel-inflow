# Work Package Issue Card

- Work package ID: P3.7
- Title: Convert internal forecast failures into resumable internal outcomes (TYPED-OUTCOME SLICE)
- Active invariant: An internal forecast/solve failure carries a registered reason code and the registry's five payload fields; a bare stack print is not a terminal state.
- Release/phase: v3.7.7 / Phase 3
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: clean at seal
- Observed failure or discovery question: The P3 gap agent proved the three internal forecast failure sites (stage-parity refusal, ownership preflight-B block, solver non-convergence) threw raw Errors terminating in a stack print, with none of the registry's five internal-failure payload fields ever emitted anywhere.
- Earliest responsible layer: the three throw sites + the terminal catch
- Reproduction command: `node scripts/run_internal_outcome_tests.mjs`
- Focused failing test ID: internal-outcomes (registered; 16 checks incl. an END-TO-END drive of run_user_flow into a real internal failure and reading the serialised artifact)
- Allowed files/modules: forecast_completion_constitution.mjs + forecast_ownership_resolver.mjs + solver.mjs (typed_internal_outcome payloads on the existing throws — messages unchanged), run_user_flow.mjs terminal catch (serialises internal-failure.json with the five fields; untyped errors get the compiler_or_graph_defect fallback with "repair: type this throw"), terminal-reason registry (+2 forecast codes), the new suite
- Explicitly out of scope (REMAINING P3.7, on the work order): forecast-stage checkpoint recipes for resume; the Phase-6 outcome-compiler rewiring of vnext statuses
- Required integration gate: user-flow 14/14, forecast-completion 13, filings 43, terminal-registry 254, registry contract 785 — green
- Last known green commit: `eb15ab4`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED (slice; remainder tracked)

## Difference classification
- Intended correction: typed payloads added to existing throws; terminal catch serialises instead of only printing (still exits 1; behavior additive)
- Regression: none
- Unresolved: resume recipes (work order)

## Completion receipt
- internal-outcomes 16 → 0 (parity refusal carries INTERNAL.forecast_completion_escalated; static pins on the other two sites; end-to-end internal-failure.json with all five fields)
- Registry now 17 reason codes; terminal suite 254 → 0
