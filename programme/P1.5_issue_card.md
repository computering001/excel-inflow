# Work Package Issue Card

- Work package ID: P1.5
- Title: Close the ETR normalisation schema and receipt boundary
- Active invariant: Tax-rate normalisation emits a compact, valid, auditable receipt referenced consistently by forecast-period authorities.
- Release/phase: v3.7.7 / Phase 1
- Exact repository/branch/commit: `agent/excel-inflow-v377-behavioural-closure` (this commit)
- Working tree status: clean at seal
- Observed failure or discovery question: The policy embedded its full ledger on candidates per forecast period, and later normalisation STRIPPED the embed — the audit provenance silently vanished from every sealed case (worse than the duplication the spec named). Discovery also surfaced (and repaired, via the legacy adapter with sealed custody declarations) the frozen AZ fixture's pre-contract broker shape, restoring the AZ replay to clean.
- Earliest responsible layer: materialisation seam (receipt sealing) + completion constitution (reference integrity)
- Reproduction command: `node scripts/run_etr_receipt_boundary_tests.mjs`
- Focused failing test ID: etr-receipt-boundary (registered; 39 checks, subagent-authored against the real pipeline)
- Allowed files/modules changed: forecast_candidate_compiler.mjs (receipt boundary at materialisation), forecast_completion_constitution.mjs (ref acceptance + integrity escalations), model-case schema (+ref/+receipt declarations), run_case_compiler_equivalence.mjs (legacy adapter), the new suite
- Explicitly out of scope: rewriting the policy's ledger content
- Required integration gate: tax 24, filings 43, user-flow 14/14, equivalence 0 diffs, registry 765 — green
- Last known green commit: `4857acb`
- Rollback plan: revert this commit
- Attempt count (1-3): 1
- Status: SEALED

## Difference classification
- Intended correction: receipt sealed ONCE at case level (hash-bound); authorities carry refs; distinct ledgers refuse; census escalates dangling/mismatched refs
- Accepted normalization: legacy embedded receipts remain readable (migration path proven)
- Regression: none
- Unresolved (recorded observations from the suite author): re-materialisation overwrites an existing receipt silently (guarded downstream by census mismatch, not at the seam); dispositionFor accepts any-method authorities carrying a valid ref — method sanity on referencing authorities is future hardening

## Completion receipt
- 39-check suite PASS; determinism + canonicalisation proven; AZ replay clean
- Next: P1.7 landing (agent), then P1.8 dual-read + phase seal
